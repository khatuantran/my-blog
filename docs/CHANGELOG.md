# Changelog

Tuân theo [Keep a Changelog](https://keepachangelog.com/) + [SemVer](https://semver.org/).

## [Unreleased]

### Added

- **T-453 FE PostHeader avatar popup + tên → profile** (2026-05-31, FE): Feed/detail — click avatar mở `AvatarPreviewModal` (ảnh avatar phóng to; click backdrop/Esc/× đóng); click tên `~/username` → `/profile/:username`. Profile tự ẩn phần self/admin-only cho non-self viewer (gating `isSelf`/`canViewSaved` sẵn có). PostHeader.test 2 case. (Refs T-453)

- **T-446/447/448 Storage driver abstraction + Docker-dev api (ADR-010)** (2026-05-31, Both/Infra): Local dev giờ chạy **api + postgres trong Docker** (web giữ host); upload tách theo `STORAGE_DRIVER`: prod `cloudinary` (signed direct upload, bất biến), local `local` (ghi `./storage/uploads` bind-mount + serve `/uploads`, không cần Cloudinary creds). BE: `StorageDriver` interface + `LocalStorageService` + `StorageService` facade + `POST /files/upload` (multipart) + `main.ts useStaticAssets`. FE: `uploadAsset` provider-aware (đọc `provider` từ `/files/sign`). DB `publicId` provider-agnostic (không đổi schema). Docker: `apps/api/Dockerfile.dev` + compose `api` service. Tests: BE local-storage/storage unit + files e2e + FE files.test. (Refs T-446/T-447/T-448)

- **T-443 FE Create Post edit mode (`/admin/create?edit=<id>`)** (2026-05-31, FE): "Edit post" giờ prefill đầy đủ content/mood/tags/images/files + đổi label subbar `~/admin/edit-post`, status `editing`, button `Update`; submit → PATCH /posts/:id (thay POST). BE đã sẵn (PATCH/GET /posts/:id). Thêm `use-update-post` hook + RichTextEditor sync prefill async. CreatePostPage.test edit case. (Refs T-443)

- **T-440 FE Feed PostCard collapse/expand bài dài** (2026-05-31, FE): Extract `CollapsibleContent` (dùng chung Feed + Create Post preview) — clamp content cao quá ngưỡng (Feed 400px / preview 320px) cắt đúng ranh giới dòng + nút show more/collapse. CollapsibleContent.test 3 case. (Refs T-440)

- **T-439 FE Create Post live preview collapse/expand** (2026-05-31, FE): Content preview render full HTML; khi cao quá ~320px → clamp + nút `▾ show more` / `▴ collapse` ẩn/hiện full content (detect overflow qua `scrollHeight`); collapsed height cắt đúng ranh giới dòng cuối qua `Range.getClientRects()` (xử lý multi-paragraph + margin). 2 test. (Refs T-439)

### Changed

- **FR-11.8 hiển thị display name + ~/handle ở feed/detail/comment (T-455, F2)** (2026-05-31, Both): Feed/Post Detail PostHeader giờ hiển thị **CẢ** display name (`User.name`, đậm blu) **+** `~/username` (highlight cyan) xếp **dọc** (name trên / handle dưới); `[ ADMIN ]` badge + status dot + timestamp ở meta row **horizontal** phía dưới. Author chưa đặt `name` → chỉ `~/username` (làm tên chính). Comment + Reply hiển thị `name` (fallback `@username`) qua helper `authorLabel`. BE: posts/comments author select + DTO trả thêm `name: string|null`. openapi.yaml + api.generated.ts regenerated. 1 PostHeader test mới + 59 BE e2e + 47 BE unit pass. (Refs T-455)

- **FR-11.9 TẠM DISABLE FE đổi username (T-456, F2)** (2026-05-31, FE): Sau khi chuyển sang hiển thị display name (T-455), tạm tắt UI đổi handle — EditProfileDrawer field Handle trở lại read-only (`@username`, opacity-50), FE không gửi `username`. **BE giữ nguyên capability** (`UpdateUserDto.username` + unique 409) dormant — bật lại FE không cần đổi BE. EditProfileDrawer test (handle read-only). (Refs T-456)

- **AvatarMenu gate Manage Tags + System Settings cho admin (T-457, F2)** (2026-05-31, FE): Non-admin (role USER) giờ chỉ thấy `Profile` + `Logout` trong dropdown avatar; `Manage Tags` + `System Settings` thêm `adminOnly` (cùng Manage Posts + Admin Dashboard sẵn có). Browse tags vẫn mở cho mọi role qua CommandPalette / URL `/tags` — chỉ entry menu ẩn với non-admin. DESIGN_SYSTEM AvatarMenu spec sync (4 adminOnly + Role gating note). AvatarMenu test cập nhật (non-admin chỉ Profile/Logout). (Refs T-457)

- **FR-11.9 cho phép đổi handle/username (T-454, F2)** (2026-05-31, Both): Amend policy cũ "reject username change" → self/admin đổi `username` được. BE `UpdateUserDto` +username (3-32, `[a-zA-Z0-9_-]`) + unique check case-insensitive (409 `DUPLICATE_USERNAME`). FE EditProfileDrawer Handle editable (prefix @) + sau đổi `navigate(/profile/:new)` (URL cũ 404). 3 BE e2e + EditProfileDrawer test. **Note: FE phần này TẠM DISABLE bởi T-456 cùng ngày — BE vẫn active.** (Refs T-454)

- **T-451 FE phóng to ảnh post (ImageGrid variant feed/detail)** (2026-05-31, FE): Ảnh post to hơn cho social network UX — `ImageGrid` thêm `variant`: feed (single 460/2-col 340/3+ 400px) · detail (single 640/2-col 460/3+ 520px), override design-file gốc 200/160/180. Post Detail dùng `detail`. (Refs T-451)

- **T-450 FE Post Detail ảnh dùng ImageGrid collage + lightbox (thay ImageCarousel)** (2026-05-31, FE): Post Detail hiển thị ảnh dạng collage giống Feed/preview (1 full / 2 split / ≥3 trái + phải stack tối đa 4 / >4 `+N`); click ảnh mở `ImageLightbox` (popup) như Feed. `ImageCarousel` còn lại (chưa xóa). (Refs T-450)

### Fixed

- **BUG-031 FE ImageLightbox click vùng đen không đóng (F3)** (2026-05-31, FE): Image area div `stopPropagation` toàn bộ → click backdrop quanh ảnh không bubble lên root onClose. Sửa: `stopPropagation` + `if (target===currentTarget) onClose()` → click vùng đen đóng, click ảnh/nút không. 1 regression test. (Fixes BUG-031, Refs T-452)

- **BUG-030 FE bài post ngắn vẫn hiện "show more" (F3)** (2026-05-31, FE): `CollapsibleContent` đổi `overflowing` so với `collapsedH` (line-box thiếu margin) → false-positive với bài ngắn. Sửa: so `scrollHeight > maxHeight + 1` + chỉ clamp khi overflow thật. (Fixes BUG-030, Refs T-451)

- **Preview polish (T-439/T-449 follow-up)** (2026-05-31, FE): (1) nút collapse/expand hiện đúng khi content thực sự bị clamp — `CollapsibleContent` đổi điều kiện `overflowing` sang `scrollHeight > collapsedH` (đồng bộ clamp, hết case clamp-nhưng-thiếu-nút); (2) preview image grid theo collage design-file (1 full / 2 split / ≥3 trái + phải stack tối đa 4 / >4 overlay `+N`) thay vì cap 3 — khớp `ImageGrid` feed/detail.

- **BUG-027/028/029 file upload local — publish blocked + multi-upload + display (F3, ADR-010 follow-up)** (2026-05-31, Both): **BUG-028** `@IsUrl()` reject `localhost` url → không publish post có ảnh/file upload local → `@IsUrl({require_tld:false})` + SetAvatarDto cho phép `/uploads/`. **BUG-027** multi-upload chỉ giữ file cuối (UploadZone stale `value` closure) → tích lũy local. **BUG-029** badge hiện MIME thô→derive từ extension; tên unicode mojibake→decode latin1→utf8 (multer); preview thiếu attachments→PostPreview render `// attachments [N]`. test-app override StorageService (e2e deterministic). 154 BE unit + 231 e2e + 16 upload FE pass. (Fixes BUG-027/BUG-028/BUG-029, Refs T-449)

- **BUG-026 FE comment mới optimistic append cuối list thay vì đầu (F3)** (2026-05-31, FE): Comment vừa gửi flash lên đầu rồi nhảy xuống cuối sau refetch (BE sort asc, FE optimistic prepend). Đổi `onMutate` sang append `[...items, optimistic]` khớp server order. 1 regression test. (Fixes BUG-026, Refs T-445)

- **BUG-025 FE reply form đặt cuối thread thay vì đầu (F3)** (2026-05-31, FE): Ấn Reply → ô nhập hiện trên các reply cũ, đẩy chúng xuống. `CommentItem` render ReplyForm trước replies list → chuyển xuống cuối (sau replies + load-more). 1 regression test. (Fixes BUG-025, Refs T-444)

- **BUG-023/024 FE Feed popup action — bị cắt khi card ngắn + ấn lần 2 không đóng (F3)** (2026-05-31, FE): BUG-023 `<article>` PostCard `overflow-hidden` clip `PostActionMenu` (absolute, mở lên trên) khi card ngắn → bỏ `overflow-hidden` (gradient line trong suốt 2 đầu nên không lú góc). BUG-024 ấn ⋯ lần 2 không đóng: mousedown outside-handler đóng menu rồi onClick toggle mở lại → thêm `triggerRef`, outside-handler bỏ qua trigger. 2 regression test. T-441 kèm: tách tag khỏi content (mt-1) cho thoáng. (Fixes BUG-023/BUG-024, Refs T-442/T-441)

- **BUG-022 FE Create Post — text.color không dùng chung được với bold (F3)** (2026-05-31, FE): Tô màu chữ cho text bold → mất màu. Root cause: TipTap nest `<span style=color><strong>` (span màu = cha); prose CSS T-437 set `color: var(--tp)` trên `strong` (con) → đè màu kế thừa. Fix: bỏ `color` khỏi rule `strong, b` (chỉ giữ `font-weight:700`) → bold kế thừa màu span cha. 1 regression test (color span + bold coexist). (Fixes BUG-022, Refs T-438)

- **BUG-019/020/021 FE Create Post editor — preview vỡ + content HTML phình + ô dead-space (F3, engine TipTap)** (2026-05-31, FE): 3 lỗi liên quan RichTextEditor (contentEditable + `execCommand`). **BUG-020**: `execCommand` (deprecated) sinh markup `<font>`/inline-style phình to cho text ngắn + counter FE đếm text-only lệch BE `@MaxLength(50000)` (đếm HTML). **BUG-019**: `PostPreview` slice content 300 ký tự → cắt giữa thẻ HTML → preview trống/vỡ. **BUG-021**: `globals.css` chỉ port placeholder, bỏ sót prose CSS → heading/list/mark margin mặc định browser quá lớn + `resize-y` không overflow → dead-space. **Fix**: thay engine RichTextEditor sang **TipTap (ProseMirror)** (ADR-009) — markup semantic gọn, giữ trọn 11-btn toolbar + 7/7 color/highlight swatches + EmojiPicker + applyLink/onRequestLink contract; counter text-len + cảnh báo htmlLen≥45000; PostPreview render full HTML + clamp CSS (max-height, bỏ fade theo feedback) + actions row match action bar Feed/Detail (ReactionIcon LIKE + React · 💬 · Share, bỏ 🏷); port prose CSS dùng chung editor + post display + editor box min-height 220 + overflow-y-auto. Deps: +`@tiptap/{react,pm,starter-kit,extension-text-style,extension-highlight,extension-placeholder}`. Tests: RichTextEditor rewrite 11/11 (normalize `<font>`→`<strong>`, counter text-only) + PostPreview regression (HTML thẻ nguyên + clamp) + CreatePostPage stale-assumption (content HTML `<p>…</p>`) + jsdom polyfill ProseMirror geometry. (Fixes BUG-019/BUG-020/BUG-021, Refs T-435/T-436/T-437)

- **T-434 FE loading animation — animated AsciiSpinner thay text/char tĩnh (F5)** (2026-05-31, FE): User "animation khi loading của một trang đang không có". `AsciiSpinner` animate (braille 10-frame @80ms) nhưng nhiều loading state dùng tĩnh: route Suspense fallback (main.tsx) `loading…` + PostDetailPage `// loading post...` (không spinner), static `⠋` (1 frame, không spin) ở ManagePosts + Profile (×3) + Tags → đổi hết sang `<AsciiSpinner />`. (Refs T-434)

- **BUG-018 FE load-more replies hiển thị total thay vì remaining** (2026-05-31, FE): Nút load-more ghi `{replyCount}` (total 6) thay vì số reply còn lại (3). Fix `remainingReplies = replyCount - previewReplies.length` + text `↳ load {N} more {reply/replies}`. 1 regression test. (Fixes BUG-018, Refs T-433)

- **BUG-017 BE comment "post as anon" không hoạt động cho authed user** (2026-05-31, BE): User login bật "post as anon" → comment vẫn hiện tên thật (`@admin`). `comments.service.create` khi authed → luôn attribute cho user, bỏ qua `dto.anonymousName` (design + FE có toggle nhưng BE chưa honor). Fix: `effectiveUserId = viewer.userId && !dto.anonymousName ? userId : null` → authed + anonymousName = comment anon (author=null + anonymousName, không log activity/notification dưới user). 2 regression (e2e + unit) + 2 stale-assumption update. (Fixes BUG-017, Refs T-432)

- **BUG-016 FE comment/reply like count = NaN (likeCount vs likesCount field mismatch)** (2026-05-31, FE): Ấn ♡ comment/reply → `❤ NaN`. FE hand-typed `Comment.likeCount` nhưng BE/openapi field = `likesCount` → undefined → optimistic `+1` = NaN (replies cùng type → cũng NaN). Fix: rename `likeCount` → `likesCount` (type + CommentItem + ReplyRow + use-create-comment + 4 test mocks) + defensive `?? 0`. Đồng thời bỏ SaveButton khỏi Post Detail action bar (design không có save — user "bỏ icon tag"). Render verify `♡1→❤2` no NaN. (Fixes BUG-016, Refs T-431)

- **BUG-015 FE tag picker "+ add tag" rỗng — `sort=top` invalid → 400** (2026-05-30, FE): Click "+ add tag" ở `/admin/create` → dropdown rỗng + "// all tags added" sai → không add tag được. Root cause: `TagPickerDropdown` gọi `useTags({ sort: 'top' })` nhưng BE chỉ nhận `name|posts|recent` → `GET /tags?sort=top` 400 → `tagsQ.data` undefined → available rỗng. Pre-existing từ T-367 (TagSort type cũng thiếu 'top'). Fix: `sort: 'top'` → `'posts'` + thêm `isError` branch. (Fixes BUG-015, Refs T-428)

- **BUG-014 FE tag create 400 "name missing" + tag/user update no-op (doubled Content-Type header)** (2026-05-30, FE): Tạo tag qua UI luôn fail 400 `["name must be a string", ...]` dù form gửi name hợp lệ; update tag trả 200 nhưng description không lưu (no-op). **Root cause**: `client.ts > doFetch` merge headers bằng object spread `{ 'Content-Type', ...(init.headers) }` — caller `tags.ts`/`users.ts` truyền `content-type` (lowercase) → object 2 key khác case → `fetch` gửi doubled `Content-Type: application/json, application/json` → NestJS body-parser bỏ qua JSON body → `req.body` rỗng → `CreateTagDto` name undefined (400), `UpdateTagDto` (PartialType) no-op. Cùng cause âm thầm phá PATCH `/users/me` + change-password. **Fix**: doFetch dùng `new Headers(init.headers)` (case-insensitive) + set `Content-Type` chỉ khi `!has('Content-Type')`; bỏ redundant `headers: {'content-type'...}` ở tags.ts (2) + users.ts (2). Reproduce qua Playwright UI capture (smoke curl single-header che giấu bug) + curl single-vs-doubled CT đối chứng. E2E re-verify: create 201 (name normalize) + edit description persist `desc-create`→`desc-EDITED`. 2 regression test `client.test.ts` (verified fail-on-old / pass-on-new). (Fixes BUG-014, Refs T-424)

- **BUG-013 FE error message coercion + actual BE validation cause display** (2026-05-30, FE): User báo "không tạo được tag" + screenshot error generic "Invalid input · check fields". Repro qua curl + dev BE: same payload `{name,color,description}` → 201 OK; BE rejects only when field actually invalid (vd `color: ""` empty). FE generic message ẩn cause user không biết fix field nào. Fix 2 file: (1) `client.ts > parseResponse` coerce `Array.isArray(rawMsg)` → `rawMsg.join(', ')` ở source (class-validator BE trả `message: string[]` cho BadRequest, ApiError.message phải string reliable cho UI consumption); (2) `TagsPage.handleSubmitModal` 400 mapping → `Invalid input · ${actual BE message}` thay generic + add 403 "Forbidden — only admin" mapping cho clarity. **Pattern**: API error coercion ở client.ts source = 1 chỗ áp cho TẤT CẢ POST/PATCH endpoint (Posts/Users/Comments…), không cần update mỗi consumer. 21/21 Tags tests pass. (Fixes BUG-013, Refs T-423)

- **BUG-012 FE TagsPage 17 drift vs design-file Tags.html** (2026-05-30, FE): User feedback "giờ đến sync design với màn hình quản lý tags". Audit `/tags` page vs `design-file/MyBlog Tags.html` L402-660 + L224-269 → 17 drift chia 5 nhóm: **Layout shell (2)**: SubBar inline → fixed top:52px 44h bg-elev với `~/tags ── N tags · M total posts` + cyan filled New Post right; container `max-w-[1200px]` → `max-w-[1400px]`. **Stats row (2)**: RECENTLY ADDED → LEAST USED; font sizes adjust (label 10px tracking-.08em + Space Grotesk 22px value). **Toolbar (4)**: Sort `<SortDropdown>` → 3 chips Most used/A→Z/Newest (BE values `posts|name|recent` giữ nguyên, chỉ relabel UI); Search ⌕ absolute icon + × clear; View toggle `<SegmentedToggle>` chunky → 30×30 simple ⊞/☰ buttons; + New Tag outline → FILLED solid cyan + glow shadow. **Grid/Results (3)**: Results count line `// showing N of M tags · filter` MISSING; grid `cols-1 sm:cols-2 lg:cols-3 xl:cols-4` fixed → `auto-fill minmax(240px, 1fr)`; dashed-border "+ create new tag" placeholder card cuối grid (admin) MISSING. **TagCard (6)**: name textShadow `0 0 12px ${color}50` glow MISSING; `since <created>` date subline dưới name MISSING; description min-h-[36px] cards align consistent MISSING; stats row redesign (Space Grotesk 22 count + "posts" label + sparkline right); actions hover-only → always visible; emoji ✎/🗑 → SVG PencilIcon/TrashIcon (T-419 pattern). **Per user decisions**: full 17 drift sweep + SVG cyber icons + rewrite list view 5-col table strict. **Fix 4 file**: (1) Move `apps/web/src/components/admin/manage-posts/StatIcons.tsx` → `apps/web/src/components/shared/cyber-icons.tsx` cho reuse (update 2 import path PostRow + PostCardMng); (2) `TagsPage.tsx` rewrite — fixed SubBar layout + Stats 4-card LEAST USED + Sort 3 chips relabeled + Search ⌕/× absolute + 30×30 view toggle + Results count line + grid auto-fill 240 + dashed create placeholder + inline 5-col table list view; (3) `TagCard.tsx` rewrite grid variant — name textShadow glow + since formatMonth subline + min-h-36 desc + Space Grotesk 22 count + "posts" + sparkline right + always-visible actions + SVG icons; (4) Remove list variant (moved inline TagsPage). Tests: 3 stale-assumption update (TagsPage stat labels TAGGED POSTS→TOTAL POSTS + RECENTLY ADDED→LEAST USED, list view 5-col table testid, view toggle button query "List view") + 4 new BUG-012 regression case (SubBar + Sort 3 chips + Results count + TagCard "since" date + Space Grotesk count). 21/21 Tags suite + 463/466 full FE pass (3 fail pre-existing env undici). **Pattern recurring lần 9** (T-406 → BUG-008 → BUG-009 → BUG-010 → T-414 → T-415 → T-416 → BUG-011 → T-418 → BUG-012). (Fixes BUG-012, Refs T-420)

- **T-418 FE ManagePostsPage readability polish (follow-up T-417)** (2026-05-30, FE): User feedback "chữ hơi nhạt, các component hơi nhỏ" sau T-417 commit. Design `.sort-btn` (L33) dùng text-tm `#8B96AA` faint + chip padding `5×12 small` — match design pixel-perfect nhưng UX user thực tế khó đọc. Bump brightness + size lên 1 nấc đồng loạt: PostRow/PostCardMng content `text-ts → text-tp`; ID/date `text-td → text-tm`; SubBar font 12→13px + total count `font-semibold`; Status/Sort chips font 11→12 + padding `px-3 py-1 → px-3.5 py-1.5`; Mood chips padding + emoji 14px; Stats card label 10→11 brighter `text-tm → text-ts`; List header columns 11→12 `font-medium text-ts`; Search input `py-2 → py-2.5 + pl-9 + focus shadow glow`; View toggle `32×32 → 36×36` + icon 15→16px; Action buttons (View/Edit/✕) 11→12 + padding `px-2 py-1 → px-2.5 py-1.5`; Results count brighter + 11→12; PostCardMng `rounded-md → rounded-lg + p-3.5 → px-4 py-3.5`. **Lesson lần 9**: design 1:1 đôi khi xung đột UX readability (designer aesthetic prefer faint/small text; user thực tế khó đọc) — user feedback override design strict. Note vào CLAUDE.md lesson stack: "design 1:1 ≠ pixel-perfect khi readability suffer; user feedback override". 15/18 ManagePostsPage pass (3 fail pre-existing env undici/AbortSignal). (Refs T-418, follow-up T-417/BUG-011)

- **BUG-011 FE ManagePostsPage 14 drift vs design-file Manage Posts.html** (2026-05-30, FE): User feedback "hãy kiểm tra màn hình quản lý bài post và update lại cho giống design". Audit `/admin/posts` page vs `design-file/MyBlog Manage Posts.html` L457-660 + L362-454 → 14 drift chia 4 nhóm: **Layout shell (2)**: SubBar inline → design fixed top:52px 44h bg-elev với breadcrumb `~/admin/posts ── N total · {pub} published · {draft} drafts` + cyan filled New Post right; Stats row 4-card (TOTAL/PUBLISHED/DRAFTS/ARCHIVED) borderLeft 3px colored + Space Grotesk 22px MISSING. **Toolbar (4)**: Sort `<select>` → 3 chips Newest/Oldest/Top; Status chips thiếu count `(N)`; View toggle 32×32 lớn (current nhỏ); Search ⌕ icon absolute position + × clear. **Results/List/Card (5)**: Results count line `// showing {N} of {M} posts · filtered` MISSING; List header bg-bg darker + fr grid (current fixed px) + có checkbox column (design không có); PostRow stats 1-line → design 2-line (counts/date) + actions 3-button View blu/Edit cyan/✕ red; PostCardMng header layout (status+mood+date right) + stats inline 📷/📎 conditional; Card grid `auto-fill minmax(320px, 1fr)`. **Minor (3)**: Empty state thiếu `◎` 28px big icon; View toggle `▦` → `⊞`; Status labels `Published/Draft/Archived` → `Pub/Draft/Arch`. **Per user decisions**: strip bulk-select checkbox column (1:1 design strict) + full 14 drift sweep. **Fix**: 3 file refactor — `ManagePostsPage.tsx` (fixed SubBar layout + Stats row 4-card + Sort 3 chips + Status counts via 3 `useAdminPosts(status, limit:1)` mini-query + Search ⌕ absolute + ⊞ view toggle 32×32 + Results count line + empty ◎ icon + STRIP checkbox/bulk-bar); `PostRow.tsx` rewrite (6-col fr grid `3fr 1fr 1.2fr 1.4fr 1fr 1.5fr` no checkbox + stats 2-line + actions 3-button View Link blu / Edit cyan / ✕ red); `PostCardMng.tsx` rewrite (top row id+status+mood pills inline + date right + 2-line clamp content + stats row tags left + counts inline 📷 images / 📎 files conditional + actions footer 3-button). Tests: 4 new BUG-011 regression (Stats row + Sort 3 chips + Results count + strip checkbox/bulk-bar) + 1 stale-assumption update (empty state text + ◎ icon). 15/18 ManagePostsPage pass + 461/464 full FE pass (3 fail pre-existing env undici/AbortSignal). **Pattern recurring lần 8** (T-406 → BUG-008 → BUG-009 → BUG-010 → T-414 → T-415 → T-416 → BUG-011) — promote rule "BẮT BUỘC grep `design-file/*.html` source markup trước rewrite page/section" lên CLAUDE.md Pre-flight Checklist là task ưu tiên TIẾP THEO (defer riêng). (Fixes BUG-011, Refs T-417)

- **T-416 FE ProfileActivityList strip icons → design plain caret pattern (reverse T-413 decision)** (2026-05-30, FE): User feedback "phần này tôi muốn update lại đúng như design" + screenshot recent.actions list. T-413 quyết định "giữ icon-based với Feed-aligned icons" giờ đảo theo user. Refactor `ProfileActivityList.tsx` theo design L600-614: bỏ ICON_MAP const + icon span hoàn toàn; thêm `❯` cyan 11px prefix (testid `activity-caret`); 2-line layout: line 1 = `{user_blu} {verb} {target_muted}` (15px text-tp, actor mono blu, target mono text-tm hover cyan + Link nếu có snippet), line 2 = time mono 11px text-td (was right-aligned same row → giờ below). Row container `flex items-start gap-2.5 + py-2.5 + border-b border-b1`. Test stale-assumption update: 4 emoji icon assertion (`✏️/💬/♡/🔖`) → 4 `activity-caret` testid count + queryByText null cho icon. 5/5 ProfileActivityList + 12/12 ProfilePage pass. **Lesson**: khi user nói "đúng như design" nghĩa là 1:1 strict, không hybrid (T-413 user OK "giữ icon-based"; T-416 user đảo về plain caret). (Refs T-416, reverse T-413/BUG-010)

- **T-415 FE ProfilePage hero action buttons (New Post + Settings) 1:1 design-file (follow-up T-413)** (2026-05-30, FE): User screenshot 2 button khác design. 2 drift fix: (1) layout `flex-col gap-2` STACKED → design L512 horizontal `flex gap-2 self-end pb-1`; (2) New Post `border border-cyan bg-cyan/[0.14] text-cyan` (outline tint) → design L513-515 FILLED solid `bg-cyan + border-none + text-[#0A0E1A] dark + font-semibold + shadow 0 0 14px rgba(0,255,229,.3)`. Settings padding minor `px-4→px-3.5` (≈ design 14px). 12/12 ProfilePage pass. **Pattern recurring lần 6** (T-406 → BUG-008 → BUG-009 → BUG-010 → T-414 → T-415). (Refs T-415, follow-up T-413/BUG-010)

- **T-414 FE HeatmapGrid compact variant 1:1 design-file (follow-up T-413)** (2026-05-30, FE): User screenshot sidebar `// activity.28d` block khác design. Audit compact variant vs `design-file/MyBlog Profile.html` L702-711 → 4 drift: thiếu day labels row, cells fixed `12×12px` → `repeat(7,1fr) × 12`, gap 2→3px, less/more legend hiển thị → bỏ (design sidebar không có legend; chỉ large variant L594-598 có). Fix `HeatmapGrid.tsx` compact variant: render day labels top row + cells `1fr` width + gap `3px` + remove legend block. `DAY_LABELS` const đổi `['Su','Mo','Tu','We','Th','Fr','Sa']` (2-char Sun-Sat) → `['M','T','W','T','F','S','S']` (single-char Mon-Sun) khớp design L243 — áp cho cả compact + large. Use `key={index}` cho DAY_LABELS map vì labels lặp (T xuất hiện 2 lần, S 2 lần). 2 new test (compact day labels + no legend / large legend present). 5/5 HeatmapGrid + 12/12 ProfilePage pass. (Refs T-414, follow-up T-413/BUG-010)

- **BUG-010 FE ProfilePage 3 tab (Saved/Activity/About) 12+ visual + structural drift vs design-file Profile.html** (2026-05-30, FE): User feedback "ở trang profile, các tab saved, activity, about đang khác với design hãy fix". Audit 3 tab body vs `design-file/MyBlog Profile.html` L569-664 + L584-598 (heatmap) → 12+ drift chia 3 nhóm. **Saved tab**: missing `// saved.posts <N items>` sb-lbl header → add. **Activity tab**: rename `// activity.28d`→`// contribution.activity` + meta `last 28 days · {N} commits`; HeatmapGrid wrap card p-5 + bg-surf + rounded-lg; add `// recent.actions` sub-header trước ProfileActivityList. **About tab** rewrite: bio card thêm inner `// bio` sub-label + content 15px + lineHeight 1.75 (was 14 text-sm); skills span thêm `❯` 8px prefix + per-color glow shadow `0 0 8px ${color}15`; NEW `<ProfileInfoGrid>` component **replace** `<InfoGrid>` (xóa) — 8 cell tile Profile metadata (Full name/Handle/Role/Born/Location/Joined/GitHub/Website) mỗi cell bg-elev + border-b2 + UPPERCASE 10px label + 13px value (mục đích đổi từ STATS aggregation → PROFILE metadata per user decision 1:1 design 1:1). **HeatmapGrid** refactor — add `variant?: 'compact' | 'large'` prop; large variant render Su/Mo/Tu day labels top row + cells h-[18px] + gap 4 + boxShadow glow khi count>1 + less/more legend right-aligned (design L584-598). **ProfileActivityList** ICON_MAP align Feed page set: POST `📝→✏️`, LIKE `👍→♡` (COMMENT 💬 + SAVE 🔖 đã match Feed, giữ). **Per user decisions**: About Info grid → 1:1 design Profile info (redundant với hero acceptable); Activity list → keep icon-based với Feed-aligned icons (không strip icon hoàn toàn). Regression test: 2 case BUG-010 (Saved `// saved.posts` + 0 items / Activity `// contribution.activity` + `// recent.actions` + heatmap-large testid). 3 stale-assumption test update: about tab `// info`→`// profile.info` + 6 profile-info testid; ProfileActivityList icons 📝→✏️ + 👍→♡. 37/37 profile suite + 456/459 full FE pass (3 fail pre-existing ManagePostsPage env). **Lesson lần 4** (T-406 → BUG-008 → BUG-009 → BUG-010): pattern recurring → promote rule "grep design-file/\*.html source markup cho tab/section body" lên CLAUDE.md Pre-flight Checklist. (Fixes BUG-010, Refs T-413)

- **BUG-009 FE EditProfileDrawer (Settings panel) 7 visual drift vs design-file Profile.html** (2026-05-30, FE): User feedback "phần setting này cũng đang khác khá nhiều so với design file" + screenshot. Rewrite `apps/web/src/components/profile/EditProfileDrawer.tsx` 1:1 theo `design-file/MyBlog Profile.html` L347-439 (function EditProfileDrawer) + L57-60 (.edit-inp + .edit-lbl CSS): (1) Drawer width `max-w-[420px]` → `max-w-[480px]` + border-left cyan tint + shadow-[-20px_0_60px_rgba(0,0,0,0.8)]; (2) Header thêm subline `~/settings/profile` mono 11 `text-td`; (3) `×` close bordered box → plain inline `bg-transparent + text-[24px] + leading-none`; (4) Field labels thêm `uppercase + tracking-[0.05em] + text-[11px]` (FULL NAME / TITLE / BIO style); (5) Input `text-mono-sm py-1.5 rounded-sm` → `text-[14px] py-2 rounded-md` per `.edit-inp`; (6) Save Changes outline → **filled solid** `bg-cyan + text-[#0A0E1A] + font-semibold + shadow 0 0 14px rgba(0,255,229,.3)` (data-testid `save-changes-btn`); (7) Cancel thêm `bg-elev + px-5 py-2`. **Security exception** (per user): giữ 3 password fields (Current+New+Confirm) vs design 2 fields — BE require currentPassword + best practice. **Deferred**: Avatar upload section (design L378-385: ProfileAvatar 56 + ↑ Upload button) → spawn F2 amend FR-11 task riêng (T-410+) vì cần BE endpoint mới + Cloudinary + FR amendment. Regression test thêm 2 case BUG-009 (2-line header + Save Changes filled cyan + Field label uppercase tracking). 11/11 EditProfileDrawer + 10/10 ProfilePage pass. (Fixes BUG-009, Refs T-409)

- **BUG-008 FE PostMiniCard 8 visual drift vs design-file Profile.html** (2026-05-30, FE): User feedback "phần card trong profile hiện tại tôi vẫn chưa thấy giống design" + screenshot design card mẫu. Rewrite `apps/web/src/components/profile/PostMiniCard.tsx` 1:1 theo `design-file/MyBlog Profile.html` L52-55 (.post-mini CSS) + L292-344 (function PostMiniCard): (1) Tags `text-[11px] color: t.color` plain → pill chip `bg ${color}15 + border ${color}40 + rounded-[3px] + px-1.5 + py-px`; (2) `read →` plain link → bordered cyan pill `border-cyan/25 + rounded + px-2 + py-1`; (3) Mood badge wrap `ml-auto` (push right); (4) Action row `gap-3` → `gap-0.5` (~2px); (5) Like + 💬 buttons thêm `px-2 py-1 + rounded`; (6) Content `text-sm` (14px) → `text-[15px]` + `leading-relaxed`; (7) Thumb `rounded-sm` (2px) → `rounded` (4px); (8) Header `mb-2` → `mb-2.5` (10px). Regression test `apps/web/tests/components/profile/PostMiniCard.test.tsx` thêm case BUG-008 assert tag pill có bg+border color style + read link có `border-cyan` class. 5/5 PostMiniCard + 10/10 ProfilePage pass; 448/451 FE (3 fail pre-existing ManagePostsPage env). **Lesson re-confirm**: rule từ T-406 — BẮT BUỘC grep `design-file/*.html` source CSS + markup trước khi code styling component. DESIGN_SYSTEM mô tả high-level, không thay thế design-file source for pixel-level. (Fixes BUG-008, Refs T-408)

- **BUG-007 FE SearchPage BigSearchInput hiển thị 2 dấu × clear** (2026-05-30, FE): `input type="search"` ở Chrome/Safari render native `::-webkit-search-cancel-button` + custom × button trùng → user thấy 2 dấu ×. Fix: `[&::-webkit-search-cancel-button]:appearance-none` ẩn native. (Fixes BUG-007, commit `b7f463d`)

### Changed

- **T-430 FE Post Detail design-file 1:1 sync (F5)** (2026-05-30, FE): Đọc trọn `MyBlog Post Detail.html` (512 dòng). Layout: `max-w-1100 centered` → full-width flex + main cap 820 centered (px-8 py-5) + **MetaPanel sticky bordered sidebar** (border-l bg-elev full-height overflow-auto) thay block thường. Breadcrumb `← feed` → cyan + post-id muted (swap). Actions bar: bỏ `──` divider → `border-y` wrap. Comments header: split màu `❯` cyan / `// comments` tp / `[N]` td (was all-cyan). MetaPanel: meta rows `border-b` + share per-brand colors (FB/X/Telegram/Copy). Deviation giữ: SaveButton (feature, design không có) + `// related` placeholder (defer) + "Reactions" label. 1 stale test (comments header split). (Refs T-430)

- **T-429 FE Typography readability bump toàn hệ thống (F5, token change override design)** (2026-05-30, FE): User "chữ trắng/cyan khá mờ + cỡ nhỏ, cho đậm + to lên 1 xíu". Base `body font-weight 400 → 500` (đậm global; explicit weights vẫn override). fontSize +1px text/label: `mono-tiny..mono-lg` (8/9/11/12/13/14 → 9/10/12/13/14/15) + Inter `small` 13→14, `body` 15→16. KHÔNG đụng headings + chrome arbitrary-px (toolbar/buttons) → no layout break. 1 stale test update (design-tokens mono-tiny). (Refs T-429)

- **T-428 FE Create Post layout + behavior fidelity round 2 (F3/F5, post-T-427)** (2026-05-30, FE): Nhiều vòng user feedback "không giống"/"khác" (render design qua HTTP-server để so 1:1). **Fix BUG-015** tag picker. **Layout**: SubBar in-flow box → fixed full-width `top:52px` + `mt-24`; thêm editor placeholder CSS + text "Start writing...". **Buttons**: Publish outline → **solid cyan dark-text weight-600**; Save Draft `px-5`; subbar font 12. **StatusBar**: AppLayout thêm info `● draft · unsaved` + fix path color (`text-tm` đè `text-cyan` → path giờ cyan). **TopBar z-index**: `z-50` → `z-topbar` (100) — fix AvatarMenu dropdown bị SubBar (z-90) che. **LinkInsertModal** rewrite (header 🔗+subline+×, `DISPLAY TEXT`/`URL` label, `❯` prefix, hint, solid `↵ Insert Link`). **Tag dropdown** (design L737): header `// system.tags · N available`+× + chip `+ #name` per-color + empty `◎ all available tags selected`; **absolute → in-flow** (fix scroll/mất-content). **Popover behavior**: color/highlight/emoji giữ mở khi click, chỉ × đóng + multi-insert saveSelection. **Emoji**: groups xếp ngang + label lowercase. Full FE 466/469 (3 pre-existing ManagePostsPage env). (Refs T-428, Fixes BUG-015, follow-up T-427)

- **T-427 FE Create Post toolbar uniform + popover drawers + AI suggest UI (F5, follow-up T-426)** (2026-05-30, FE): User feedback "toolbar chiều cao item không đều nhìn xấu + popup khi click icon khác design" (render design qua HTTP server :8765 để so 1:1). **RichTextEditor**: `ToolbarBtn` fixed `32×30` uniform (`h-[30px] min-w-[32px]` inline-flex center 13px) — was `px-2 py-1` content-based → buttons lệch chiều cao — trong bordered strip; reorder `B I U S 🖍 H1 H2 • 1. 🔗 ✕ A▾ │ 🙂` + active cyan tint khi popover mở. Popover color/highlight/emoji → **connected drawer** per design: `text.color:`/`highlight:` label + swatches + × close; highlight A-preview swatches + `✕ clear`; EmojiPicker drawer + `// emoji.picker · click to insert` header. **CreatePostPage** content-header thêm `✨ AI suggest` button (purple **UI shell** — modal "sắp ra mắt", generate+BE defer T-347/FR-17) + format hints `⌘B bold · ⌘I italic · ⌘U underline · highlight selection to format`; tags `// select from system tags only` hint. **Mood giữ pill+label+order** (user quyết, không icon-only). DESIGN_SYSTEM toolbar-btn spec synced. 42/42 create-post suite (1 new AI button test). (Refs T-427, follow-up T-426)

- **T-426 FE Create Post design-file 1:1 sync (4 visual drift, F5)** (2026-05-30, FE): Đối chiếu `design-file/MyBlog Create Post.html` ↔ `/admin/create` (Playwright render). (1) Save `⌘S Save` → `⌘S Save Draft`; (2) images dropzone → 2-line `❯ drag & drop or click to upload` + `PNG, JPG, WebP · max 5MB each`; (3) files dropzone → `❯ drag & drop docs, PDFs, spreadsheets...` + `PDF · DOC · DOCX · XLS · XLSX · TXT · CSV · max 20MB each`; (4) tags label → `// tags (N selected)` (move count vào label, bỏ `TagPickerDropdown` footer `N/max tags selected` redundant). `UploadZone` thêm `subHint?: string` prop (2-line dropzone) — khôi phục đúng DESIGN_SYSTEM spec (FE đã drift sang "slots left"). DESIGN_SYSTEM UploadZone spec synced 2 variant. 1 stale-assumption test update; 49/49 (UploadZone+create-post+CreatePostPage) pass. **Gap chưa làm (defer):** AI suggest (FR-17 / T-347 TODO) — feature riêng cần BE `POST /ai/generate` + AI_API_KEY. (Refs T-426)

- **T-425 FE TagCard readability — since-date + description đậm hơn (F5, override design)** (2026-05-30, FE): User feedback "phần này trong card tags mờ quá cho đậm hơn". Grep design-file confirm code match 1:1 (`MyBlog Tags.html` L239 since `#566176`=text-td, L252 desc `#8B96AA`=text-tm) → KHÔNG drift, là user override readability (cùng lesson "design 1:1 ≠ pixel-perfect khi readability suffer" — T-405/T-418). Bump 1 nấc: `since` text-td → text-tm; description text-tm → text-ts. Pure color token swap, no behavior change. DESIGN_SYSTEM.md TagCard spec synced (Body `--tm → --ts` + since-date line + ⚠️ override note chống revert khi sync design-file 1:1). 21/21 Tags suite (TagCard+TagModal+TagsPage) pass. (Refs T-425)

- **T-419 FE CyberIcons shared SVG line-art set (heart/comment/eye/edit/trash)** (2026-05-30, FE, backfill via docs audit): Replace emoji ♡/💬/👁/✎/✕ trong Manage Posts bằng 5 SVG line-art component (viewBox 24, strokeWidth 1.6-1.8, no fill, line-cap round — cùng style ReactionIcon T-357). Commit `36f85c7` tạo `apps/web/src/components/admin/manage-posts/StatIcons.tsx`; T-420 (`92f84b9`) move → `apps/web/src/components/shared/cyber-icons.tsx` cho reuse (TagCard dùng PencilIcon/TrashIcon). Resolve dangling "T-419 pattern" reference đã ghi sẵn ở BUG-012/T-420. DESIGN_SYSTEM.md thêm CyberIcons section. (Refs T-419, commit `36f85c7`)

- **T-422 FE ManagePostsPage PostCardMng equal-height + toolbar polish (F5, follow-up T-417/T-418)** (2026-05-30, FE, backfill via docs audit): Chuỗi 6 commit visual polish (`22d7ea0`/`4807d5e`/`3b57884`/`e94448d`/`50d8472` PostCardMng + `3b1b443` toolbar) chưa có task/CHANGELOG/PROGRESS. PostCardMng: equal card height + grid `minmax(0,1fr)` + actions explicit 1fr×3 grid w-full + tags/stats/actions anchored đáy card (flex-col + mt-auto) + show-more toggle. Toolbar (ManagePostsPage): sort chips + status chips + search + view toggle đồng h-9 (36px). Behavior unchanged — ManagePostsPage suite 15/18 pass (3 fail pre-existing env undici/AbortSignal). (Refs T-422, follow-up T-417/T-418)

- **T-407 FE NotificationBell dropdown design refactor (F5)** (2026-05-29, FE): User cung cấp screenshot design notification popup. 4 nhóm thay đổi: (1) Header inline → 2 dòng (title + `N unread · M total`); (2) Tab bar underline → pill style với cyan glow active; (3) Mark-all-read text link → pill bordered cyan; (4) Footer single link → 2-col (`// click to mark as read` left + `view all →` right). NotifRowBell: `@username` prefix (anon `[anon]` → `@anon`), helper `shortVerb` per-reaction subtype (LIKE→"liked", LOVE→"loved"), `● new` chuyển right vertical-center, bỏ line snippet placeholder targetId (T-403 add metadata.snippet sau). Test stale-assumption: `[anon]` → `@anon`. 15/15 notif tests pass. (Refs T-407, commit `0958c51`)

- **T-406 FE ProfilePage tabs + buttons + count badge refine (F5)** (2026-05-29, FE): User feedback "phần filter này đang nhỏ hơn design gốc". **Initial commit `f54f9bb`** + 3 follow-up iterations sau user feedback "vẫn khác" → final state `4e92643` đọc design-file 1:1. (1) Tabs Saved/Activity bỏ `hidden` → 4 tab luôn visible (content gate `canViewSaved`). (2) `TabButtons.tsx` **final state per `design-file/MyBlog Profile.html` L49-50 `.tab-btn`**: `text-mono-md (13px)` + `py-1.5 px-4` (= design `padding:6px 16px`) + `border-b-2` + transition all .15s + active `text-cyan border-cyan` — **KHÔNG box-shadow / glow** (design không có; iterations đầu tự bịa glow đã bị revert). Count badge per L532 inline style: `bg-elev + border-b2 FIXED` (không đổi theo active) + `text-cyan` (active) / `text-td` (inactive) + `rounded-[3px] px-[5px] py-0 text-[11px]`. (3) New Post button: solid cyan filled glow `border-cyan bg-cyan/[0.14] text-mono-md px-4 py-2 + shadow 0 0 14px rgba(0,255,229,.3)`. Settings đồng kích cỡ `bg-elev px-4 py-2 text-mono-md`. Test stale-assumption: non-self viewer Saved/Activity vẫn visible. 67/67 affected tests pass; 447/450 FE. **Lesson**: lần đầu tự bịa glow + cyan-tint badge active thay vì đọc `.tab-btn` spec — rule: grep design-file `.html` source CSS trước khi code styling. (Refs T-406) (2026-05-29, FE): User feedback "phần filter này đang nhỏ hơn design gốc, nút new post và setting cũng hơi khác". (1) Tabs Saved/Activity bỏ `hidden` conditional → tất cả 4 tab luôn visible theo design (content vẫn gate `canViewSaved`). (2) `TabButtons.tsx` styling bigger: `text-mono-sm` (11px) → `text-mono-md` (13px), `py-2` → `py-2.5`, active tab thêm cyan shadow glow `0 2px 8px -2px rgba(0,255,229,.5)`; count badge bordered `border-b2 bg-b1` (inactive) / `border-cyan/30 bg-cyan/[0.08] text-cyan` (active) thay flat bg-b2. (3) New Post button: solid cyan filled glow `border-cyan bg-cyan/[0.14] text-mono-md px-4 py-2 + shadow 0 0 14px rgba(0,255,229,.3)` (was `border-cyan/50 bg-cyan/10 text-mono-sm px-3 py-1.5`); Settings đồng kích cỡ `bg-elev px-4 py-2 text-mono-md`. Test stale-assumption: non-self viewer test giờ assert Saved/Activity vẫn visible. 67/67 affected tests pass; 447/450 FE. (Refs T-406)

- **T-405 FE ProfilePage hero deco + bio màu refine (F5)** (2026-05-29, FE): User feedback "UI phần này chưa giống" sau T-404 — soi kỹ design screenshot zoom. (1) Hex deco corner 3-line cũ `01001101 / uid:xxx / pid:xxx` → 4-line cyberpunk pattern theo design: `#DEAD-BEEF-XXXX-XXXX` (slice từ user.id) + `01101010-10101010` (static binary) + `#ID NNNN . PID NNNN` + `MID NNNN . MID N` (sum charCode mod 10000 deterministic). Helper `deco4(id, offset)` + `decoNum(id, offset, len)` ngay trong ProfilePage. (2) Bio: `text-tm` (#8B96AA muted blue-gray) → `text-ts` (#A0AEC0 sáng hơn) khớp design. Test stale-assumption: assert `uid:u-alice` regex → 4 line patterns. 10/10 ProfilePage pass; 447/450 FE pass. (Refs T-405)

- **T-404 FE ProfilePage hero + stat cards + sidebar design update (F5)** (2026-05-29, FE): User-provided design screenshot 2026-05-29. Hero handle row gộp `~/user · title · born year` single line (was `@user` block + title/bio separate); thêm meta icons row `📍 location · 📅 joined YYYY-MM · # github · 🌐 website` (conditional theo field present); bỏ inline stats meta row. Thêm 4 `<StatCard>` section dưới TabButtons khi `tab='posts'`: POSTS cyan / LIKES mag / VIEWS yel / STREAK pur, sparkline từ `stats.heatmap28d.map(c=>c.count)`. Sidebar: skills.top chip thêm `›` prefix; activity.28d title show total count (sum heatmap counts); tags.used flat → list rows `<TagPill>` + `N posts` count right. Test stale-assumption: `@alice`→`~/alice`, `/day streak/i`+`'12'`→`'12d'`+`'STREAK'`. 10/10 ProfilePage pass; 447/450 FE pass. (Refs T-404)

- **T-402 FE TopBar search → click navigate /search (F5)** (2026-05-30, FE): User report: ở Feed, click ô search trên TopBar phải qua thẳng `/search` (design-file Feed.html L1233 dùng `<input readOnly onClick={navigate '/search'}>`); hiện cần nhập + Enter. `TopBar.tsx`: bỏ `useState(searchInput)` + `handleSearchSubmit`, `<form onSubmit>` → `<div role="search">`, input thêm `readOnly` + `onClick`/`onFocus` cùng gọi `navigate('/search')` + `cursor-pointer` + `hover:border-b3`. Mọi typing diễn ra ở `BigSearchInput` của `/search` (đã `autoFocus`). UI_DESIGN.md TopBar Search row spec cập nhật. Tests T-232 (form submit) → stale-assumption (contract đổi hợp lệ), thay bằng 2 case mới T-402 (click navigate + focus navigate parity). 11/11 TopBar tests pass. (Refs T-402, design-file Feed.html L1233)

- **T-401 FE Search page follow-up adjustments (F5)** (2026-05-30, FE): User feedback sau T-400: (1) `BigSearchInput` `max-w-[720px]` → `max-w-[820px]` để input ngang width result card; (2) bỏ sidebar 4 StatBox (Total/Images/Files/Saved) — design Search results không có sidebar — wrap results trong `max-w-[820px] mx-auto`; (3) `FilterChip` (shared, dùng riêng SearchPage) `text-mono-sm` (11px) → `text-mono` (12px) khớp design `.filter-chip` 12px, cùng pattern T-399. Filter row container max-w 720→820. 16/16 search tests pass (không assert sidebar / font class). (Refs T-401, design-file Search.html)

### Added

- **T-421 Both FR-11.8 Contact + identity fields (name/location/bornYear/github/website)** (2026-05-30, Both): User feedback "API /me thiếu fields fill profile". Audit gap: `AuthUserDto` chỉ trả 6/15 expected fields (thiếu avatarPublicId/title/bio/skills đã có DB + 5 contact fields chưa migration); `UpdateUserDto` thiếu 5 contact → EditProfileDrawer FE (T-376) PATCH bị 400 forbidNonWhitelisted khi submit contact section. F2 amend FR-11.8 align FE-BE end-to-end. **Migration**: `20260530180000_add_user_contact_fields` (5 ADD COLUMN nullable, non-breaking; applied main + test DB). **BE**: `UpdateUserDto` +5 fields với class-validator (bornYear `IsInt @Min(1900) @Max(currentYear)`, name+location+github+website `IsString MaxLength` 80/80/120/200; github/website KHÔNG strict IsUrl để allow handle hoặc URL); `UserResponseDto` +5 nullable; **`AuthUserDto` expand 6→15** (id/username/email/role/avatarUrl/createdAt + avatarPublicId/title/bio/skills + 5 contact mới) cho FE useAuth consumer 1-query; `toAuthUser` + `toUserResponse` helpers map đủ; `UsersService.update` propagate 5 fields conditional. **FE**: `AuthUser` type (hand-typed `apps/web/src/services/api/auth.ts`) expand 6→15 cùng shape; `api.generated.ts` regenerated. **Tests**: 5 new BE e2e (3 PATCH happy/bornYear non-int/bornYear<1900/github>120 + 1 AuthUserDto include 9 expanded fields). 46/46 affected pass (was 41 → +5). **Docs sync**: REQUIREMENTS FR-11.8, DATA_MODEL v0.6.0-alpha + Prisma snippet, API_CONTRACT PATCH /users/me Notes + /auth/me row shape, MIGRATIONS.md log. (Refs T-421, FR-11.8, UC-14 extends)

- **T-412 FE Avatar upload — AvatarUploadModal + EditProfileDrawer integration (FR-11.7 step 3/3)** (2026-05-30, FE): Install `react-easy-crop@^5.5.7` (~30KB) cho crop UI. NEW `services/api/avatar.ts` (3 endpoint wrap + `uploadAvatarChain` helper chain: sign → Cloudinary direct upload → PATCH save). NEW `hooks/mutations/use-avatar.ts` (`useUploadAvatar` + `useRemoveAvatar` — invalidate `['auth-me']` + `['user-by-username', username]` on success → refetch khắp UI surface). NEW `components/profile/AvatarUploadModal.tsx` 480px portal — Cropper aspect 1:1 + zoom slider 1-3× + `getCroppedBlob(src, area)` canvas-draw 400×400 jpeg quality 0.9 → upload chain. EditProfileDrawer integrate `// avatar` section đầu form (above // basic.info): ProfileAvatar 56 preview real avatarUrl + `↑ Upload` button mở native file picker + `× Remove` button conditional (chỉ visible khi avatarUrl ≠ null) + ConfirmDialog flow. FE inline validate mime ∈ {jpeg/png/webp} + size ≤ 5MB qua toast.showToast reject trước khi mở modal. `ProfileUser` type (api.ts hand-typed) thêm `avatarPublicId?: string|null`. **Test note**: AvatarUploadModal unit test SKIP vì react-easy-crop dùng requestAnimationFrame + Image hang trong jsdom; thay vào đó mock entire `@/components/profile/AvatarUploadModal` trong EditProfileDrawer integration test. 4 new regression case T-412 (avatar section render + Remove button conditional visibility + Remove ConfirmDialog → DELETE flow). ConfirmDialog `confirmLabel: 'Confirm remove'` thay vì `× Remove` để tránh button name conflict với trigger. ProfilePage.test wrap thêm `<ToastProvider>` vì EditProfileDrawer (open=false) vẫn run useToast hook trước early return. 35/35 profile suite + 454/457 full FE pass (3 fail pre-existing ManagePostsPage env). **FR-11.7 Avatar upload chain HOÀN TẤT** (T-410 → T-411 → T-412). (Refs T-412, FR-11.7)

- **T-411 BE Avatar upload — 3 endpoints (FR-11.7 step 2/3)** (2026-05-30, BE): `POST /users/me/avatar/sign` (Cloudinary signed params, folder=`avatars/`, publicId=`<userId>-<ts>`, throttle 10/min) + `PATCH /users/me/avatar {url, publicId}` (validate publicId prefix `avatars/<userId>-` chống cross-user PII injection + url Cloudinary domain regex + cleanup old `avatarPublicId` via `cloudinary.destroyMany()` best-effort + save 2 field) + `DELETE /users/me/avatar` (destroy + null fields, idempotent). NEW DTO `SetAvatarDto` + `AvatarResponseDto`. UsersService inject `CloudinaryService` via `FilesModule` import. Routes declared TRƯỚC `:id` để Nest match `me/avatar` đúng. UserResponseDto thêm `avatarPublicId?: string|null` (hide ở `toPublicUserResponse`). Skip server transformation `c_fill,g_face,w_400,h_400` vì FE đã crop square via react-easy-crop — redundant. OpenAPI yaml + FE types regen synced. Tests: UsersService.spec 19/19 (+6 mới) + users.e2e 29/29 (+8 mới) + full BE e2e 221/221 pass. (Refs T-411, FR-11.7)
- **T-410 BE migration `add_user_avatar_public_id` (FR-11.7 step 1/3)** (2026-05-30, BE): Schema add `User.avatarPublicId String?` nullable cho Cloudinary publicId cleanup tracking. Migration `20260530155000_add_user_avatar_public_id` (1 ADD COLUMN, non-breaking). Applied cả main DB :5434 + test DB :5433. Non-interactive `prisma migrate dev` blocked trong env → tạo migration folder + SQL manual rồi `migrate deploy`. UsersService.spec 13/13 pass — Prisma client regen không break. (Refs T-410, FR-11.7)

- **T-403 Both Notification snippet trong metadata (design Bell popup line 2)** (2026-05-29, Both): Extend `NotificationMetadata` thêm optional `snippet?: string` để FE render line 2 quoted post/comment snippet match design notification popup. **BE**: `deriveSnippet(text)` helper trong `notifications.service.ts` (strip HTML tag + collapse whitespace + truncate 80 chars + `…` marker); `CreateNotificationInput` thêm `snippet?: string`; `createNotification` merge derived snippet vào `metadata.snippet` khi caller cung cấp source text. 3 trigger site enrich: `reactions.service` truyền `snippet: post.content` (cần update select thêm `content`), `comments.service` COMMENT + REPLY truyền `snippet: comment.content`. **FE**: `NotificationMetadata.snippet?: string` (api.ts hand-typed). `NotifRowBell` thêm line 2 italic muted truncate `"snippet"` khi present (skip gọn nếu absent). `NotifRowPage` thay line targetId `#abcd…` bằng snippet (giữ targetId fallback cho notification cũ pre-T-403 — backward compat). Tests: BE 5 unit `deriveSnippet` (null/HTML/whitespace/truncate/intact) + 1 e2e integration (bob react alice's post → metadata.snippet present & matches truncated content). FE 2 case (NotifRowBell + NotifRowPage render snippet quoted khi có / fallback khi absent). 55/55 affected BE e2e pass; 447/450 FE. Future: SHARE notification create site (chưa implement trong BE — defer). OpenAPI yaml regen + FE types synced. (Refs T-403, design Bell popup line 2)

- **T-400 FE Search page design-file 1:1 sweep (F1)** (2026-05-30, FE): Enrich ResultCard với 6/8 element còn thiếu vs `design-file/MyBlog Search.html`, redesign Recent searches + Browse tags, tweak filter row. **No BE change** — `GET /search` đã trả đủ PostView (author/tags/files/counts/createdAt). `ResultCard.tsx` rewrite 67→139 lines: avatar 26×26 cyan border + gradient inline, author `~/username` 13px blu, `[ ADMIN ]` badge 10px conditional, separator + timestamp `[YYYY-MM-DD HH:MM]` 11px, MoodBadge inline 11px right-aligned (ml-auto), content 15px line-clamp-2, tags inline per-color 11px (NOT shared TagPill), `📎 N files` red badge conditional, engagement `♡ reactions 💬 comments` 11px ml-auto, post-id corner deco giữ. `SearchPage.tsx`: mood emoji 15→14px + rounded-sm→rounded-[5px], reset button "× reset" → "reset ×" + ml-auto, Recent searches plain text → chip-pill 13px với `↺` icon prefix, Browse tags shared TagPill → custom chip 13px per-color bg + postCount number, empty-state wrap max-w-[820px]. 8 test mới: ResultCard 6 case + SearchPage 2 case (recent chip ↺, browse tag count). 445/448 FE pass. (Refs T-400, design-file Search.html)

### Notes

- **Design Fidelity Audit + fix — M11.10 (code-level, 11 screens)** (2026-05-28→29, Docs+FE): Deep code-level diff `design-file` vs FE (`docs/audits/DESIGN_FIDELITY_2026-05-28.md`) — 4 bucket (typography/components/animations/colors), capture được animation/hover mà screenshot bỏ sót. 5 systemic theme + per-screen drift. Fix qua 6 wave T-390→T-395 + T-397 (EmojiPicker wire). T-396 re-verify: **0 Critical còn lại** — UI đạt chuẩn design-file v2 (trừ defer/backlog có task riêng: AI suggest T-346/347, related posts M11, born-year meta). User decisions: Profile hero → full name; mono font → token-based normalize (Option A, giữ token scale). Khuyến nghị next: Playwright `toHaveScreenshot()` baseline.
- **UI Design Fidelity Review — 8 implemented screens** (2026-05-26, Docs): Compare `design-file/MyBlog *.html` prototypes vs FE actual `http://localhost:5173` via Playwright Chromium @ 1440×900. 16 screenshots saved `/tmp/ui-review-all/<slug>/{design,fe}.png`. Result: ✅ Search match · ⚠️ 6 minor drift (Login/Feed/Post Detail/Profile/Tags/Create Post — covered by existing M11.9 tasks T-370/371/373/374/368/347) · ❌ Admin broken (NEW BUG-006 Critical P0 → T-380 fix task TODO). Read-only audit — no code changes.

### Added

- **T-397 FE EmojiPicker wire vào RichTextEditor toolbar** (2026-05-29, FE): EmojiPicker component (T-366, 4-group 64 emoji) đã tồn tại nhưng chưa integrate. Wire vào `RichTextEditor.tsx`: import EmojiPicker + `showEmoji` state + 🙂 ToolbarBtn (đặt sau ✕ clear — saveSelection + toggle + đóng color/highlight popover) + `insertEmoji` handler (restoreSelection + `insertHTML(emoji)` qua execCommand, consistent với link/format insert — contentEditable dùng insertHTML thay vì textarea insert-at-cursor util) + render `<EmojiPicker>` dưới color/highlight popover. Tests: 5 case mới (🙂 button present + open + close-button + select→insertHTML(emoji)+đóng + mutual-exclusion với text-color popover). 14/14 RichTextEditor tests pass; 437/440 FE (3 pre-existing ManagePostsPage). Closes Create Post audit gap "EmojiPicker chưa wire" (tách từ T-394 W5 vì là feature work). (Refs T-397)

- **T-381 BE Search add `saved` type filter** (2026-05-27, BE): Extend `SearchType` enum DTO add `'saved'` value. `SearchService.search()` thêm branch `type=saved`: require authed user (anon → 401 UNAUTHORIZED), `prisma.post.findMany({ where: { savedBy: { some: { userId: viewerUserId } } }, ... })` với mood filter optional + q content ILIKE optional + pagination. Trả `files: []`, `tags: []` cho saved branch. OpenAPI yaml + api.generated.ts regenerated. FE `SearchType` hand-typed cũng synced add `'saved'`. Tests: 3 e2e case mới (200 type=saved authed returns saved posts + 401 anon + 200 type=saved + mood narrow). 12/12 search e2e pass. Unblocks T-351 FE SearchPage Saved chip. (FR-12.9, Refs T-381)

- **T-352 FE NotificationsPage rewrite** (2026-05-26, FE): NEW `apps/web/src/pages/notifications/NotificationsPage.tsx` + `NotifRowPage.tsx`. Route `/notifications` (ProtectedRoute authed). SubBar `~/notifications · N total · M unread` + `✓ mark all read` + `✕ clear all` (ConfirmDialog). 6 type tabs (All/Unread/Reactions/Comments/Replies/Shares) + count badges. Search input `⌕` 150ms debounce client-side filter. `☐ select all visible` when no selection. Bulk action bar (`N selected` + mark read + delete + clear) on any selection. Group-time sticky labels (today/yesterday/older). 2 empty states (inbox zero + filtered). Reply notification renders `from @<replyTo>` clause. Toast feedback all actions. `useInfiniteNotifications` hook (useInfiniteQuery page-based). `useBulkDeleteNotifications` hook added. `NotificationMetadata.replyTo` type extended. 11/11 tests pass. (FR-14.7-.13, Refs T-352)

- **T-345 BE Notifications bulk endpoints** (2026-05-26, BE): `PATCH /notifications/bulk-read { ids: string[] }` — mark up to 100 notifications as read (self-scope, ids belonging to other users silently skipped), returns `{ updated: number }`. `DELETE /notifications/all` — clear all notifications of current user, returns `{ deleted: number }`. New `BulkMarkReadDto` (class-validator ArrayMaxSize 100). FE: `bulkMarkNotificationsRead` + `deleteAllNotifications` service functions + `useBulkMarkRead` + `useDeleteAllNotifications` mutation hooks. OpenAPI regenerated + FE types synced. 4 new e2e cases (bulk-read happy + skip other-user ids, bulk-read 400 >100 ids, delete-all happy + 401 anon). 209/209 BE e2e pass. (FR-14.10/.12, Refs T-345)

- **T-372 FE ManagePostsPage + QuickEditModal greenfield** (2026-05-26, FE): NEW `apps/web/src/pages/ManagePostsPage.tsx` — route `/admin/posts` (ProtectedRoute requireRole=ADMIN). SubBar `~/admin/posts` + total count + `+ New Post →` link. Toolbar: search (300ms debounce, URL `?q=`), status 4-chip filter (ALL/PUBLISHED/DRAFT/ARCHIVED, `?status=`), mood 7-emoji filter (`?mood=`), sort select (`?sort=latest|oldest`), view toggle list/card (`?view=`). List view: 7-col PostRow grid (checkbox, snippet 80-char, StatusBadge, mood emoji, tags +N, counts ❤💬👁, edit/delete actions) with select-all header. Card view: 2-col PostCardMng grid (StatusBadge + mood + snippet 160-char + tags 4 + counts + edit/delete). Bulk select bar (`data-testid="bulk-bar"`) appears on any selection, shows `N selected`. QuickEditModal 560px: STATUS select + MOOD 7-emoji picker + CONTENT textarea + TAGS chip input (Enter/comma add, Backspace remove, `aria-label="Add tag"`). ConfirmDialog (destructive) with 80-char content snippet. Toast feedback on save/delete. NEW: `admin-posts.ts` service (listAdminPosts, updateAdminPost, deleteAdminPost) + `use-admin-posts.ts` hooks (useAdminPosts, useUpdateAdminPost, useDeleteAdminPost) + StatusBadge component. Tests: 15 cases. 15/15 pass.

- **T-377 FE ImageCarousel Post Detail refresh** (2026-05-26, FE): `ImageCarousel.tsx` visual refresh per design-file spec. Nav arrows redesigned to 44px round frosted-glass buttons (`rgba(10,14,26,.75)` bg + `backdrop-filter blur(4px)` + `border-b2`). Pagination dots: active dot `18×6px rounded-full bg-cyan` + `box-shadow 0 0 6px var(--cyan)` glow, inactive `6×6px bg-b2`. Counter `N/total` moved to `absolute bottom-right` as standalone mono-11 text-td (separated from dot row). 9 tests (5 T-377 target cases + 4 keep). 9/9 pass.

- **T-376 FE EditProfileDrawer 4-section redesign** (2026-05-26, FE): `EditProfileDrawer.tsx` refactored from 2 sections → 4: `// basic.info` (2-col Full name + Handle readonly + Title + Bio textarea), `// contact.links` (2-col Location + Born year + GitHub + Website), `// skills.stack` (SkillChipInput now cycles NEON_COLORS 8-palette), `// security` (Current + New + Confirm password, kept). Sticky header (// edit.profile + × close) + sticky footer (`✓ Save Changes` cyan primary + `Cancel`). `animate-slide-in 250ms` drawer entrance. Single unified profile form with `form id="profile-form"` + footer submit button. `AdminUser` + `UpdateUserPayload` types extended with optional `name`, `location`, `bornYear`, `github`, `website`. Tests: 9 cases (8 target + 1 baseline open=false). 34/34 profile suite pass.

- **T-374 FE ProfilePage hero rewrite** (2026-05-26, FE): `ProfilePage.tsx` rewritten — hero banner with `linear-gradient(180deg, #0F1525, #0A0E1A)` bg + hex deco corner (binary/uid/pid muted) + `animate-glitch` name H1 26 + `@username` handle cyan + role badge + action buttons (self: `✏️ New Post` → /admin/create + `⚙️ Settings` opens drawer). TabButtons extended with optional `count` badge; Posts tab shows `postsCount`. Posts/Saved tabs swap PostCard → PostMiniCard. Activity tab adds HeatmapGrid above ProfileActivityList. About tab gains `// info` grid 8 k-v (joined/posts/likes/comments/views/streak/top-mood/top-tag). InfoGrid helper component. Tests: 10 cases. 384/384 FE pass.

- **T-375 FE PostMiniCard component** (2026-05-26, FE): NEW `apps/web/src/components/profile/PostMiniCard.tsx` — compact post card for Profile Posts/Saved tabs. `#<id>` corner mono 10 muted + header (relative timestamp + MoodBadge) + content 3-line clamp + image mini thumbs 40×30 max 3 with +N overlay + tags row + action row border-top (♡/❤ LIKE reaction toggle via `useUpsertReaction`, 💬 comment count, `read →` cyan link to `/post/:id`). Tests: 4 cases. 379/379 FE pass.

- **T-373 FE TagModal NEON_COLORS refactor + TagCard polish** (2026-05-26, FE): `TagModal.tsx` rewritten to 440px: close × button + live preview row at top showing `#<name>` mono 17 with text-shadow glow + name input with `#` prefix styled in selected color + 8 NEON_COLORS swatches 28×28 (active: white border + scale 1.15 + per-color glow) + native `<input type="color">` + hex display + description textarea rows 2 + error block red mono 12 + body scroll lock. `NEON_COLORS` export added to `tag-colors.ts` (8-accent palette: cyan/mag/pur/grn/yel/ora/blu/red). `TagCard.tsx` grid variant: top accent line absolute h-[2px] opacity-0 → group-hover:opacity-100 + progress bar `h-1` → `h-[2px]` + `animate-fade-up` stagger (60ms × index). Tests: 9 cases TagModal (7 new-feature + 2 keep) — 375/375 FE pass.

- **T-355 FE ImageLightbox component (M11.9 design polish)** (2026-05-25, FE): NEW `apps/web/src/components/feed/ImageLightbox.tsx` — full-screen image viewer portal via `createPortal(document.body)`. Header (path + counter `N/M` + close ×) + image area (max-h 70vh, max-w 960px, object-contain) + thumbnail strip (chỉ render khi `images.length > 1`) + footer hint. Nav arrows 44px round backdrop-blur (chỉ multi-image). Keyboard: Esc close, ← prev (wrap), → next (wrap). Body scroll lock + animate-fade-up-xs (T-361 token). `ImageGrid` refactor: thêm `onImageClick?: (idx: number) => void` prop + button wrapper around ImageCell khi callback provided. PostCard wire: `setLightboxIdx` state + conditional `<ImageLightbox>` render. Tests: 5 cases (open render+counter+close button + keyboard nav wrap-around + Esc close + thumbnail strip click jump + single image skip strip/arrows). 340/340 FE pass.
- **T-354 FE PostCard action row refactor — remove SaveButton, integrate PostActionMenu** (2026-05-25, FE): Per design-file 2026-05-24 spec. `PostCard.tsx`: remove `<SaveButton>` standalone import + render (moved to PostActionMenu item `🔖 Save post`). Action row giờ chỉ 3 buttons: `[React]` + `💬 N` + `↗ Share`. `⋯` More actions button (ml-auto) toggles PostActionMenu. Test updates (test-stale-assumption): "click save → toggle saved state" replaced với 2 new regression tests — SaveButton REMOVED + ⋯ toggles PostActionMenu. 335/335 FE pass.
- **T-356 FE PostActionMenu component (M11.9 design polish)** (2026-05-25, FE): NEW `apps/web/src/components/feed/PostActionMenu.tsx` — context menu khi click `⋯` button trên PostCard. 250px minWidth, animate-fade-up-xs (T-361). Header `// post.actions · #<id>` cyan/td. Items: user-actions (↗ Open detail / 🔗 Copy link / 🔖 Save post toggle) + admin-only sections (✎ Edit / ✕ Delete) gated qua `user.role === ADMIN || user.id === post.author.id`. Click outside close (mousedown listener). Copy link uses `navigator.clipboard.writeText` + 900ms `Copied!` feedback. Reuse `useTogglePostSave` mutation. Tests: 6 cases (open render + non-admin gating + click outside + Open detail nav + clipboard write + Save toggle). 334/334 FE pass.
- **T-362 FE Toast shared component + useToast hook + ToastProvider** (2026-05-25, FE): `apps/web/src/components/shared/Toast.tsx` + `apps/web/src/hooks/use-toast.ts` (NEW). React Context Provider wrapping AppLayout. `useToast()` hook returns `{ showToast(msg, type), toasts, dismiss }`. 3 variants per DESIGN_SYSTEM L911-923: success (✓ grn), error (✕ red), info (ℹ cyan). Position `fixed bottom-11 right-5 z-[200]`. Auto-dismiss 2500ms via setTimeout. Slide-down 200ms animation. Stack rendering cho multiple concurrent toasts. Tests: 4 cases (3 variants + auto-dismiss fake timer). 328/328 FE tests pass.
- **T-361 FE Animation registry consolidation (M11.9 foundation)** (2026-05-25, FE): `tailwind.config.ts` add `cursorBlink 1s steps(2)` keyframe (cho Search caret + terminal cursor) + split `fade-up 300ms` thành 5 variants per DESIGN_SYSTEM Motion table: `fade-up-xs 120ms` (picker/dropdown small), `fade-up-sm 150ms` (default modal), `fade-up 200ms` (DeleteConfirm — bump from 300ms generic), `fade-up-md 250ms` (drawer), `fade-up-lg 350ms` (Login card). Verified existing 7 keyframes from T-341/T-342: glitch 8s, pulse-status (drop-shadow), blink, shake, scan-card, borderRotate, liveDot, slideIn, slideDown. 324/324 FE tests pass — non-breaking additive change.

- **T-350 FE ReplyForm + ReplyRow + CommentItem refactor (FR-03.6 reply MVP complete)** (2026-05-25, FE): 2 NEW components + 1 refactor + 1 hook + types/service updates:
  - `apps/web/src/components/feed/ReplyForm.tsx` (NEW): inline form opens khi click ↩ Reply. Header `↩ replying to @parentuser` blu+cyan. Textarea Inter 13 rows 2 auto-focus. Anon toggle + name input. ⌘↵/Ctrl↵ submit. Esc cancel. Reuse `useCreateComment` mutation với `parentId` param.
  - `apps/web/src/components/feed/ReplyRow.tsx` (NEW): nested under parent indent `ml-10` (40px), avatar size `xs` (24×24), like với `♡/❤` binary (NOT reaction picker). Show `↩ @parentname` từ `reply.replyTo` denorm.
  - `CommentItem.tsx` refactor: thêm `showReplyForm` toggle state, render inline `<ReplyForm>` khi expanded, render `<ReplyRow>` list từ `comment.replies` preview (max 3 từ BE), `↳ N replies (load more)` button khi `replyCount > preview.length` (lazy load via `useReplies` hook).
  - `apps/web/src/hooks/queries/use-replies.ts` (NEW): `useReplies(commentId, params, enabled)` wrapping `GET /comments/:id/replies`.
  - `services/api/comments.ts`: thêm `listCommentReplies(commentId, params)`.
  - `hooks/mutations/use-create-comment.ts`: onSettled invalidate replies cache nếu mutation có `parentId`.
  - `types/api.ts`: Comment thêm `parentId?`, `replyTo?`, `replies?`, `replyCount?`. CreateCommentDto thêm `parentId?`. New `CommentRepliesResponse` type.
  - `Avatar.tsx`: thêm `xs` size variant (24×24, font 10px) cho ReplyRow.
  - Tests: 4 ReplyForm cases (render+focus, Cmd+Enter submit, Esc cancel, Cancel button) + 2 ReplyRow cases (render+replyTo display, anon variant). Plus CommentItem test update: stale "Reply button disabled (defer)" → "regression FR-03.6: Reply button toggles ReplyForm".
  - **324/324 FE tests pass.** Option A reply-to-comment chain COMPLETE (T-343 → T-344 → T-348 → T-349 → T-350, 5 commits).

- **T-349 FE wire PostCard 💬 → CommentsModal (FR-04.7)** (2026-05-25, FE): Refactor `PostCard.tsx` — `💬` button từ `<Link to="/post/:id">` → `<button onClick={() => setShowComments(true)}>` opens CommentsModal portal. Post Detail page `/post/:id` giữ nguyên cho deep-link/SEO (direct URL access OK). Test update: stale `expect(link).toHaveAttribute('href', '/post/abc123')` → assert button tagName + click opens modal-testid (test-stale-assumption per CLAUDE.md).
- **T-348 FE CommentsModal component (FR-04.7 DEFINITIVE)** (2026-05-25, FE): NEW `apps/web/src/components/feed/CommentsModal.tsx` — 640px modal portal vào `document.body` via `createPortal`. Header (// comments + post excerpt truncated + close ×) + Body (CommentItem list reuse + AsciiSpinner loading + empty state) + Footer (integrated CommentForm reuse). Esc keyboard close + click backdrop close + body scroll lock. Tests: 5 cases (open render + close button + click backdrop + Esc + list+empty+form footer). 318/318 FE tests pass. **MVP note:** Infinite scroll + page indicator dots defer (current BE GET /posts/:id/comments returns full list; client-side pagination/IntersectionObserver có thể add khi list grows >50 items).
- **T-344 BE GET /comments/:id/replies + top-level filter (FR-03.6)** (2026-05-25, BE): NEW endpoint `GET /comments/:id/replies` paginated (default `page=1&limit=20`, max 50), public role-aware (USER chỉ APPROVED, ADMIN tất cả). 404 nếu parent không tồn tại. Plus update `GET /posts/:id/comments`: top-level filter `parentId IS NULL`, include first 3 replies + `replyCount` per top-level comment. New DTO: `ListRepliesDto`, `CommentRepliesResponseDto`. Service `listReplies()` method + refactored `list()` to use `COMMENT_TOPLEVEL_INCLUDE`. OpenAPI synced (`docs/contracts/openapi.yaml` + `apps/web/src/types/api.generated.ts`). Tests: 5 e2e (list happy + pagination + 404 + role-aware filter + top-level+replies preview). 27/27 e2e pass.
- **T-343 BE Migration Comment.parentId + replyTo (FR-03.6 reply-to-comment MVP)** (2026-05-25, BE): Prisma schema thêm `Comment.parentId String?` self-relation (CASCADE delete) + `replyTo Json?` denormalized `{ username, isAnon }` + `@@index([parentId])`. Migration `20260525033356_add_comment_parent_id_for_reply` (auto-gen có stale Reaction constraint rename, manually cleaned). CommentsService.create accept `parentId`, validate depth-1 only (reject `INVALID_PARENT_DEPTH` 400), cross-post (reject `INVALID_PARENT_POST` 400), denormalize parent author into `replyTo`. CommentResponseDto thêm `parentId` + `replyTo` fields. Tests: 4 unit (happy + depth-2 reject + cross-post reject + anon parent) + 3 e2e (201 reply + 400 nested + DELETE cascade). 20/20 unit + 22/22 e2e pass.

### Changed

- **T-399 FE Feed FilterBar font-size design-file sync (F5)** (2026-05-30, FE): Tiếp nối T-398 chính sách design-file 1:1. `FilterBar.tsx` mood chips (`All` + 5 mood) + sort button `Latest ▾` + sort dropdown items (`Latest`/`Oldest`/`Most liked`): `text-mono-sm` (11) → `text-mono` (12) để khớp design-file class `.flt-btn` 12px (Feed.html L105-114). Header line `// feed.posts · N total` giữ 11px (đã khớp design L113). 4 className edit, 8/8 FilterBar tests pass (không assert font class). (Refs T-399, design-file Feed.html .flt-btn)

- **T-398 FE Feed font-size design-file sync (F5)** (2026-05-29, FE): User chốt bám `design-file/MyBlog Feed.html` 1:1 cho PostCard meta line + action bar — **supersede quyết định "Option A token-scale / accepted-noise" của T-392** (Post Detail design-file đã verify dùng cùng giá trị nên sửa shared component nhất quán cả 2 màn). `PostHeader.tsx`: author `~/username` `text-mono` (12) → `text-mono-lg` (14); `[ ADMIN ]` `text-mono-sm` (11) → `text-[10px]` (10, đảo quyết định T-378 chốt 11px); separator + timestamp + relative `text-mono-sm` (11) → `text-mono` (12). `MoodBadge.tsx` + `TagPill.tsx` `text-mono-sm` (11) → `text-mono` (12). `PostCard.tsx` divider deco `text-[10px]` (10) → `text-mono-sm` (11) + 3 action buttons `text-mono` (12) → `text-mono-md` (13). `ReactionButton.tsx` label `text-mono` (12) → `text-mono-md` (13). `PostContent.tsx` code block `text-mono` (12) → `text-mono-md` (13). Post body 15px giữ nguyên. Test cũ làm contract — component đổi (PostCard/ReactionButton/PostContent/MoodBadge/TagPill) 29/29 pass, không assert font class nên không cần update. DESIGN_SYSTEM Typography note cập nhật role-badge mapping. (Refs T-398, design-file Feed.html)

- **T-395 FE design fidelity Wave 6 — minor cleanup (audit scattered minor)** (2026-05-29, FE): 6 opacity/blur/size deltas còn lại. `PostCard.tsx` hover glow `shadow-glow-cyan-lg` (45px/.12) → arbitrary `0 0 24px rgba(0,255,229,.1), 0 4px 24px rgba(0,0,0,.3)` (token shared 3 nơi nên không đổi token, dùng arbitrary localized); `SearchPage.tsx` mood button active tint `${color}1A` → `${color}12` + glow `${color}66` → `${color}30` (giảm 2× độ sáng về đúng design); `NotificationBell.tsx` dropdown width 360→380px; `CommandPalette.tsx` 560→540px; `NotifRowPage.tsx` checkbox `rounded-sm` → `rounded-[3px]` + `border-[1.5px]` + unread tint `${color}0f` → `${color}06`; `PostContent.tsx` code block border-left opacity `.5` → `.4`. 432/435 FE pass. (Refs T-395, audit minor)

- **T-394 FE design fidelity Wave 5 — per-screen specific (audit THEME-5 + per-screen)** (2026-05-29, FE): 6/7 nhóm (EmojiPicker wire defer T-397 — feature work). (1) `NotifRowPage` avatar solid `--elev` → `linear-gradient(135deg, rgba(0,255,229,.2), rgba(187,154,247,.2))` + text cyan (anon giữ solid); (2) `TagCard` top accent solid → `linear-gradient(90deg, transparent, ${color}60, transparent)` + per-color hover glow via onMouseEnter/Leave (`0 0 18px ${color}10` + border `${color}60`) + progress bar glow `0 0 4px ${color}80`; (3) `HeatmapGrid` thêm legend row (less + 4-color scale + more); (3b) `PostMiniCard` thêm `::before` cyan top-edge glow (hover reveal) + `hover:shadow-[0_0_18px_rgba(0,255,229,0.08)]`; (4) `ProfilePage` hero `~/{username}` → `{name || username}` (full name, user decision — field `name` đã có sẵn FE-only); (6) `TerminalCard` cursor blink `1.06s` → `530ms` + shake `.45s` → `.4s`; (7) `NotificationBell` mark-all-read `text-grn` → `text-cyan`; + `UploadZone` `animate-dashed-pulse` keyframe (border #2A3548↔#3D4A63 2s) + CreatePost publish button cyan glow `0 0 14px rgba(0,255,229,.3)` hover `0 0 22px .4`. ProfilePage test updated (stale-assumption: hero full name). 432/435 FE pass. (Refs T-394, audit THEME-5)

- **T-393 FE design fidelity Wave 4 — fadeUp timing + stagger (audit THEME-4)** (2026-05-29, FE): Verify design duration thật trong design-file (Feed card `.3s`, TagCard `.25s` + stagger `i*20`, Search ResultCard `.2s` + cascade — FE thiếu hẳn). Fix: `ResultCard.tsx` thêm `animate-fade-up` + `index?` prop → `animationDelay: index*50ms` (wire 2 map site trong SearchPage — empty-state all.posts + results); `TagCard.tsx` `animate-fade-up` → `animate-fade-up-md` (200→250ms) + stagger `*60` → `*20`ms; `PostCard.tsx` (Feed) `animate-fade-up` → `animate-fade-up-md` (design 300ms không có token, dùng 250ms gần nhất). Notifications + ManagePosts rows: FE không có entry animation (audit over-stated) — defer thêm vì risk jank trên infinite-scroll list. 432/435 FE pass. (Refs T-393, audit THEME-4)

- **T-392 FE design fidelity Wave 3 — mono font size normalize (audit THEME-2)** (2026-05-29, FE): Option A token-based normalize (user chọn giữ token scale T-360 thay vì pixel-exact raw px). Verify design-file cho thấy phần lớn THEME-2 là 1px token-noise + design-file tự inconsistent (cùng `~/username` xuất hiện 11/12/14px ở các screen khác nhau). Fix 2 high-confidence drift: `PostHeader.tsx` username `text-mono-sm` (11px) → `text-mono` (12px) — shared Feed PostCard + Post Detail; `LoginPage.tsx` 2 input `text-mono-lg` (14px) → `text-[15px]` (mono-lg là shared token 7 nơi nên dùng arbitrary localized). Accepted-noise giữ nguyên để bảo toàn token discipline: timestamp 10px (no token), [ADMIN] badge 10px (T-378 chốt 11px), comment body 13px mono vs design 15px Inter (terminal-aesthetic choice, không phải pure size). 432/435 FE pass. (Refs T-392, audit THEME-2)

- **T-391 FE design fidelity Wave 2 — cyan glow shadows + modal recipe (audit THEME-3)** (2026-05-28, FE): Thêm 3 shadow token vào `tailwind.config.ts`: `glow-cyan-modal` (`0 0 50px rgba(0,255,229,.1) + 0 24px 60px rgba(0,0,0,.7)`), `glow-cyan-panel` (`0 0 30px .08 + 0 16px 50px black .6`), `glow-cyan-input` (`0 0 14px rgba(0,255,229,.22)`). Apply: (1) **Card hover glow** — Admin StatCard (`border-b2` + `hover:border-cyan/30` + `hover:shadow-[0_0_20px_rgba(0,255,229,0.06)]`), Manage Posts PostCardMng (`hover:border-cyan/40` + `hover:shadow-[0_0_18px_rgba(0,255,229,0.08)]`, was `hover:border-b3`); (2) **Focus glow** — Notifications search (was no glow) + Tags search → `focus:shadow-glow-cyan-input`; (3) **Modal shadow** — TagModal + QuickEditModal `shadow-xl` → `shadow-glow-cyan-modal` + `border-cyan/25`, NotificationBell dropdown → `shadow-glow-cyan-panel` + `border-cyan/25`. CommandPalette đã có inline cyan glow (skip). TagCard dynamic per-color glow defer T-394 W5. DESIGN_SYSTEM shadow recipe table updated. 57/57 affected tests pass. (Refs T-391, audit THEME-3)

- **T-390 FE design fidelity Wave 1 — cyan-vs-blue active states (audit THEME-1)** (2026-05-28, FE): Sửa active/accent/selected state dùng sai blue `#7DCFFF` (`rgba(125,207,255,…)`) → cyan `#00FFE5` (`rgba(0,255,229,…)`) theo design-file. 5 site: `ResultCard.tsx` hover accent gradient; `NotificationsPage.tsx` active tab background (.1→.08) + border (.3→.5) + count badge; `NotifRowBell.tsx` avatar gradient stop 1; `NotifRowPage.tsx` selected row bgTint; `NotificationBell.tsx` open-state glow shadow. `text-blu` ở links (Login/Register register-link, TagPickerDropdown) giữ nguyên — intentional design blue. Verified `grep 125,207,255 = NONE remaining`. Source: [docs/audits/DESIGN_FIDELITY_2026-05-28.md](audits/DESIGN_FIDELITY_2026-05-28.md) THEME-1. 40/40 affected tests pass. (Refs T-390)

- **T-331 close — PostDetail ImageCarousel → Lightbox wiring** (2026-05-27, FE): T-355 (2026-05-25) đã cover PostCard side + ImageLightbox component + ImageGrid `onImageClick` prop. Gap còn lại: PostDetailPage dùng ImageCarousel (không ImageGrid) → chưa wire lightbox. Fix: `ImageCarousel` thêm optional `onImageClick?: (idx: number) => void` prop — khi cung cấp, image area wrap trong button `cursor-zoom-in` data-testid `image-carousel-open-lightbox`. PostDetailPage thêm `useState lightboxIdx` + conditional render `<ImageLightbox images startIdx postPath onClose />`. Tests: 2 new ImageCarousel case (onImageClick fires với current idx khi click + advance idx → click again; not-render button khi prop absent). 14/14 ImageCarousel+PostDetail tests pass. T-331 status → DONE. (Foundation, Refs T-331)
- **T-351 FE SearchPage rewrite (design-file 2026-05-24 sync, FR-12.8-.12)** (2026-05-27, FE): Full rewrite SearchPage per design v2. `BigSearchInput`: Inter 18px (was mono 16) + `❯ search` label mono 11 cyan + `⌘K` badge (right side khi empty) + × clear button (khi có value). Filter row: 3 type chips `All / Saved / Files` (was 4 chips All/Posts/Files/Tags) + vertical divider + **7 mood emoji buttons** (30×28 square, active = mood color border + tint + glow, was 5 chips) + `× reset` red link khi `hasFilterActive`. 3 empty-state sections khi `q='' && type='all' && !mood`: (1) `// recent.searches` 5 items + clear button; (2) `// browse.tags` (useTags hook, per-color chips); (3) `// all.posts {N} total` (useQuery → listPosts limit 10, ResultCard preview). No-results state khi filtering + zero results: ◎ 32px + `// no results for "<q>"` mono 14 + bash hint `$ grep -r "<q>" ./posts --no-results` mono 12 + `← clear search` cyan button + top 3 `try "<recent>"` quick-try buttons. `ResultCard` refresh: top accent line `linear-gradient(90deg, transparent, cyan/30, transparent)` opacity 0→1 on hover (200ms) + post-id corner deco top-right `#<id-6char>` mono 10 muted + highlight `<mark>` switched từ `bg-cyan/30` → `bg-cyan/20` per spec. SearchPage consumes new `type=saved` từ T-381 BE. Tests: SearchPage.test.tsx rewrite 8 cases (hero render + Saved chip click + mood HAPPY toggle + 3 empty sections render + results highlight + no-results state + reset filters + recent.searches localStorage). 8/8 pass. 430/433 full FE (3 pre-existing ManagePostsPage). (FR-12.8-.12, Refs T-351, depends T-381)
- **T-359 FE NotificationBell visual refactor (design-file 2026-05-24 sync)** (2026-05-27, FE): Replace `🔔` emoji 18px → inline SVG bell 15×15 viewBox 24 stroke=currentColor 2px (2 paths: body `M6 8a6 6 0 0 1 12 0c0 7 3 9 3 9H3s3-2 3-9` + clapper `M10.3 21a1.94 1.94 0 0 0 3.4 0`). Button 32×32 thêm `border 1px --b2` + `bg --elev` (was no-border, no-bg) + hover/open state `bg cyan/8` + `shadow 0 0 12px cyan/20`. Badge: ring `1.5px solid --surf` via inline `boxShadow` (Tailwind ring không interpolate var-tokens), color `--bg` (was `text-white`), threshold `> 9 → "9+"` (was `> 99 → "99+"` để tránh badge overflow vào icon — chốt theo design-file). DESIGN_SYSTEM "Code drift" flag for NotificationBell flipped to RESOLVED. Tests: 4 new T-359 cases (SVG render + threshold 9+/9+ + bordered button + badge ring) + 1 stale-assumption update (test 3 threshold). 9/9 NotificationBell tests pass; 426/429 full FE (3 pre-existing ManagePostsPage failures unrelated). (Refs T-359, FR-14)
- **T-353 FE NotifRow split — NotifRowBell + NotifRowPage** (2026-05-27, FE): Refactor inline `NotificationRow` của `NotificationBell.tsx` thành component riêng `NotifRowBell` (`apps/web/src/components/layout/NotifRowBell.tsx`) match design spec — 34×34 avatar + 18×18 type badge + 2px border-left + cfg.color tint + 3-line content. Shared `NOTIF_TYPE_CFG` (REACTION/COMMENT/REPLY/SHARE, đồng bộ NotificationType enum — KHÔNG dùng legacy `like/save` từ design-file cũ) + `notificationVerb` + `reactionEmoji` + `notifTargetPath` extracted ra `apps/web/src/lib/notification-format.ts`. NotifRowPage refactor để reuse `NOTIF_TYPE_CFG` (xóa duplicate TYPE_CFG). Doc sync: FR-14.13 + DESIGN_SYSTEM.md NotifRowBell section update để chỉ rõ 4 real types (clarify mismatch giữa design-file legacy spec và actual data model). Tests: 2 new test files (`tests/components/layout/NotifRowBell.test.tsx` + `tests/pages/notifications/NotifRowPage.test.tsx`) — 12 case (6 mỗi: variant size, badge size, border-left thickness, type config 4 types, anon variant, hover/action state). Update existing NotificationBell.test.tsx tab-switch assertion (test-stale-assumption: row text giờ split spans, emoji moved từ verb text vào badge). 18/18 notif-related tests pass.
- **T-358 polish — ReactionPicker per-color hover state + merged React button + inline count** (2026-05-26, FE): Second iteration after user feedback showing actual FE didn't match design. ReactionPicker hover state moved từ static Tailwind hover classes → inline `onMouseEnter`/`onMouseLeave` handlers per design-file L750 spec: `bg ${color}22` + `border ${color}A0` + `box-shadow 0 0 10px ${color}50, inset 0 0 8px ${color}30` + `translateY(-2px)`. Active state: `bg ${color}18` + `border ${color}80`. Per-color requires inline interpolation — Tailwind cannot tree-shake 6 dynamic hex values. ReactionButton MERGE: remove separate count badge button; inline `· {count}` span inside React button per design Feed.html L901. Count span has nested `onClick={stopPropagation}` to open ReactionList modal without triggering parent toggle. Active reacted state adds `textShadow: 0 0 8px <color>60` for tinted glow. Test updates: PostCard count assertion `'5'` → `'· 5'`; ReactionPicker tests switch from className hover checks → fireEvent.mouseEnter + style assertions (rgba match for hex+alpha JSDOM normalization). 361/361 FE pass.
- **T-358 ReactionPicker container refactor — pill → panel** (2026-05-26, FE): `apps/web/src/components/feed/ReactionPicker.tsx` shape overhaul per design-file Feed.html L717-758. Container: `rounded-full` pill + `border-b2` + `shadow-glow-cyan-lg` → `rounded-lg` panel (8px) + `border-cyan/35` + `shadow-glow-cyan-md` + `z-popover` + `animate-fade-up-xs` (T-360+T-361 tokens). Buttons: `h-9 w-9` (36×36) → `h-10 w-10` (40×40). Hover transform: `scale-125` → `-translate-y-0.5` (translateY −2px). Per-color hover glow via CSS var (`--rx-color` inline from `r.color`) + group-hover Tailwind arbitrary `[filter:drop-shadow(0_0_6px_var(--rx-color))]` (Tailwind can't interpolate dynamic color across 6 variants without var). Active state ReactionIcon `glow` filter retained. 250ms hover-close grace intact (BUG-001 fix T-340). Tests: 4 new ReactionPicker cases. 361/361 FE pass.
- **T-357 ReactionIcon SVG component + REACTION_CONFIG migration** (2026-05-26, FE): NEW `apps/web/src/components/feed/ReactionIcon.tsx` (~110 lines) renders 6 line-art SVG variants per design-file Feed.html L718-723 — LIKE thumbs-up, LOVE heart, HAHA square face + LOL text, WOW round face oval mouth, SAD round face down-curve + tear, ANGRY round face slash brows. Props `{ r: ReactionConfig | ReactionType, size?: 18, glow?: false }`; `glow=true` applies `drop-shadow(0 0 4px <color>)` tinted với reaction color. REACTION_CONFIG migration: drop obsolete `emoji: string` field; switch `color` from CSS var tokens (`var(--cyan)`) → hex literals because SVG stroke/fill cannot resolve `var()` reliably (jsdom + Safari flake). Color palette realigned per design-file: LIKE blu `#7DCFFF`, LOVE mag `#FF6E96`, HAHA yel `#E0AF68`, WOW pur `#BB9AF7`, SAD blu `#7DCFFF`, ANGRY red `#F7768E`. Refactor 3 consumers: ReactionButton (default trigger icon + active state + topReactions stack), ReactionPicker (panel buttons), ReactionList (tab labels + row icons) — all swap emoji renders → `<ReactionIcon>`. Tests: 3 new cases. 357/357 FE pass. Unblocks T-358 ReactionPicker panel refactor.
- **T-371 FE AdminPage rewrite** (2026-05-26, FE): AdminPage.tsx rewrite per design-file spec. SubBar now `fixed top-[52px] z-subbar` with `animate-live-dot` green dot + ✏️ New Post + 🏷 Tags links. Content area adds `pt-10` spacer for fixed SubBar. Mood distribution + activity log each wrapped in `rounded-lg border bg-surf` cards; activity log section gets `max-h-80 overflow-y-auto`. Users table wrapped in card with sb-lbl header. Comments moderation wrapped in card with pending count badge (from `useAdminComments` total). `UsersTable.tsx` updated: columns now Username / Role `[ X ]` / Last seen / Posts (`—`) / Actions (Ban + View); View button added for all users. Tests: 8/8 AdminPage pass + 4/4 UsersTable pass (role text updated `test-stale-assumption`: `ADMIN` → `[ ADMIN ]`). 372/372 FE pass.
- **T-320 BE PostStatus enum + admin post endpoints** (2026-05-26, BE): Prisma migration `20260526000000_add_post_status_enum` — new `PostStatus` enum (PUBLISHED/DRAFT/ARCHIVED) + `Post.status PostStatus @default(PUBLISHED)` + `@@index([status])`. `PostsService.list()` now filters `status: PUBLISHED` by default (public feed hides drafts/archived). New `PostsService.adminList()` — full-status list with status/mood/q/sort/page/limit filters. `PostsService.update()` extended to accept `status` field. 3 new admin endpoints wired via `AdminController`: `GET /admin/posts`, `PATCH /admin/posts/:id`, `DELETE /admin/posts/:id` (204). `AdminModule` imports `PostsModule`. New DTOs: `ListAdminPostsDto` + `UpdateAdminPostDto`. `makePost` factory gains `status?` param. Tests: 24/24 unit (PostsService) + 204/204 e2e all pass. OpenAPI + api.generated.ts regenerated.
- **T-370 LoginCard polish refresh** (2026-05-26, FE): LoginPage anonymous link align to spec — add `font-mono` + `text-[13px]` (was 12) + `py-[9px]` (was 10). Add `data-testid` on anon + register links. 4 new tests for anon/register links, bracket logo SVG polylines, and bottom status. 369/369 FE pass.
- **T-369 LinkInsertModal + saveSelection/restoreSelection** (2026-05-26, FE): NEW `apps/web/src/components/create-post/LinkInsertModal.tsx` — 420px modal triggered by 🔗 toolbar button / ⌘K. Pre-fills text label from selected text; on apply calls `RichTextEditorHandle.applyLink(url, label)` which restores saved selection then `exec('createLink', url)` if selection exists or `insertHTML('<a href=url>display</a>')` if not. Esc + Cancel + ↵ Insert. RichTextEditor refactored to `forwardRef` exposing `RichTextEditorHandle` interface; `onRequestLink` updated to pass `selectedText: string`. autoFocus pattern (no setTimeout) prevents focus-steal during tests. CreatePostPage wires `showLinkModal` + `linkInitialText` state + `editorRef`. Tests: 4 new cases trong `LinkInsertModal.test.tsx`. 365/365 FE pass.
- **T-368 RichTextEditor contentEditable refactor — replace MarkdownEditor** (2026-05-26, FE): NEW `apps/web/src/components/create-post/RichTextEditor.tsx` (~280 lines) per design-file v2 spec (`Create Post.html` L322-441). contentEditable div + 5 Range API patterns (`exec(cmd, val)` / `insertAtCursor(text)` / `insertHTML(html)` / `saveSelection()` / `restoreSelection()`). 11-button toolbar `B I U S A 🖍 H1 H2 • 1. 🔗 ✕` với per-button visual styling. 2 inline color popovers (TEXT_COLORS 7 + HIGHLIGHT_COLORS 7 `/40` alpha) mutually exclusive — `saveSelection` trên trigger button mousedown + `restoreSelection` trên swatch click. Keyboard shortcuts ⌘B/I/U/K khi editor focused. 🔗 button accepts `onRequestLink?` callback (T-369 modal wires later); fallback `window.prompt`. **Content format migration:** PostContent thêm `isHtmlContent` heuristic (regex `^\s*<[a-z]`) → `dangerouslySetInnerHTML` cho HTML format (admin-only trust, sanitizer defer); legacy markdown posts vẫn render qua parsePostContent. CreatePostPage swap consumer. Delete `MarkdownEditor.tsx` + `MarkdownEditor.test.tsx` (markdown-wrap obsolete). Tests: 9 new cases trong `RichTextEditor.test.tsx` (stub `document.execCommand` cho JSDOM). 354/354 FE pass (349 + 9 new − 4 deleted).
- **T-367 TagPickerDropdown master-data picker — replace free-form TagInput** (2026-05-26, FE): NEW `apps/web/src/components/create-post/TagPickerDropdown.tsx` per design-file v2 master-data pattern. `+ add tag` dashed border button opens listbox below field (z-popover + animate-fade-up-xs + shadow-drop-md from T-360). Available system tags via `useTags({sort:'top', limit:30})` filtered against current selection. Each chip `+ <name> N` shows postCount + per-color (`style={{color:tag.color}}` inline since arbitrary hex). Footer link `// can't find your tag? manage tags →` blu Link → `/tags`. Delete obsolete `TagInput.tsx` + `TagInput.test.tsx` (free-form input removed per spec — admin creates tags at `/tags`, users only pick existing). TagDraft type moves from TagInput → TagPickerDropdown export; PostPreview + CreatePostPage update imports. Tests: 5 new cases. 349/349 FE pass.
- **T-366 EmojiPicker inline refactor — tabbed popup → 4-group stack** (2026-05-26, FE): Rewrite `apps/web/src/components/create-post/EmojiPicker.tsx` từ tabbed absolute popup (1 active group of 16 emojis visible) sang inline stack — all 4 groups (faces/hands/dev/nature) × 16 emojis = 64 cells render simultaneously, pushes editor textarea down per design v2. Each group has `// {label}` header (text-mono-tiny uppercase tracking-wide-2 muted). Header row has `// pick an emoji` label + × close button. Esc + × close (no outside-click — inline persistent). MarkdownEditor consumer moves `<EmojiPicker>` từ inside flex toolbar → below toolbar div (inline flow). Tests rewrite from 7 tab-switched cases → 6 inline cases per CLAUDE.md test-stale-assumption rule. 350/350 FE pass.
- **T-365 CommandPalette design v2 refactor (11 → 8 commands)** (2026-05-26, FE): Refactor `commands.ts` từ 11 entries xuống 8 grouped per design v2 spec — navigate (5: ~/feed ⌘1 / ~/saved ⌘2 / Create Post ⌘N / ~/admin ⌘3 / ~/tags ⌘4) + actions (2: Toggle theme ⌘T / Logout ⌘Q) + recent group renders placeholder `// no recent activity yet` italic muted khi không có tracking. `groupCommands(cmds, hasQuery)` signature thêm `hasQuery` param — recent group always shown when no query, hidden khi filtering. Drop Profile + Search entries (Profile via AvatarMenu, Search via TopBar input). CommandPalette migrate `z-[200]` → `z-dropdown` + `animate-fade-up` → `animate-fade-up-sm` (T-360+T-361 tokens). Header `// {group}` migrate `tracking-[0.05em]` → `tracking-wide-1`. ⌘K global handler đã ở AppLayout từ trước. Tests: 4 stale-assumption updates per CLAUDE.md test-stale-assumption rule (Profile/Search removed, Create Post moved navigate, recent placeholder). 351/351 FE pass.
- **T-364 AvatarMenu 7-item refactor — extract from TopBar inline** (2026-05-26, FE): NEW `apps/web/src/components/layout/AvatarMenu.tsx` (168 lines) per DESIGN_SYSTEM L848 spec. Design v2 menu items swap: drop `Create Post` + `Saved` from menu (still accessible via /admin/create + /saved routes — just no longer surfaced here); add `📝 Manage Posts` blu /admin/posts adminOnly + `🏷 Manage Tags` yel /tags + `🔧 System Settings` grn disabled `// TBD` placeholder (route deferred to future task). Keep `⚙️ Admin Dashboard` pur ⌘3 + `👤 Profile` ts /me (separator before) + `🚪 Logout` red ⌘Q. Header avatar 28 + `~/<user>` blu + `[ ADMIN ]` ora (admin only). Status dot uses `animate-pulse-status` (T-360 token), panel uses `z-dropdown` + `animate-fade-up-xs` (T-360 + T-361 tokens). TopBar shrunk 226 → 82 lines. Tests: 5 new cases `tests/components/layout/AvatarMenu.test.tsx` (7 items render admin + non-admin filter + click outside close + status dot pulse + per-item color classes). Plus 4 TopBar test stale-assumption updates per CLAUDE.md test-stale-assumption rule. 352/352 FE pass (347 + 5).
- **T-363 UploadZone extract to shared** (2026-05-26, FE): Move trio (`UploadZone.tsx` + `ImageThumb.tsx` + `FileItem.tsx`) from `apps/web/src/components/create-post/` → `components/shared/` via `git mv` (preserve history). UploadZone gains 2 new optional props: `hint?: string` (override default `❯ drag & drop or click — N slots left` text — useful for variant copy like "❯ paste your screenshot here"), `maxSizeMB?: number` (client-side `file.size <= limit` filter before upload — silent skip for oversized files). CreatePostPage import path updated. Test file mirrored to `tests/components/shared/UploadZone.test.tsx` + 3 new cases (hint render override / accept attr wired / maxSizeMB rejects oversized). 347/347 FE pass (344 + 3). Unblocks future shared upload usage (avatar uploader, attachment widgets) without copy-paste.
- **T-360 Phase 2 — token foundation complete (Z-index + shadow + typography v2.1 + letter-spacing + line-height)** (2026-05-26, FE): Remaining 15% scope của T-360 implemented sau Phase 1 + Wave 1-3 migration. Add to `apps/web/src/styles/globals.css` + `apps/web/tailwind.config.ts`: **Z-index** 10 named tiers wired qua CSS custom properties (`--z-base` 0 → `--z-dev-tweaks` 9999) + Tailwind `zIndex` extend (`z-modal`/`z-lightbox`/`z-popover`/`z-dropdown`/etc. utility classes); **Shadow recipes** 17 new tokens — 4-tier cyan glow scale (`glow-cyan-xs/sm/md/lg`), 7 per-color glow-md (mag/pur/grn/yel/ora/red/blu for ReactionPicker hover + mood badge + tag color hover), 4 drop shadows (`drop-sm/md/lg/xl`), 1 compound `stack` cho modal cyan-border + dark drop combo; **Typography v2.1** 5 variant fontSize tokens (`text-h1-hero` 26 SG hero glitch / `text-input-hero` 18 Inter search big input / `text-display-sm` 24 / `text-mono-tiny` 8 status dot + page indicator / `text-display-glyph` 40 empty-state large glyphs); **letterSpacing** 3 tokens (`wide-1/2/3` = .05/.06/.08em); **lineHeight** 3 tokens (`relaxed-1/2/3` = 1.75/1.8/1.9 cho bio/sidebar/sparkline). New test file `apps/web/tests/styles/design-tokens.test.ts` — 3 cases (z-index 9 named tiers + shadow class shape + typography v2.1 + letterSpacing + lineHeight). 344/344 FE pass (341 + 3). Tokens additive non-breaking — chưa migrate consumer call-sites (deferred sang T-371 AdminPage / T-373 TagsPage / T-374 ProfilePage / T-377 ImageCarousel page rewrite tasks).
- **Typography token foundation + screen font-size sync sweep** (2026-05-25, FE, T-360 partial — 8 code commits + 1 spec commit):
  - **Spec (`701c959`):** DESIGN_SYSTEM Typography table updated — narrow `text-mono-xs` scope (bỏ "Badges", chỉ "hex IDs corner deco"), clarify `text-mono-sm 11px` cho role badges, add `text-mono-md 13px` row, append M11.8 implementation note.
  - **Tokens (`6b4f894`):** Add 7 base tokens vào `tailwind.config.ts` (đã spec từ trước nhưng chưa implement): `mono-md 13px`, `small 13px`, `body 15px`, `h3 14px`, `h2 18px`, `h1 22px`, `display 28px`.
  - **Implementation (`3c60c0f`, `02bf43f`, `e73b8a5`, `10c02c5`, `6669916`, `2398921`):** Fix font-size drift across 6 screens — Admin StatCard 24→28, TopBar shared (⌘K/version 9→11, AvatarMenu ADMIN badge), ProfilePage hero handle 18→14, PostContent body 14→15, TerminalCard Login header 14/10/12→15/11/13, SearchPage/TagsPage StatBox 20/24→28. PostHeader timestamp + PostCard action buttons đã match design (no change).
  - **Test update:** `PostContent.test.tsx` — regex `/14px/` → `/text-body/` sau khi spec đổi sang 15px (test-stale-assumption per CLAUDE.md). 313/313 FE tests pass.
  - **Deferred:** Notifications page (skeleton only — T-314/T-352) + Manage Posts page (greenfield T-372) + v2.1 variant tokens (`text-h1-hero 26`, `text-input-hero 18`, `text-display-sm 24`, `text-mono-tiny 7-8`, `text-display-glyph 32-48`) + Z-index 9 tiers + Shadow recipes 10 tokens + Letter-spacing + Line-height tokens — all remain TODO trong T-360 chính.

- **T-360 Wave 1-3 Full token migration sweep** (2026-05-25 follow-up — 4 code commits):
  - **Wave 1a** (`b653d8a`): Migrate text-mono-xs → text-mono-sm cho 7 files (Feed + Comment + Notifications + Layout, 17 occurrences) — UI labels/badges/hints/kbd shortcuts.
  - **Wave 1b** (`b0ce57c`): Bulk migrate text-mono-xs → text-mono-sm cho 24 remaining files (Admin/Profile/Tags/Search/Saved/CreatePost/Post/Shared + globals.css `.sb-lbl`). Keep PostCard hex ID corner deco (only file dùng text-mono-xs đúng spec).
  - **Wave 2** (`8e06e29`): Login + Register form labels text-[10px] → text-mono-sm (11px) per design-file Login.html L254/258/293/310 (10 occurrences). Status dot text-[8px] giữ nguyên (design intentional 7-8px).
  - **Wave 3** (`3e3f1e2`): Convert 38+ hardcoded text-[XYpx] → token classes (text-mono-sm/mono/mono-md/mono-lg/body). Kept arbitrary cho contexts không có clean token (text-[8/9/10px] status/icons, text-[15px] font-brand, text-[18px] emoji).
  - **Wave 4 UNCLEAR resolved** automatically trong Wave 1b (`.sb-lbl` globals.css + EmojiPicker + ModerationQueue đã swap).
  - **Result:** Token migration ~85% done. Còn lại 19 arbitrary `text-[XYpx]` legitimate (no clean token mapping). 313/313 FE tests pass.

### Fixed

- **T-380 BUG-006 [Critical] AdminPage `/admin` TypeError crash — BE/FE contract drift `likes` → `reactions`** (2026-05-26, FE): UI Design Fidelity Review 2026-05-26 phát hiện `/admin` crash với `TypeError: Cannot read properties of undefined (reading 'total')`. Root cause: M11.7 multi-reaction migration rename BE field `likes` → `reactions` ([admin-response.dto.ts:20](apps/api/src/admin/dto/admin-response.dto.ts#L20) + openapi.yaml StatsResponseDto) nhưng FE consumer ([admin.ts:21](apps/web/src/services/api/admin.ts#L21) type + [AdminPage.tsx:69](apps/web/src/pages/AdminPage.tsx#L69) JSX) + MSW test mock ([AdminPage.test.tsx:35](apps/web/tests/pages/AdminPage.test.tsx#L35)) vẫn dùng `likes` (stale). MSW copy FE bug → drift escape test detection. Fix: rename FE `likes` → `reactions` 3 sites (type + JSX + STAT_COLORS + label `"LIKES"` → `"REACTIONS"` + MSW mock). New regression test `regression BUG-006: reads stats.reactions (not stats.likes) — BE renamed field in M11.7 multi-reaction migration` assert REACTIONS label + value render + no exception. 5/5 AdminPage + 341/341 full FE suite pass. **Lesson:** MSW mocks copy FE expectations, không phải BE actual shape → openapi-driven types (T-302 cutover defer) cần wire để eliminate hand-typed drift fundamentally.
- **T-379 BUG-005 [High] REPLY notification missing — replies fire wrong COMMENT notification** (2026-05-25, BE): T-343 audit follow-up. `comments.service.ts` create() branch logic — `dto.parentId` set → `NotificationType.REPLY` to parent comment author với `metadata.replyTo: { username }`. Skip nếu parent anonymous (userId null) hoặc self-reply (avoid spam). Top-level comment giữ COMMENT → post author behavior. 2 new unit tests (REPLY trigger + anon parent skip). Plus stale list() test mocks updated cho T-344 refactor (`parentId: null` filter + `_count.replies` field). 22/22 unit + 27/27 e2e pass.
- **T-342 BUG-003 [Medium] Login scanCard animation drift** (2026-05-25, FE): `tailwind.config.ts` rename `scan-line 6s` → `scan-card 4s` + keyframe `top: -100%→200%` (was `translateY`). Note: `TerminalCard.tsx` đã dùng inline `scanCardStripe 4s` (correct) từ trước — tailwind entry là dead code nhưng được cleanup cho consistency. Regression test: `tests/components/auth/TerminalCard.test.tsx` 3 cases.
- **T-378 BUG-004 [Low] ADMIN badge vertical alignment + undersized font (3 sites)** (2026-05-25, FE):
  - `c97e1f0` ProfilePage hero: layout + padding fix (`inline-flex items-center` + `leading-none` + `padding: 1px 6px` + `bg-{ora|red}/[0.06]`).
  - `668101c` ProfilePage hero: font `text-mono-xs` (9px) → `text-mono-sm` (11px) — brackets render đúng baseline.
  - **(next commit)** PostHeader (Feed PostCard) + PostPreview (Create Post): apply cùng pattern. Match `design-file/MyBlog Profile.html L488` spec ở cả 3 call sites.
- **T-341 BUG-002 [High] ProfileAvatar 6 visual/animation bugs** (2026-05-25, FE):
  - Initial fix (commit `4c9b622`): gradient ring 3-stop linearGradient (cyan→pur→mag) + `stroke-dasharray="6 4"` + `animate-border-rotate` + 2px solid cyan border + glow shadow + text-shadow + online dot 12×12 `animate-pulse`. `tailwind.config.ts`: `glitch 8s` (was 9s) + `pulse-status` keyframe → opacity+drop-shadow glow (NOT scale) + 4 new keyframes (`borderRotate 8s`, `liveDot 1.5s`, `slideIn 250ms`, `slideDown 200ms`).
  - **Visual sync refinement** (commits `b492c9d` + `b7b5524`): SVG `viewBox` → concrete `width/height` props để stroke 2px + dash 6/4 render pixel-exact (trước đó bị scale 0.88×). Gradient stop 3 `#FF79C6` → `#FF6E96` (rose). Online dot `#50FA7B` → `#9ECE6A` (olive). Inner avatar margin 3px → 4px all sides. Explicit `transformOrigin: 50% 50%` cho SVG (default origin = 0,0 trên một số browser → ring quay lệch tâm).
  - Regression test: `tests/components/shared/ProfileAvatar.test.tsx` 3 cases `regression BUG-002`. 5/5 tests pass after refinement.
- **T-340 BUG-001 [High] ReactionPicker hover gap** (2026-05-25, FE): Thêm invisible CSS bridge div (`h-3 absolute bottom-full`) lấp gap `mb-2` (8px) giữa button và picker trong `ReactionButton.tsx`. Mouse cross gap → luôn trên DOM child của container → `onMouseLeave` không fire → close instant (0ms, không delay). Regression test: `tests/components/feed/ReactionButton.test.tsx` case `regression BUG-001`.

### Added

- **M11.9 backlog opened (2026-05-25, docs-only):** Convert 19 deferred items từ "Out of M11.8 scope (defer phase 2)" thành 18 actionable tasks T-360 → T-377 (StatusBar dropped vì FE component đã match spec). Target milestone date 2026-06-26. Pure FE refactor — KHÔNG touch BE, KHÔNG cần FR amendment (specs đã có trong DESIGN_SYSTEM.md + UI_DESIGN.md commit `24c040e`):
  - **Theme A Token foundation (P1):** T-360 token system (Z-index 9 tiers + Shadow recipes 10 named + Typography refinement + letter-spacing + line-height vars) + T-361 animation registry tailwind config (5 new keyframes borderRotate/liveDot/slideIn/slideDown/cursorBlink + fade-up split 5 variants).
  - **Theme B Shared components (P1/P2):** T-362 Toast shared + useToast hook + ToastProvider + T-363 UploadZone extract to shared.
  - **Theme C TopBar + layout refactor (P2):** T-364 AvatarMenu 7-item extract + T-365 CommandPalette 8 commands + ⌘K global handler.
  - **Theme D Create Post enhancements (P2):** T-366 EmojiPicker inline (4×16 groups) + T-367 TagPickerDropdown master-data + T-368 RichTextEditor contentEditable + 11 toolbar + 2 popovers + T-369 LinkInsertModal saveSelection/restoreSelection.
  - **Theme E Page rewrites (P2):** T-370 LoginCard polish (anonymous + register + bracket logo) + T-371 AdminPage rewrite (SubBar + StatCards + 2-col + users table + comments moderation) + **T-372 ManagePostsPage greenfield (SUPERSEDES T-321/322/323)** + T-373 TagsPage TagModal NEON_COLORS refactor + T-374 ProfilePage hero rewrite (large) + T-375 PostMiniCard + T-376 EditProfileDrawer 4-section redesign + T-377 ImageCarousel Post Detail refresh.

- **M11.8 design-file sync + F2 amendments** (2026-05-25, docs-only commits `24c040e` + `f9f407a`):
  - **FR-17 AI Content Generation (NEW)** — Admin `POST /ai/generate` via Anthropic Claude (`claude-haiku-4-5`) + AISuggestModal trong Create Post + rate limit 10 req/min/admin + Sentry cost tracking. NEW UC-22. 8 sub-FRs (scope/endpoint/provider/prompt template/UI spec/override warning/cost guard/error handling). 4 new env vars (`AI_PROVIDER`, `AI_API_KEY`, `AI_MODEL`, `AI_RATE_LIMIT_PER_MIN`). New AIModule (NEW) trong BE.
  - **FR-03.6 reply-to-comment MVP (NEW)** — Comment thêm `parentId String?` self-reference + `replyTo Json?` denorm + cascade delete + `@@index([parentId])`. Depth 1 only (validate `parentComment.parentId === null` reject 400 `INVALID_PARENT_DEPTH`). POST `/comments` body accept optional `parentId`. NEW endpoint `GET /comments/:id/replies` paginated. NEW UI components `ReplyForm` + `ReplyRow` + `CommentItemRow` (CommentItem refactor). Notification type=REPLY trigger với `metadata.replyTo`.
  - **FR-04.7 CommentsModal Feed pattern (NEW, DEFINITIVE)** — Feed `💬` click opens modal popup overlay (KHÔNG navigate `/post/:id`). Post Detail page reserved cho deep-link/SEO. NEW CommentsModal component (640px portal vào body, infinite scroll PAGE_SIZE 5, page indicator dots, integrated CommentForm footer). Reuse `CommentItemRow` + `ReplyForm` + `ReplyRow` từ FR-03.6.
  - **3 user-reported bugs logged** trong BUGS.md:
    - BUG-001 [High] ReactionPicker hover gap — missing 250ms close debounce. User không chọn được reaction khi hover qua 6px gap.
    - BUG-002 [High] ProfileAvatar 6 visual/animation bugs + tailwind config drift — `spin 4s` vs `borderRotate 8s`, dasharray sai, solid stroke vs gradient, 1px vs 2px border, missing online dot, missing inner/text shadow. Plus `glitch 9s` vs `8s` + `pulse-status` keyframes scale-shrink vs design `pulse` drop-shadow glow.
    - BUG-003 [Medium] Login `scan-line 6s` vs design-file `scanCard 4s` + missing keyframe rename.
  - **M11.8 backlog 20 tasks (T-340 → T-359)** trong TASKS.md: 3 F3 bug fixes (T-340/341/342, P1 priority cao user-blocking) + 3 F2 BE prerequisites (T-343/344/345 migration + endpoints) + 14 F1 implementations (AI module BE+FE, CommentsModal Feed + PostCard refactor, ReplyForm + ReplyRow, SearchPage rewrite, NotificationsPage rewrite + NotifRow split, design visual sync — PostCard action row + ImageLightbox + PostActionMenu + ReactionIcon + ReactionPicker container + NotificationBell visual). Plus 20+ out-of-scope items defer phase 2.
  - **Design system token refinement (DESIGN_SYSTEM.md commit `24c040e`)**: Z-index scale 9 tiers (`--z-base` 0-3 / `--z-popover` 50-60 / `--z-subbar` 90 / `--z-topbar` 100 / `--z-dropdown` 200 / `--z-modal` 300 / `--z-modal-stacked` 400 / `--z-lightbox` 500 / `--z-dev-tweaks` 9999) + Shadow recipes 10 named tokens + Motion tokens expanded (5 new: `motion-borderrotate` 8s, `motion-livedot` 1.5s, `motion-slidein` 250ms, `motion-slidedown` 200ms, `motion-scancard` 4s renamed).
  - **NEW components grouped trong DESIGN_SYSTEM.md** (Phase 1 + Phase 2 từ design-file 2026-05-24 deep audit): ImageLightbox, PostActionMenu (with `🔖 Save post`), CommentsModal (DEFINITIVE), ReactionIcon (6 SVG line-art), AvatarMenu (7-item), SubBar pattern, LoginCard refresh, Toast pattern, NotifRowBell + NotifRowPage (split 2 variants).

- **T-313** NotificationBell FE primitive (M11.7, FR-14.3, FE-only):
  - `types/api.ts` — new types: `NotificationType`, `NotificationActor`, `NotificationMetadata`, `NotificationItem`, `NotificationListResponse`, `UnreadCountResponse`, `MarkReadResponse`, `MarkAllReadResponse`, `BulkDeleteNotificationsResponse`, `ListNotificationsParams`.
  - `services/api/notifications.ts` (NEW) — 6 functions wrapping notification REST endpoints: `listNotifications`, `getUnreadCount`, `markNotificationRead`, `markAllNotificationsRead`, `deleteNotification`, `bulkDeleteNotifications`.
  - `lib/query-keys.ts` — adds `qk.notifications.all`, `qk.notifications.list(params)`, `qk.notifications.unreadCount`.
  - `hooks/queries/use-notifications.ts` (NEW) — `useNotifications(params)` via `useQuery`.
  - `hooks/queries/use-unread-count.ts` (NEW) — `useUnreadCount()` with `refetchInterval: 30s`, enabled when authed.
  - `hooks/mutations/use-notification-mutations.ts` (NEW) — `useMarkNotificationRead`, `useMarkAllNotificationsRead`, `useDeleteNotification` (all invalidate `qk.notifications.all`).
  - `components/layout/NotificationBell.tsx` (NEW) — composite: bell button + badge (bg-mag, `animate-pulse-status`, 99+) + dropdown 360px (header with unread count + mark-all-read button, segmented tab All/Unread, time-grouped list rows with AvatarSm/verb/relative-time/unread dot, footer "view all → /notifications" link). Click outside + Esc close. Click row → `useMarkNotificationRead` + navigate target.
  - `components/layout/TopBar.tsx` — wires `<NotificationBell />` before Avatar dropdown (authed-only).
  - `tests/_helpers/msw-server.ts` — adds 2 default MSW handlers (notifications/unread-count + notifications list) so all existing tests with TopBar continue to pass without stub noise.
  - **Tests:** `tests/components/layout/NotificationBell.test.tsx` (6 case — badge hidden count=0 / badge text count>0 / badge 99+ count>99 / dropdown toggle + click-outside close / tab switch All→Unread / click item nav+mark-read). Totals: **FE 306 unit tests pass, tsc + lint clean**.

- **T-317** Reactions FE + BE PostView extend (M11.7, FR-16.4/16.5, Both):
  - **BE:** `posts.service.ts` — new helper `buildReactionMetaMap(prisma, postIds, viewer?)` — single `prisma.reaction.groupBy({by:['postId','type']})` + viewer-aware `findMany` → returns `Map<postId, {topReactions[3], myReaction}>`. `toPostView` extended with 2nd arg `meta?: ReactionMeta` (default empty). `list()`/`findById()` now accept `viewer?: PostsViewer` and pass meta into mapping. `search.service.ts` also takes `viewer` for posts results.
  - **BE controllers:** `posts.controller.ts` GET `/posts` + GET `/posts/:id` now wrap `JwtOptionalAuthGuard` and pass `{userId, anonymousId}`; `search.controller.ts` same pattern. `posts/dto/post-response.dto.ts`: `PostResponseDto` adds `topReactions: ReactionType[]` + `myReaction: ReactionType | null`.
  - **FE types:** `types/api.ts` — `PostCounts.likes → reactions`; `Post` adds `topReactions: ReactionType[]` + `myReaction: ReactionType | null` (removed `liked?`); new exports `REACTION_TYPES`, `ReactionType`, `TotalReactionCounts`, `UpsertReactionResponse`, `ReactionCountsResponse`, `ReactionListItem`, `ReactionListResponse`, `ListReactionsParams`.
  - **FE service + config:** `services/api/reactions.ts` (NEW) — `upsertReaction(postId, type)` / `removeReaction(postId)` / `getReactionCounts(postId)` / `listReactions(postId, params)`. `services/api/likes.ts` trimmed to comment-only (`toggleCommentLike`). `lib/reaction-config.ts` (NEW) — `REACTION_CONFIG` (emoji/label/color per type), `REACTION_LIST`, `emptyReactionCounts()`.
  - **FE hooks:** `hooks/mutations/use-reaction.ts` (NEW) — `useUpsertReaction` + `useRemoveReaction` with cache-level optimistic patches (list + detail + reactionCounts) + invalidate-on-settle. 410 Gone passes through to caller. `hooks/queries/use-reaction-counts.ts` (NEW) + `hooks/queries/use-reaction-list.ts` (NEW). `lib/query-keys.ts` adds `qk.posts.reactionCounts(id)` + `qk.posts.reactionList(id, params)`.
  - **FE components:** `components/feed/ReactionPicker.tsx` (NEW) — popover 6 emoji buttons 36×36 pill bg-surf, hover scale 1.25x, selected scale 1.1x + bg-elev. `components/feed/ReactionList.tsx` (NEW) — portal-rendered modal 480px, tab bar All + 6 types với byType count, scrollable list (actor avatar / username / emoji / relative time), Esc close + backdrop click close. `components/feed/ReactionButton.tsx` (NEW, replaces `LikeButton`) — local optimistic mirror (reset useEffect on prop change), hover trigger opens picker với `relatedTarget contains` check to survive gap to absolute picker, click trigger = LIKE (or remove if active), click count = open modal, 410 Gone → disable + inline error.
  - **FE wiring:** `PostCard.tsx` + `PostDetailPage.tsx` swap `<LikeButton>` → `<ReactionButton>` (props: myReaction/topReactions/count.reactions). `MetaPanel.tsx` `Likes → Reactions`. `tests/_helpers/post-factory.ts` updated (counts.reactions + topReactions[] + myReaction).
  - **Deleted:** `components/feed/LikeButton.tsx`, `hooks/mutations/use-like.ts`, `tests/hooks/mutations/use-like-save.test.tsx`.
  - **Tests:** `tests/components/feed/ReactionButton.test.tsx` (8 case T-317 — picker hover reveals 6 emoji / click trigger optimistic LIKE / change type LIKE→LOVE via picker / toggle off / top3 + count display / modal open / modal tab filter / 410 fallback disable + alert) + 3 BE e2e on GET /posts/:id (empty / multi-react top3 sorted + auth myReaction / null viewer myReaction null) + adjusted `posts.service.spec.ts` mock prisma `reaction.groupBy/findMany`. Totals: **BE 125 unit + 189 e2e + FE 300 unit = 614 tests pass, tsc + lint clean**.
  - `docs/contracts/openapi.yaml` + `apps/web/src/types/api.generated.ts` regenerated via `pnpm openapi:sync`.

- **T-312** BE Notifications REST endpoints (M11.7, FR-14.3/14.4/14.5, BE-only):
  - `src/notifications/notifications.controller.ts` (NEW): 6 endpoints under `@Controller('notifications')` + `@UseGuards(JwtAuthGuard)`.
    - `GET /notifications` — list with `filter=all|unread`, `page`, `limit` (max 50); response `{ items, total, page, limit, unreadCount }`. Items include actor (id/username/avatarUrl), type, targetType, targetId, postId, read, metadata, createdAt.
    - `GET /notifications/unread-count` — `{ count: number }`.
    - `PATCH /notifications/mark-all-read` — mark all unread → read; `{ updated: number }`.
    - `PATCH /notifications/:id/read` — Body `{ read: boolean }`; self-scope 403; `{ id, read }`.
    - `DELETE /notifications/bulk` — Body `{ ids: string[] }` max 100; self-scope (non-own silently skipped); `{ deleted: number }`. 400 if empty.
    - `DELETE /notifications/:id` — self-scope 403; 204.
  - Static routes (`mark-all-read`, `bulk`) declared before parameterized (`:id`) to prevent NestJS route conflict.
  - `src/notifications/dto/`: `list-notifications.dto.ts`, `mark-read.dto.ts`, `bulk-delete.dto.ts`.
  - `NotificationsModule`: added `NotificationsController` to controllers.
  - Tests: `tests/notifications.e2e-spec.ts` (10 cases: list all/unread, unread-count, mark-read, 403 other-user, mark-all-read, delete single + 403, bulk delete + 400 empty). **186 e2e pass**, 125 unit pass, tsc clean.

- **T-311** BE NotificationsModule + createNotification hook (M11.7, FR-14.1/14.2, BE-only):
  - `src/notifications/notifications.service.ts` (NEW): `createNotification(input)` — self-action guard (`actorId === userId` → skip), creates `Notification` row, best-effort (caller wraps in try-catch). `metadata` cast to `Prisma.InputJsonValue`.
  - `src/notifications/notifications.module.ts` (NEW): exports `NotificationsService`.
  - `ReactionsService.upsertReaction`: after new reaction create (not update, not anonymous, not self), fire `REACTION` notification to post.authorId with `metadata.reactionType`.
  - `CommentsService.create`: after comment create by authed user, fire `COMMENT` notification to post.authorId.
  - Both hooks are **best-effort** (`try/catch` + `logger.warn`) — parent operation never fails due to notification error.
  - `ReactionsModule` + `CommentsModule` + `AppModule` import `NotificationsModule`.
  - Tests: `reactions.service.spec.ts` +1 case (REACTION called on new auth-user reaction), `comments.service.spec.ts` +1 case (COMMENT called on auth-user comment). **125 unit pass**, tsc clean.

- **T-310** BE Notifications migration (M11.7, FR-14.1, BE-only):
  - Migration `20260524140000_add_notification_table`: CREATE TABLE `"Notification"` + CREATE TYPE `"NotificationType"` (REACTION/COMMENT/REPLY/SHARE). Fields: id/userId/actorId/type/targetType(String poly)/targetId(soft-FK)/postId?(FK SetNull)/read(default false)/metadata(Json?)/createdAt. FK Cascade on userId+actorId (actor deleted → notification deleted), SetNull on postId (post deleted → postId null).
  - Indexes: `[userId, createdAt]` (list per user DESC) + `[userId, read]` (unread count fast query).
  - `schema.prisma`: `NotificationType` enum + `Notification` model + `User.receivedNotifications/sentNotifications` + `Post.notifications` relation.
  - `tests/_helpers/db-reset.ts`: added `prisma.notification.deleteMany` + `prisma.activityLog.deleteMany` to cleanup between e2e tests.

- **T-316** BE Reactions — rename Like → Reaction + ReactionsModule + 4 endpoints (M11.7, FR-16.x, BE-only):
  - Migration `20260524125935_rename_like_to_reaction_with_type`: data-preserving `ALTER TABLE "Like" RENAME TO "Reaction"` + `ADD COLUMN type ReactionType DEFAULT LIKE` + `ADD COLUMN updatedAt` + new index `[postId, type]`. Existing rows backfill to LIKE (zero data loss).
  - `src/reactions/reactions.service.ts` (new): `upsertReaction()` (create or change type, activity-log on first react), `removeReaction()` (404 nếu chưa react), `getReactionCounts()` (groupBy aggregate → 6-type totalCounts + topThree + myReaction for authed viewer), `listReactions()` (paginated + optional type filter), `toggleCommentLike()` (binary carry-over from LikesService, uses `prisma.commentLike`).
  - `src/reactions/reactions.controller.ts` (new): `POST /posts/:id/reactions`, `DELETE /posts/:id/reactions` (204), `GET /posts/:id/reactions/counts`, `GET /posts/:id/reactions`, `POST /posts/:id/like` (410 Gone legacy), `POST /comments/:id/like` (unchanged).
  - Old `src/likes/` module deleted. `app.module.ts` swapped `LikesModule → ReactionsModule`.
  - `posts.service.ts` + `posts/dto/post-response.dto.ts`: `_count.likes → reactions`, `PostCountsDto.likes → reactions`, `toPostView()` + sort orderBy updated.
  - `admin.service.ts` + `admin/dto/admin-response.dto.ts`: all `prisma.like.*` → `prisma.reaction.*`, response key `likes → reactions`.
  - `users.service.ts`: `prisma.like.count` → `prisma.reaction.count` (getStats likes received).
  - Tests: `tests/reactions/reactions.service.spec.ts` (8 unit), `tests/reactions.e2e-spec.ts` (14 e2e). Updated `posts.service.spec.ts`, `admin.service.spec.ts`, `saved.service.spec.ts`, `posts.e2e-spec.ts`, `admin.e2e-spec.ts`, `saved.e2e-spec.ts`, `users.e2e-spec.ts`. All **123 unit + 175 e2e pass**.

- **T-330** Foundation v2 refresh (M11.7, FR-15/NFR-02, FE-only):
  - `apps/web/tailwind.config.ts`: extend `theme.screens` với 5-tier max-width sub-breakpoints `mx-980 / mx-760 / mx-640 / mx-480 / mx-420` cho design v2 compress pattern (giữ nguyên `sm/md/lg/xl/2xl` defaults).
  - `apps/web/src/styles/globals.css`: thêm 3 CSS vars typography v2 — `--fs-ui: 11px` (UI label mono), `--fs-ui-text: 13px` (meta mono), `--fs-body: 15px` (body content prose) — upper bound per DESIGN_SYSTEM:75.
  - `apps/web/src/lib/status-config.ts` (NEW): `PostStatus` type + `POST_STATUS_CFG` Record (PUBLISHED `#9ECE6A` / DRAFT `#E0AF68` / ARCHIVED `#566176`) + `POST_STATUS_KEYS`. Mirror pattern `mood-config.ts`. Sync với Prisma enum `PostStatus` (T-320 sẽ tạo migration).
  - `apps/web/src/components/shared/StatusBadge.tsx` (NEW): variant `post` — inline-flex pill `[ LABEL ]` font-mono 11px với tinted bg (12% alpha) + border (40% alpha) + opacity 0.8 cho ARCHIVED (visual "less active"). Port từ `design-file/MyBlog Manage Posts.html:379`.
  - Tests: `tests/lib/status-config.test.ts` (2 case) + `tests/components/shared/StatusBadge.test.tsx` (4 case) — 6 case mới, all pass; total FE **296 tests pass** (was 290).
  - Foundation cleared cho downstream M11.7 task (T-313/314/317/321/322/323/331-334 đều consume tokens này).

- **F2 spec for M11.7 — Design v2 (Notifications + Admin Manage Posts + Multi-Reactions) (2026-05-24, docs only):** Design v2 overhaul commit baseline `a56ee72` (2 screen mới + foundation refresh). F2 docs spec execute trên 7 doc:
  - `docs/REQUIREMENTS.md`: thêm FR-14 (Notification System, 6 sub) + FR-15 (Admin Manage Posts, 6 sub) + FR-16 (Multi-Reaction System, 6 sub thay binary Like) + NFR-06 (Pagination universal) + UC-17/18/19/20/21 + Glossary 6 terms (Notification, Engagement event, NotificationBell, PostStatus, Reaction, ReactionType) + Traceability rows FR-13/14/15/16
  - `docs/DATA_MODEL.md`: entity Notification (+ NotificationType enum) + rename Like → Reaction (+ ReactionType enum 6 values LIKE/LOVE/HAHA/WOW/SAD/ANGRY + index `[postId, type]`) + Post.status field (PostStatus enum PUBLISHED/DRAFT/ARCHIVED) + Prisma snippet sync + migration log v0.4.0-alpha
  - `docs/API_CONTRACT.md`: 6 Notifications endpoint + 4 Reactions endpoint (rename `/likes` → `/reactions`, multi-type) + 3 Admin Posts endpoint (PATCH/DELETE/GET) + WS event `notification:new` + `reaction:new` (replace `like:new`); legacy `POST /posts/:id/like` → 410 Gone 1 release window
  - `docs/UI_DESIGN.md`: Screen 11 Notifications (`/notifications`) + Screen 12 Manage Posts (`/admin/posts`) — wireframe ASCII + state machine + interactions + responsive
  - `docs/DESIGN_SYSTEM.md` v2.0: NotificationBell primitive + ReactionPicker + ReactionList modal + StatusBadge PostStatus variant + typography v2 +1px note + responsive 5-tier sub-breakpoints note + Token Change History entry
  - `docs/TASKS.md` M11.7 backlog: 17 task T-310→T-334 (Foundation T-330, Reactions T-316/317, Notifications T-310-315, Admin Posts T-320-323, Polish T-331-334)
  - `docs/PROGRESS.md`: thêm M11.7 row 🟡 Doing, tổng 13/17 milestone, weekly log entry 2026-05-24
  - **F2 STOP:** docs xong, chưa implement code. Sau khi user duyệt backlog → F1 Foundation → BE Reactions → BE Notifications → openapi:sync → FE Reactions → FE Notifications → BE+FE Admin Posts → Polish 8 màn cũ.

- **T-301** ActivityLog FE (M11.6 Wave B3 close, FR-13.1): hand-typed types thêm vào `apps/web/src/types/api.ts` (ActivityType / ActivityTargetType / ActivityDirection / ActivityItem / PaginatedActivity / ListActivityParams — T-302 cutover deferred nên thêm thủ công). `services/api/activity.ts` `listUserActivity(userId, params)`. `useUserActivity` qua `useInfiniteQuery` với `qk.users.activity(id, params)` key, page-based getNextPageParam. **ProfileActivityList** component (path `apps/web/src/components/profile/`): icon map per ActivityType (📝/💬/👍/🔖) + verb template direction-aware (OUTGOING `You {verb} <snippet>` / INCOMING `<actor> {verb} your post · <snippet>`). Click target snippet → `/post/:id`; snippet null → render `[deleted post]` text-tm, không link. IntersectionObserver sentinel `rootMargin: 200px` trigger fetchNextPage. Loading state `// loading activity...`, 403 → `// activity is private`, error → `// failed to load activity`, empty → `// no activity yet`. ProfilePage Activity tab wire (`canViewSaved` = self/admin gates cả Saved + Activity tab + tab content). 5 FE test (empty / 4 icon + OUTGOING text / INCOMING text + actor link / deleted snippet no link / 403 private hint). Total **FE 290 tests pass** (was 285). **M11.6 ✅ Done 2/2 task (T-300 + T-301).**

- **T-300** ActivityLog BE foundation (M11.6 Wave B2, FR-13): migration `add_activity_log` thêm model ActivityLog (actorId FK Cascade + type ActivityType + targetType ActivityTargetType + targetId soft-FK polymorphic + targetOwnerId FK Cascade nullable + metadata Json? + createdAt) với 2 enum + 2 index `[actorId, createdAt]` & `[targetOwnerId, createdAt]`. ActivityModule mới (service + controller + DTO + spec): `ActivityService.log()` best-effort try-catch (không break parent), `listForUser(userId, page, limit)` hybrid query `WHERE actorId = :id OR (targetOwnerId = :id AND actorId != :id)`, hydrate actor (JOIN) + target snippet truncate 80 batch Post/Comment lookup (null nếu deleted), compute direction OUTGOING|INCOMING per row. Controller `GET /users/:id/activity` JwtAuthGuard + visibility self/admin (403 cho non-self) + 404 user not exists. ListActivityDto validator page/limit (max 50). Hook ActivityService.log() vào 4 service: PostsService.create POST_CREATED, CommentsService.create COMMENT_CREATED (skip anonymous, lookup post.authorId), LikesService.togglePostLike LIKE_CREATED only on like=true + skip anonymous, SavedService.toggleSave SAVE_CREATED only on save=true. Mocks ActivityService trong Likes/Comments/Saved/Posts service spec. Test: 7 unit (log shape + best-effort fail safe + listForUser empty/OUTGOING/INCOMING/deleted snippet null/truncate 80) + 8 integration (401 anon / 403 other / 200 self direction mix / 200 admin override / 404 user gone / pagination page=2 / POST_CREATED hook qua POST /posts / SAVE_CREATED hook only on save not unsave). Total **BE 126 unit (was 119) + 169 integration (was 161) = 295 BE tests pass**.

- **T-009 partial** OpenAPI auto-gen workflow (Wave A1 + A3, F7 chore): scaffold pipeline tự động generate spec từ NestJS Swagger.
  - `apps/api/scripts/dump-openapi.ts`: bootstrap Nest standalone → `SwaggerModule.createDocument` → `js-yaml.dump` → write `docs/contracts/openapi.yaml` với banner cảnh báo. Runtime dùng `ts-node -r tsconfig-paths/register` (tsx không transform `@nestjs/swagger` decorator metadata).
  - Scripts: `apps/api` `openapi:gen`, `apps/web` `openapi:types` (output `api.generated.ts` reference, không đè hand-typed `api.ts`), root `openapi:sync`.
  - `turbo.json` (v2 tasks) thêm `openapi:gen` + `openapi:types` với inputs/outputs cho cache.
  - CI `lint-typecheck` job: step "OpenAPI drift check" chạy `pnpm openapi:sync` + `git diff --exit-code` → fail PR nếu BE controller/DTO thay đổi mà quên regen.
  - Install: `js-yaml@4` + `@types/js-yaml` (api), `openapi-typescript@7` (web).
  - Output: `docs/contracts/openapi.yaml` 1787 line (33 endpoint, thay placeholder 34 line cũ) + `apps/web/src/types/api.generated.ts` 1997 line.
  - `docs/CODING_CONVENTION.md` Universal section thêm "OpenAPI contract sync" rule.
  - **Wave A2 deferred (T-302):** cutover hand-typed `api.ts` → generated cần fix 15+ BE decorator gap (nullable string thiếu `type: String`, skills Json, nested DTO heatmap/moodBreakdown/tagsUsed, search response polymorphic). Ước 6-9h, defer session sau.

- **T-220** Profile BE foundation (M11.5 Wave 3 start, FR-11): migration `add_user_profile_fields` thêm `User.title` (max 80, nullable) + `User.bio` (nullable, max 500) + `User.skills` (Json default "[]"). Skills shape `{ name: string ≤32, color: hex }[]`. UpdateUserDto thêm validators (title max 80 / bio max 500 / skills ArrayMaxSize 20 + nested SkillItemDto IsHexColor). UserResponseDto thêm title/bio/skills + helper `parseSkills` an toàn parse Json. **3 endpoints mới:** `GET /users/by-username/:username` (@Public, email hidden), `GET /users/:id/stats` (@Public, response `{ postsCount, likesReceived, commentsReceived, viewsTotal, streak, heatmap28d: 28 zero-fill, moodBreakdown 7 zero-fill, tagsUsed top 8 }`), `POST /auth/change-password` (JwtRefreshGuard cần tid current session → bcrypt verify current pw → update + revoke ALL refresh tokens TRỪ current — 401 INVALID*CREDENTIALS / 400 < 8 chars). Test fixtures dùng `pwuser*\*` register fresh user → tránh side-effect lên test-admin. 4 BE auth integration + 9 BE users integration (PATCH skills + 400 max 20 + 400 hex invalid + 400 bio > 500 + by-username 404/200 + stats 404/zero/aggregate). **BE 152 integration tests pass** (was 139).
- **T-212** `/tags` page assemble (M11.5 Wave 2 close, FR-10.1/10.2): new `/tags` public route, single page với admin actions conditional. 4 StatBox cards (Total tags / Tagged posts / Most used / Recently added — derived từ data). Toolbar: search input (debounce qua state→query) + Sort dropdown (Name/Posts/Recent) + SegmentedToggle grid/list view. `useTags({ sort, q, limit: 100 })` hook + 3 mutations (`useCreateTag`/`useUpdateTag`/`useDeleteTag`) invalidate `['tags']` prefix. Admin-only `[ + New Tag ]` → TagModal create variant. Hover ✎ Edit → TagModal edit variant pre-filled. Hover 🗑 Delete → ConfirmDialog destructive — nếu BE 409 TAG_IN_USE → switch sang `forceDelete=true` + double-confirm message. Error mapping 409 → "Tag 'x' đã tồn tại", 400 → "Invalid input". 5 FE test (4 stat cards + grid + non-admin no actions + admin actions + create form submit + view toggle list). routes.tsx thêm `/tags` (public). Total **FE 228 tests pass** (was 223). **Wave 2 F2-A Tags ✅ 3/3 done.**
- **T-211** Tag FE primitives (M11.5 Wave 2, FR-10.3/10.4): types/api.ts thêm `Tag`, `TagWithStats`, `TagSort`, `ListTagsParams`, `ListTagsResponse`, `CreateTagPayload`, `UpdateTagPayload`. services/api/tags.ts: `listTags`, `createTag`, `updateTag`, `deleteTag(id, force)`. 5 components mới: **FilterChip** (toggle role=checkbox + aria-checked + cyan active state), **SegmentedToggle** (radio group T extends string với generic options + icon support), **ConfirmDialog** (modal w/ Esc + backdrop click + destructive variant red + pending loading state), **TagCard** (grid variant với color swatch + Sparkline 60×20 + progress bar role=progressbar % so với maxCount + admin hover overlay Edit/Delete, list variant flex-row compact, click navigate `/?tag=` Feed), **TagModal** (create/edit variant prop + name input auto-strip `#` + color picker 7-swatch TAG_COLORS + description textarea 280 + live preview chip + error banner + Esc close). 21 FE unit test (3 FilterChip + 2 SegmentedToggle + 5 ConfirmDialog + 5 TagCard + 6 TagModal). Total **FE 223 tests pass** (was 202).
- **T-210** Tags BE foundation (M11.5 Wave 2 start, FR-10): migration `add_tag_description_and_created_at` thêm `Tag.description String?` (max 280) + `Tag.createdAt DateTime @default(now())`. ListTagsDto thêm `sort: name|posts|recent` (default `posts`) + `q` substring filter. GET /tags response thêm `description`, `sparkline7d: number[7]` (last 7 days post-with-tag count buckets oldest→newest), `createdAt`. POST/PATCH /tags accept `description` (validator max 280). DELETE /tags/:id thêm `?force=true` — nếu postCount > 0 + force=false → 409 `TAG_IN_USE` với body `{ code, postCount, message }`. Existing cascade test cập nhật để dùng `?force=true`. 5 BE unit (sort=name/recent/posts/q insensitive + remove force guard) + 8 BE integration (sort=name asc + sort=recent newest + q filter + sparkline7d shape + POST description + 400 description >280 + 409 TAG_IN_USE + 204 orphan no-force + 204 force). Total **BE 119 unit (was 114) + 139 integration (was 123)**.
- **T-203** `/saved` standalone route (M11.5 Wave 1 close, FR-03.3): new `SavedPage` page authed-only — header `// saved.posts · N items`, `useQuery(qk.saved.list({}))` → `listSavedPosts` (đã có T-saved), render PostCard list reuse Feed pattern. Empty state `🔖 // no saved posts yet · browse feed && click 🔖 to save`. Loading skeleton + error hint. routes.tsx thêm path `saved` wrap `ProtectedRoute` (no role required — any authed). 3 unit test (empty với Link `/` / 1 saved item + total `1 items` / 500 → fail hint). Total **FE 202 tests pass** (was 199). **Wave 1 quick wins ✅ 4/4 done.**
- **T-202** Comment moderation queue UI + BE endpoint (M11.5 Wave 1, FR-07.4): BE thêm `GET /admin/comments?status=PENDING|APPROVED|REJECTED&page=&limit=` qua AdminController + `CommentsService.listForAdmin`. Response shape `{ items: [{ ...Comment, post: { id, content (truncate 80) } }], total, page, limit }`. AdminModule import CommentsModule cho dependency. ListAdminCommentsDto class-validator: status enum default PENDING + page/limit defaults. FE: `services/api/admin.ts` thêm `listAdminComments(status?, page?, limit?)` + AdminCommentItem type. `useAdminComments` hook query (default status=PENDING). `mutations/use-moderate-comment.ts`: `useUpdateCommentStatus` (PATCH /comments/:id/status) + `useDeleteComment` (DELETE /comments/:id) — onSuccess invalidate `['admin','comments']` prefix. `ModerationQueue` component thay placeholder section AdminPage: render pending count badge + item rows (status badge + author + relative time + content + truncated post title + ✓ Approve / ✕ Delete buttons). Empty state `// queue empty · no pending comments`. Loading + error states. 4 BE integration test (401/403/200-default-pending+truncate/200-filter-APPROVED/400-invalid-status) + 4 FE test (empty hint / render items với truncate / Approve PATCH body + URL / Delete DELETE id). Total **BE 14 admin integration (was 9) / FE 199 tests pass** (was 195).
- **T-201** FilterBar sort dropdown + BE `GET /posts?sort=` (M11.5 Wave 1, FR-04.6): BE thêm `sort: 'latest' | 'oldest' | 'likes'` vào `ListPostsDto` (`@IsIn(POST_SORT_VALUES)` validator, default `latest`). `PostsService.list` switch theo sort → orderBy `{ createdAt: 'desc' }` / `{ createdAt: 'asc' }` / `{ likes: { _count: 'desc' } }`. FE FilterBar đổi static "Latest ▾" button thành ARIA listbox dropdown — 3 options (Latest/Oldest/Most liked), outside-click + Esc close, controlled qua props `sort` + `onSortChange`, disabled khi không có handler. FeedPage + PostList truyền sort qua chuỗi → `usePostsInfinite` → query key thay đổi → refetch. `services/api/posts.ts toQuery` skip param khi `sort==='latest'` (giữ URL clean). 3 BE integration test (sort=oldest createdAt ASC + sort=likes \_count desc + invalid sort 400) + 2 BE unit (likes orderBy + oldest orderBy) + 4 FE test (disabled khi no handler + open listbox 3 options + click option callback+close + sort=likes label). Total **BE 114 unit + 28 posts integration / FE 195 tests pass** (FE was 191).
- **T-200** Copy link button MetaPanel (M11.5 Wave 1 start): tách `🔗 Copy link` ra khỏi SHARE_TARGETS array → dedicated button với state local `copied: boolean`. Click → `navigator.clipboard.writeText(${origin}/post/${id})` → state flip `copied=true` 2000ms → render `✓ // link copied` text grn + bg grn 10% / border grn 50%. Try-catch silently skip nếu clipboard unavailable (insecure context). 3 unit tests (clicks success + writeText URL + label toggle / clipboard reject does NOT throw + label unchanged / 3 social + copy buttons render). Total **191 FE tests pass** (was 188).

### Changed

- **M11.8 design-file sync (commit `24c040e`, 2026-05-25 docs-only):** DESIGN_SYSTEM.md + UI_DESIGN.md +513/-129 lines comprehensive update theo `design-file/` @ 2026-05-24:
  - **NotifRow split into 2 components** (NotifRowBell + NotifRowPage) — sizes (34×34/18×18/2px vs 40×40/20×20/3px) + 4 type configs (like/comment/share/save legacy vs reaction/comment/reply/share new) + features (page variant has checkbox + replyTo + mark toggle + delete actions). Code drift flag for FE refactor.
  - **PostCard action row** 3-button (React/Comment/Share) + `(ml-auto) ⋯` menu — **bỏ SaveButton standalone** (moved sang PostActionMenu item `🔖 Save post`). 💬 → CommentsModal popup (DEFINITIVE — KHÔNG navigate `/post/:id`).
  - **PostHeader Post Detail variant** action row chỉ 3 button + `(ml-auto) 👁 N views`, KHÔNG có Save, KHÔNG có ⋯ menu.
  - **NotificationBell visual refactor spec** — SVG bell (inline 15×15, 2 paths body+clapper) thay `🔔` emoji + bordered button (32×32 border `1px --b2` bg `--elev`) + badge ring `1.5px solid --surf` + threshold `> 9 → "9+"` (NOT 99+). Flag FE code drift.
  - **ReactionPicker container refactor spec** — panel radius 8 (KHÔNG pill) + 40×40 buttons (KHÔNG 36×36) + per-color `translateY(-2px) + glow` hover (KHÔNG `scale-125`) + SVG `<ReactionIcon>` thay emoji. Hover 250ms close debounce pattern.
  - **ReactionButton spec** — SVG ReactionIcon trigger + 250ms hover grace pattern (Hover-reveal popover with grace period — CRITICAL bug fix BUG-001).
  - **ProfileAvatar spec refresh** — rotating ring 8s `borderRotate` (NOT spin 4s) + linearGradient 3 stops stroke (cyan→pur→mag) + dasharray `"6 4"` + 2px solid border + inner shadow + text-shadow + online status dot (12×12 green pulse). 6-bug flag for FE.
  - **MOOD_CFG 7 moods table** + note 2 outliers (HAPPY #FFD93D + SAD #6BCFFF) KHÔNG trong 8-accent palette.
  - **Motion table `motion-glitch` 9s → 8s** + 5 new motion tokens.

- **M11.8 F2 amendments (commit `f9f407a`, 2026-05-25 docs-only):** Expanded scope cho 2 existing FRs:
  - **FR-12.8-.12 SearchPage expanded scope** — hero refresh (Inter 18 NOT mono), 3 filter chips (All/Saved/Files) + 7 mood emoji buttons, 3 empty-state sections (recent.searches / browse.tags / all.posts preview), no-results state với ◎ + bash hint, ResultCard top accent line + Highlight `<mark>` cyan/20.
  - **FR-14.7-.13 NotificationsPage expanded scope** — 6 type tabs (All/Unread/Reactions/Comments/Replies/Shares) thay 2 tabs cũ + search input client-debounce 150ms + bulk select bar + Toast feedback (3 variants slideDown 2500ms) + `✕ clear all` SubBar action + NotifRow split into 2 variants. NEW endpoints `PATCH /notifications/bulk-read` + `DELETE /notifications/all`. Flag F2 amend FR-14 trước F1 implement.
  - **PostPreview action row update** — static `♡ 0 · 💬 0 · 🏷 · ↗` → match new PostCard `[React + picker] / 💬 / ↗ Share / ⋯`.

- **Chore (design tooling, 2026-05-23):** Track `design-file/` trong git (bỏ khỏi `.gitignore`) để xem lịch sử thay đổi prototype qua git log/diff — design-file là bộ tham khảo, không phải source of truth. Chuyển lint-staged config từ `package.json` sang `lint-staged.config.mjs` (root) + filter `design-file/` ra khỏi eslint/prettier (eslint vốn ignore, tránh `--max-warnings=0` fail khi commit file ignored).

- **T-303** Relax change-password validation (FR-11.3 amend, 2026-05-19): `ChangePasswordDto` simplify còn `@IsString + @MinLength(5)` cho cả `currentPassword` + `newPassword` (drop `MaxLength(128)` + min 1/8). FE EditProfileDrawer đồng bộ: client check `< 5`, label `(min 5)`, input `minLength={5}`. Test `auth.e2e` rename `400 < 8 chars` → `400 < 5 chars` (payload `'short'` → `'abcd'`). Motivation: validator chain over-spec sinh error verbose 6 dòng khi payload sai — UX xấu; chính sách 8-char không cần thiết cho personal blog. **Đây là change request (F2 amend FR), không phải bug** — FR-11.3 đã update ghi rõ amendment date.

- **F2 spec for M11.6 — Activity Log (2026-05-19, docs only):** User-scope activity timeline với hybrid query (own actions + actions on own posts). Append-only ActivityLog denorm table, hook log vào posts/comments/likes/saved create events. F2 flow execute trên 6 doc:
  - `docs/REQUIREMENTS.md`: thêm FR-13 (5 sub-FR) + UC-16 + Glossary distinguish Activity admin-scope (FR-07.5) vs user-scope (FR-13).
  - `docs/DATA_MODEL.md`: model ActivityLog (actorId + type + targetType + targetId + targetOwnerId denorm + metadata Json? + createdAt) + 2 enum (ActivityType POST_CREATED/COMMENT_CREATED/LIKE_CREATED/SAVE_CREATED + ActivityTargetType POST/COMMENT) + 2 index. Migration `add_activity_log` (v0.3.1-alpha planned).
  - `docs/API_CONTRACT.md`: `GET /users/:id/activity?page=&limit=` với JwtAuthGuard + visibility check (200 self/admin, 401 anon, 403 other, 404). Response items có `direction: 'OUTGOING' | 'INCOMING'` derived theo actor vs targetOwner.
  - `docs/UI_DESIGN.md`: Profile Activity tab expand spec — direction-aware text template + infinite scroll IntersectionObserver + truncate snippet 80 + deleted target degrade `[deleted post]` + 403 defensive fallback.
  - `docs/DESIGN_SYSTEM.md`: thêm component ProfileActivityItem (variant của ActivityLogItem admin, share base, prop `variant: 'admin' | 'profile'`).
  - `docs/TASKS.md`: M11.6 backlog 2 task — T-300 (BE migration + ActivityModule + 4 service hook + endpoint) + T-301 (FE types thủ công vì T-302 deferred + useUserActivity + ProfileActivityList + wire ProfilePage Activity tab).
  - `docs/PROGRESS.md`: thêm M11.6 row Doing, tổng milestone 12/16 (75%).
  - **F2 STOP:** docs xong, chưa implement code. Sau khi user review F2 spec → F1 BE (T-300) → openapi:sync → F1 FE (T-301).

- **F2 spec for M11.5 — Tags / Profile / Search / Create Post enhance (2026-05-18, docs only):** User update design files với 3 screen mới (Profile/Search/Tags) + Create Post emoji picker + Saved standalone route + TopBar search submit pattern. F2 (New Requirement) flow execute trên 6 doc:
  - `docs/REQUIREMENTS.md`: thêm FR-10 Tags Module / FR-11 User Profile / FR-12 Full-text Search + expand FR-02.7 emoji picker / FR-04.6 sort dropdown / FR-05.2 copy-link wire / FR-07.4 moderation queue / FR-08.4 CommandPalette nav fix. Thêm UC-13/14/15. Glossary: Skill, Heatmap. Traceability matrix thêm 3 row.
  - `docs/DATA_MODEL.md`: User.title (max 80) + User.bio (Text max 500 markdown) + User.skills (Json `{name,color}[]` max 20) + Tag.description (max 280) + Tag.createdAt. Migration `add_user_profile_fields_and_tag_description` planned (v0.3.0-alpha section).
  - `docs/API_CONTRACT.md`: thêm `POST /auth/change-password` + `GET /users/by-username/:username` + `GET /users/:id/stats` + `PATCH /users/me` body extend + `GET /tags` query params + sparkline + `DELETE /tags/:id?force=` + `GET /admin/comments` queue + `GET /search` (multi-table ILIKE) + rate limit entries.
  - `docs/UI_DESIGN.md`: Screen 7 Profile (hero + 4 tabs + sidebar + EditProfileDrawer) + Screen 8 Search (hero + filter chips + result grid + sidebar) + Screen 9 Tags (4 stat cards + toolbar + TagCard grid/list + TagModal admin) + Screen 10 Saved (reuse Feed). TopBar `hideSearch` prop + form onSubmit navigate. CommandPalette fix `n-saved`/`r-search` routes + thêm n-tags/n-profile entries.
  - `docs/DESIGN_SYSTEM.md`: 14 component mới — EmojiPicker, EditProfileDrawer, SkillChipInput, TagCard, TagModal, BigSearchInput, FilterChip, ResultCard, HeatmapGrid, ProfileAvatar, StatSparkline, TabButtons, SegmentedToggle, ConfirmDialog.
  - `docs/TASKS.md`: M11.5 backlog 17 task (4 quick wins T-200..T-203 + 3 Tags T-210/211/212 + 4 Profile T-220..T-223 + 5 Search T-230..T-234 + 1 emoji T-240).
  - `docs/PROGRESS.md`: thêm M11.5 row, tổng milestone 11/15 (73%).
  - **F2 STOP:** docs xong, chưa implement code. User pick task đầu tiên (Wave 1 quick wins hoặc Wave 2 Tags BE T-210).

### Added

- **T-114** GitHub Actions CI matrix (close M12): `.github/workflows/ci.yml` 5 jobs với pnpm cache + Node 24. (1) lint-typecheck root, (2) web-unit `pnpm --filter web test:unit -- --coverage` + upload coverage artifact, (3) api-unit `pnpm --filter api test -- --coverage` + artifact, (4) api-integration với postgres:16 service container :5433 + env stub (JWT/Cloudinary), runs `prisma migrate deploy` then `test:e2e`, (5) e2e depends 4 jobs trên — postgres service + ALLOW_TEST_RESET=1 + `playwright install --with-deps chromium` (cached `~/.cache/ms-playwright`) + `prisma migrate deploy` + `playwright test --project=chromium`, upload `playwright-report/` artifact on failure (7 days retention). Concurrency group `ci-${{ ref }}` cancel-in-progress. `tsconfig.e2e.json` cover `playwright.config.ts` + `e2e/**/*.ts` cho typecheck. Add `@types/node` ở root. Fix specs: dùng `request as pwRequest` từ `@playwright/test` cho isolated HTTP contexts thay vì `request.newContext()` trên fixture. M12 ✅ Done (5/5 task) — milestone tỉ lệ **79% (11/14)**.
- **T-113** Playwright E2E suite scaffolding (M12): cài `@playwright/test ^1.60.0` ở root. `playwright.config.ts` chromium project, baseURL từ `E2E_BASE_URL` (default localhost:5173), trace on-first-retry, retries 1 trong CI, webServer auto-start BE+FE khi `CI=1` (env `ALLOW_TEST_RESET=1`). BE bổ sung `POST /admin/test-reset` (gated env `ALLOW_TEST_RESET`, marked `@Public()` + `@Roles()` để bypass JwtAuth+RolesGuard, return 404 nếu env không bật) — truncate volatile tables + reseed `admin/admin1234` + `user/user1234`. Helpers `e2e/_helpers/{seed,auth}.ts`: `resetDb(request)` qua POST endpoint + `loginAs(page, role)` UI login + `logout(page)` dropdown menu. 13 spec files mirror E2E-01 → E2E-13 catalog. **Functional flows (8 specs):** E2E-01 Auth (register + login + wrong-password banner), E2E-02 Admin create post (mood + tag + content + Publish → /post/:id), E2E-03 Admin edit/delete via API + visual assert, E2E-04 Anon view + like + comment, E2E-05 User save persists qua /me/saved, E2E-06 Mood filter, E2E-09 Admin ban → Unban button flips, E2E-12 Command palette ⌘K nav + Esc close. **Skipped (5 specs với documented reason):** E2E-07 Share (UI placeholder), E2E-08 admin moderate delete-via-API (kept smoke but covered BE), E2E-10 Admin moderate queue (UI placeholder, BE endpoint thiếu), E2E-11 file upload Cloudinary (external dependency), E2E-13 realtime (depends M11). Scripts root: `test:e2e:playwright`, `e2e:install`. Playwright `--list` confirms 16 tests in 13 files. Verification local: `ALLOW_TEST_RESET=1 pnpm --filter api start:dev` + `pnpm --filter web dev` + `pnpm test:e2e:playwright`. Full pass-rate sẽ verify trong CI matrix (T-114).
- **T-112** FE unit test audit + mutation/service gap-fill (M12): chạy `vitest --coverage` (cài thêm `@vitest/coverage-v8@^2.1.8`). Audit: hầu hết hooks/services ≥80%. Gap: `mutations/use-like.ts` 52% / `mutations/use-save.ts` 54% / `services/api/saved.ts` 47% — optimistic update + rollback paths chưa được exercise trực tiếp. Thêm `tests/hooks/mutations/use-like-save.test.tsx` (4 cases: like optimistic patch list+detail / like 500 rollback / save optimistic / save 500 rollback) với `QueryClient` riêng `gcTime: Infinity` để assert cache state sau invalidate. Thêm `tests/services/api/saved.test.ts` (3 cases: togglePostSave POST / listSavedPosts no params / listSavedPosts query string). 7 test mới → coverage use-like 100% / use-save 100% / saved.ts 100%. Total FE unit **188 tests pass** (was 181).
- **T-111** BE integration test audit (M12): chạy `pnpm --filter api test:e2e` chống postgres-test :5433 — 10 suites, 123 tests pass (admin 9 / auth 8 / comments 19 / files 11 / likes 10 / posts 25 / saved 9 / tags 18 / throttle 2 / users 12). Audit từng module: mọi controller endpoint (28 endpoints / 9 module REST + auth) đều có ≥1 happy + 1 negative (401 no cookie / 403 forbidden role / 400 validation / 404 not found / 409 conflict tuỳ context). Spec đầy đủ — không cần gap-fill.
- **T-110** BE unit test audit + cloudinary gap-fill (M12 start): chạy `jest --coverage` cho 10 services trong `apps/api`. Audit kết quả: admin 97.91% / auth 91.13% / comments 100 / files 100 / likes 100 / posts 86.81 / saved 100 / tags 98.18 / users 100 ≥80% threshold. Gap duy nhất `cloudinary.service.ts` 20.68% → thêm `tests/files/cloudinary.service.spec.ts` mock `cloudinary` SDK (config + utils.api_sign_request + uploader.destroy): 8 cases cover constructor config, signUpload default folder + custom folder/publicId + missing API_SECRET throw, destroyMany empty/missing-secret/multi-asset success/partial-failure logger.warn path. Coverage cloudinary 100% statements + 90% branches. Total BE unit **112 tests pass** (was 104).
- **T-091** RegisterPage skeleton (close M10): rewrite từ placeholder, reuse `TerminalCard` từ T-090. Form 3 fields: username (3-32 chars, regex `[a-zA-Z0-9_-]`, hint `// alphanumeric + - _`) + password (min 8, show/hide eye) + email optional. Local validation trước khi POST → display `[ERROR] ...` red banner + shake. `useRegister` mutation POST /auth/register → setUser → navigate `/`. Error mapping: 409 or `USERNAME_TAKEN` → `username already taken`, 400 → `invalid input · check fields`, other → message từ ApiError. Submit `[ CREATE ACCOUNT ↵ ]` → loading `[ ⠋ CREATING... ]`. Link `❯ sign in` → `/auth/login`. Add route `/auth/register` lazy load trong routes.tsx. 4 unit tests (render 3 fields + invalid username local error + valid POST + setUser + navigate / + 409 banner). Total **181 FE tests pass** — M10 milestone đóng (5/5 ✅).
- **T-094** Avatar dropdown wire Logout + guest variant (M10): TopBar refactor menu items: `AUTHED_MENU` (Create Post / Admin Dashboard với `adminOnly` flag / System Settings / Profile) filter theo role, hoặc `GUEST_MENU` (Login / Register) khi `!isAuthed`. Logout giờ là `<button>` thay vì Link → fire `useLogout` mutation (POST /auth/logout → clear store + qc.clear) onSettled navigate `/auth/login` replace. Button disabled khi pending + label "Logging out...". USER role: ẩn admin-only items (Create Post + Admin Dashboard) + ẩn `[ ADMIN ]` badge. Guest: avatar `?` fallback + dropdown chỉ Login/Register. 2 new test groups: guest variant (chỉ Login+Register, no Logout) + USER non-admin (admin-only ẩn). Logout flow test (mutation called + store cleared + navigate). Total **177 FE tests pass**.
- **T-093** ProtectedRoute hydrating-aware + role gate (M10): refactor stub thành full guard. `isHydrating` → render `null` (AppLayout splash overlay đã cover, tránh flash redirect khi cookie còn nhưng store chưa hydrated). `!isAuthed` → Navigate `/auth/login?next={pathname+search encoded}` replace. `requireRole && user.role !== requireRole` → Navigate `/` replace. Extend prop `requireRole` type từ `'ADMIN' | 'USER'` → full `Role` (thêm `BANNED`). 5 unit tests (hydrating no flash + guest redirect login + authed pass-through + USER blocked from ADMIN page redirect home + ADMIN pass-through). Total **175 FE tests**.
- **T-090** LoginPage terminal card (M10): rewrite từ placeholder match design-file/MyBlog Login.html. `TerminalCard` reusable wrapper (Login + Register sẽ reuse) — header brand mark SVG `< >` + path `~/auth/login_` với cursor blink keyframe + scan-stripe absolute moving top → bottom 4s linear + shake animation 450ms khi auth fail + fadeUp mount + body slot. Inline keyframe injection one-time (dedupe by id). Form fields: username (auto-focus, ❯ icon left, JetBrains Mono 14px) + password (show/hide eye toggle). Submit `[ AUTHENTICATE ↵ ]` → loading `[ ⠋ AUTHENTICATING... ]` (AsciiSpinner reuse). Error banner red với background tint + border khi credentials sai → shake animation trigger. Divider `── or ──`. Anonymous link `Continue as anonymous →` purple → `/`. Register link `// no account? ❯ register here` → `/auth/register`. Bottom status `● connected to server · build: {sha}`. Honor `?next=<path>` query: onSuccess decoded navigate. Mock useNavigate trong tests để tránh React Router lazy + undici unhandled rejection. 5 unit tests (render form + brand + scan line / submit empty validates / 401 invalid banner + shake / valid POST + setUser + navigate / show-hide pw toggle). Update routes.test.tsx login case dùng aria-label thay placeholder text. Client retry: skip nếu `signal.aborted` (component unmount safe). Total **170 FE tests pass**.
- **T-092** Foundation auth store + 401 auto-refresh interceptor (M10 start): refactor `apiFetch` với module-level mutex `refreshPromise` — 401 trên non-auth path tự động POST /auth/refresh và retry 1 lần. NO_REFRESH_PATHS list (`/auth/{refresh,login,register}`) prevent infinite loop. Concurrent 401s share cùng 1 refresh in-flight. Refresh fail → emit `auth:logout` window event. `ApiFetchInit` extend `RequestInit` thêm `skipRefresh?` flag. Zustand store `useAuthStore` `{ user, status: idle|hydrating|authed|guest, hydrate, setUser, clear }` dedupe khi đã hydrating. `services/api/auth.ts` 5 wrappers (login/register/logout/getMe/refresh). 3 mutation hooks (use-login invalidate posts/saved, use-logout clear store + qc.clear, use-register). `useAuth` rewrite subscribe store expose `{ isAuthed, isHydrating, user }`. `main.tsx` kick off `hydrate()` trước render. `AppLayout` splash overlay `⠋ initializing session...` z-300 khi isHydrating. `tests/setup.ts` default authed admin cho backwards-compat tests; tests M10 override qua `useAuthStore.setState`. 10 unit tests: 5 store (hydrate success/401/setUser/clear/concurrent dedupe) + 5 client (happy/refresh retry/refresh fail/no-refresh auth paths/concurrent mutex). Total **165 FE tests pass**.
- **T-076** AdminPage assemble (close M9): full dashboard layout match design-file/MyBlog Admin.html. Sub-bar `~/admin/dashboard · ── · ● live mode (pulse grn) · last update · ✏️ New Post button`. 4 StatCards grid (POSTS cyan / LIKES mag / COMMENTS pur / VIEWS grn, formatViews `1.2k` cho thousands) wired qua `useAdminStats`. 2-col grid: `// mood.distribution {N} posts` MoodBar map theo MOOD_KEYS (zero-fill missing mood từ Map lookup, render đủ 7 progress bars) + `// activity.log // mock · M11` ActivityLogItem map từ ACTIVITY_LOG_MOCK. `// users.table` UsersTable. `// comments.moderation` placeholder `// pending moderation queue · wire khi BE admin cross-post endpoint sẵn` (defer — BE cần admin-wide GET /admin/comments endpoint trước). `services/api/admin.ts` getStats/getMoods/getHeatmap + `useAdminStats`/`useAdminMoods` queries. 4 unit tests (sub-bar + 4 StatCards values + 7 MoodBars zero-fill + activity mock items + UsersTable + moderation placeholder). Total **155 FE tests pass** (M9 closed 10/10 ✅).
- **T-079** UsersTable với ban/unban admin actions (M9): table view Username/Role/Joined/Email/Actions. Username `~/...` blu, Role badge color theo Role enum (ADMIN orange / USER muted / BANNED red), Joined formatRelative, Email fallback `—`. Actions: ADMIN row → `—` (không ban được admin); USER/BANNED → Ban/Unban button red/green. BANNED row dim opacity-60. `services/api/users.ts` listUsers/banUser/unbanUser. `useUsersList` query + `useToggleBan` mutation (auto-detect via banned prop) onSuccess invalidate `qk.users.all`. Extend types `Role` thêm `BANNED` + `AdminUser` + `PaginatedUsers`. Query keys extend `qk.users.*` + `qk.admin.{stats,moods,heatmap}`. 4 unit tests (render 2 users với role badges + Ban click POST /users/:id/ban + ADMIN role hiển thị `—` không ban + empty state). Total **151 FE tests**.
- **T-078** MoodBar + ActivityLogItem + mock data (M9): `MoodBar` single row emoji + label + count/% + horizontal progress bar gradient (90deg color80 → color) + glow shadow. `role="progressbar" aria-valuenow={pct}` cho a11y; safe-guard total=0 → 0%. `ActivityLogItem` row: colored icon + truncated message + relative time. `mocks/activity-log.ts` ACTIVITY_LOG_MOCK 6 entries (like/comment/save/anon session/etc) — note `// TODO wire to /admin/activity sau M11 (T-042 deferred)`. 6 unit tests (3 MoodBar emoji+label+count+pct/progressbar aria/total=0 safe + 3 ActivityLogItem render+mock 6+ entries+unique ids). Total **147 FE tests**.
- **T-077** StatCard + Sparkline SVG (M9 Phase B start): `Sparkline` reusable SVG polyline với linearGradient stroke (20% → 100% opacity) + endpoint circle drop-shadow glow theo `color` prop. Calculates min/max range với padding y; null khi data empty. `StatCard`: border-left 3px accent color, mono uppercase label tracking 0.08em, Space-Grotesk bold value, `+N/±0/-N today` delta theo formatDelta helper, inline Sparkline. Match design-file/MyBlog Admin.html L143-149. 7 unit tests (3 Sparkline empty/svg/aria + 4 StatCard label-value/positive delta/zero ±0/sparkline render). Total **141 FE tests**.
- **T-070** CreatePostPage assemble (close M9 Phase A): split layout `max-w-[1400px]` editor flex-1 + preview 380px (hidden < 900px via `[@media(min-width:900px)]:block` arbitrary). Sub-toolbar `~/admin/create-post · ── · status · ⌘S Save · ⌘↵ Publish`. Sections labelled `.sb-lbl` utility: `// mood` (MoodPicker), `// content` (MarkdownEditor), `// images (N/10)` (UploadZone variant=image folder=myblog/posts), `// files (N/20)` (UploadZone variant=file folder=myblog/files), `// tags` (TagInput). Live preview right column với MoodBadge + PostContent + ImgSlot grid + TagPill list. `useCreatePost` mutation POST /posts → invalidate `qk.posts.all` → setTimeout(navigate /post/:id, 800ms) cho UX brief success state. `deriveFileType(name)` helper extract extension match BE FileType enum. Keyboard shortcuts: ⌘S local save (defer BE draft) + ⌘↵ publish. Status machine: draft·unsaved (yellow) → draft·saved (green) → publishing (yellow ⠋) → published (green ✓) / error (red banner). 4 unit tests (render 5 sections + preview + Publish disabled empty + POST body shape + Save flips status). Total **134 FE tests pass** (M9 Phase A đóng: 6/10 tasks).
- **T-075** PostPreview live mini-card (M9): match design-file/MyBlog Create Post.html:75-132. Mock author (~/admin) + ADMIN badge + just-now timestamp + MoodBadge selected. PostContent reuse với variant=card; truncate content > 300 chars + `...` suffix. Empty content → italic placeholder. ImgSlot grid mini (max 3 cells) khi imageCount > 0. TagPill list khi tags > 0. Actions row 4 placeholders disabled. Cyan border + glow surface. 5 unit tests. Total **130 FE tests**.
- **T-074** TagInput với chip add/remove + color cycle (M9): input field bên trong border-container + chip list flex-wrap. Keydown Enter/comma/space → addTag (normalize: lowercase + strip `#` prefix + dedupe + max 10). Backspace khi input empty → remove last chip. `lib/tag-colors.ts` TAG_COLORS palette 7 cyberpunk (mirror BE TAG_COLORS) + `pickTagColor(index)` cycle + `normalizeTagName(raw)`. Reuse `TagPill` cho chip render. Remove chip → re-assign colors theo index (giữ palette consistent). Counter `Press Enter, comma or space · N/10`. 6 unit tests (Enter add + comma add + space add + dedup + strip # + × remove). Total **125 FE tests**.
- **T-073** UploadZone với Cloudinary direct upload (M9): 2-step flow — POST /files/sign → BE trả signed params (signature/timestamp/apiKey/cloudName/folder/resourceType) → FE upload binary FormData tới `https://api.cloudinary.com/v1_1/{cloudName}/{image|raw}/upload` → trả `{ public_id, secure_url, width, height, bytes }`. `services/api/files.ts`: signUpload + uploadToCloudinary. `hooks/mutations/use-upload.ts`: useUploadFile(resourceType) chain mutationFn. UploadZone component: dashed border drop zone với click + drag-drop + onDragOver highlight + accept types + maxCount cap. Pending state per-file với `URL.createObjectURL` preview cho images. ImageThumb 60px square + × remove. FileItem row với type badge (reuse FILE_CFG) + name ellipsis + size + × remove. Variant: image (flex-wrap thumbnails) vs file (vertical list). Setup polyfill `URL.createObjectURL` trong jsdom. 5 unit tests (render slots remaining + upload flow Cloudinary → thumbnail + maxCount cap + remove × + file variant render). Total **119 FE tests**.
- **T-072** MarkdownEditor (M9): textarea + 5 toolbar buttons (B/I/`code`/`# h`/link) insert markdown syntax wrap quanh selection hoặc tại cursor. `lib/insert-at-cursor.ts` pure `wrapSelection(content, start, end, before, after)` (testable). Selection preserved post-update qua `queueMicrotask` + setSelectionRange. Char counter footer `// markdown supported · N chars`. Min-height 280px resize-y. Focus → cyan border + glow. 8 unit tests: 4 `wrapSelection` (bold wrap / cursor insert / asymm heading / link url) + 4 MarkdownEditor (render 5 buttons + typing onChange + click Bold wrap selection + click Heading prefix). Total **114 FE tests**.
- **T-071** MoodPicker (M9 start): 7 emoji radio buttons grid (HAPPY/EXCITED/THOUGHTFUL/CALM/SAD/GRATEFUL/ANGRY) match `MOOD_KEYS`. Active mood: border + bg tint + glow shadow theo `MOOD_CFG[mood].color`. `role="radiogroup"` + `role="radio" aria-checked` cho a11y. Props `{ value, onChange }`. Reuse `lib/mood-config.ts`. 3 unit tests (render 7 + active aria-checked + click onChange). Total **106 FE tests**.
- **T-069** CommentItem + CommentList (close M8): match design-file/MyBlog Post Detail.html CommentItem section. Header Avatar size=sm + display name (`@username` cho authed / `Anon · {name}` cho anonymous / `Anon` plain fallback) + relative time. Body whitespace-pre-wrap content. Footer: like button (♡ count, optimistic flip local state pattern giống LikeButton, useEffect sync khi prop đổi) + Reply button disabled (defer reply thread feature). `useToggleCommentLike` mutation simple (no cache touching — local state đủ). `CommentList`: `usePostComments(postId)` query → loading AsciiSpinner / error message / empty state `// no comments yet — be the first ❯` / map render CommentItem. Wire CommentForm + CommentList vào PostDetailPage replace placeholder section. 7 new tests (4 CommentItem render+anon name+like optimistic+reply disabled + 3 CommentList empty/2 items/error). Total **103 FE tests pass**. M8 ✅ Done (10/10 tasks, Phase A + Phase B complete).
- **T-068** CommentForm + post-as-anon toggle (M8): textarea `// add a comment...` (1000 char max + resize-y) + footer row `as: ~/username | [as anon] toggle | ↵ Send`. Authed user default render `~/username` label; click `[as anon]` → swap sang anon name input (30 char max). Anonymous user → anon name input mandatory. Submit disabled khi content empty / anon name empty (khi as-anon) / pending. POST /posts/:postId/comments với body `{ content, anonymousName? }`. `useCreateComment` mutation optimistic prepend tempId vào `qk.comments.list` cache + rollback onError + invalidate comments + post detail (cập nhật count). onSuccess clear textarea. Error banner `// failed to post comment — try again`. 6 unit tests (render authed default + send disabled empty + toggle anon flow + submit clears + as-anon body kèm anonymousName + error message).
- **T-067** ImageCarousel full (M8): replace T-066 stub. 280px height container `role="region" aria-roledescription="carousel"` tabIndex=0 focus-visible cyan border. Single image: render `<img>` không có nav. Multi (>1): prev/next round buttons absolute left/right + dot indicator (role="tab" aria-selected) + counter `N/M` bottom-center pill. Wrap-around modulo (prev tại idx 0 → last). Keyboard ArrowLeft/ArrowRight khi region focused. onError fallback ImgSlot per-image (broken state map). Reset idx + broken state khi images prop thay đổi. 8 unit tests (empty/1 image no nav/4 images full controls/next advance/prev wrap/keyboard arrow/dot jump/onError fallback). Total **90 FE tests pass**.
- **T-066** PostDetailPage (M8 Phase B start): rewrite từ placeholder thành full detail layout. `usePost(id)` via useQuery; breadcrumb `← feed / ~/post/{shortId}`; 2-col flex layout `max-w-[1100px]` (main flex-1 + MetaPanel 280px hidden < lg). Main: PostHeader avatarSize=lg + PostContent variant=detail + ImageCarousel stub + FileAttachments + tags row + actions (LikeButton + comment count + SaveButton + Share + view count). MetaPanel right: 4 sections `// post.meta` (ID/Views/Likes/Comments dl) + `// tags` TagPill flex-wrap + `// share` 4 buttons placeholder (FB/X/TG/Copy) + `// related` placeholder M11. `useTrackView(postId)` hook fire POST /posts/:id/view 1 lần per mount qua useRef debounce (BE dedup 30min by userId/anonymousId). 404 handling: `ApiError.status === 404` → `// post not found` + back to feed link. Loading state inline. CommentForm/CommentList placeholder cho T-068/T-069. ImageCarousel stub fallback về ImageGrid (T-067 sẽ implement đầy đủ). 3 unit tests pass (render full + 404 fallback + view tracking fired) + update routes.test.tsx PostDetail case dùng MSW 404. Total **82 FE tests pass**.
- **T-061** PostCard full implementation (M8, close Phase A): match design-file/MyBlog Feed.html:186-269. Container rounded-lg bg-surf border-b2 với hover: cyan border + glow shadow + top gradient line (`::before` simulate qua span absolute opacity transition). Corner deco `#{shortId}` text-mono-xs td. Sections: `<PostHeader>` (Avatar + ~/username + ADMIN badge + [YYYY-MM-DD HH:MM] + pulse + relative time + MoodBadge) → `<PostContent variant="card">` → `<ImageGrid>` → `<FileAttachments>` → tags row (TagPill flex-wrap gap-1.5) → divider `────` → actions (LikeButton + comment link navigate /post/:id + SaveButton + Share placeholder + overflow ⋯). `LikeButton`/`SaveButton` local optimistic state với `useEffect` sync khi parent prop thay đổi (cho phép standalone test + propagation từ cache invalidate). `lib/format-date.ts`: `formatTimestamp(iso)` → `[YYYY-MM-DD HH:MM]` + `formatRelative(iso, now)` → `just now / Xs / Xm / Xh / Xd / Xmo / Xy ago`. 11 new tests: 5 PostCard (render + click like optimistic flip + click save optimistic + comment link + ImageGrid+FileAttachments embedded) + 6 format-date helpers. Total **79 FE tests pass**.
- **T-060** FeedPage + foundation API infra (M8): build complete TanStack Query stack — typed wrappers `services/api/{posts,comments,likes,saved}.ts`, query keys factory `lib/query-keys.ts`, types mirror BE DTOs trong `types/api.ts` (Post/Comment/PaginatedPosts/etc + Role/CommentStatus enums), hooks `queries/{use-posts,use-comments}.ts` (usePostsInfinite + usePost + usePostComments), mutations `mutations/{use-like,use-save}.ts` (optimistic update infinite list + detail cache, rollback onError, invalidate onSettled). FeedPage `mx-auto max-w-[820px]` với FilterBar (mood pills All/HAPPY/EXCITED/THOUGHTFUL/CALM/SAD + Latest sort placeholder) + PostList (useInfiniteQuery + IntersectionObserver sentinel rootMargin 120px + AsciiSpinner braille 10-frame). Loading skeleton + empty state ◐ + error state. PostCard stub minimal (T-061 sẽ expand đầy đủ). Install dep `msw ^2.6`. Test infrastructure: `tests/_helpers/{msw-server,test-providers,query-client,post-factory}.ts` + setup.ts wire beforeAll/afterEach/afterAll MSW lifecycle. vitest.config env block cho VITE_API_URL/WS_URL. routes.test.tsx update wrap QueryClientProvider + default MSW empty list handler. 8 new tests (4 FilterBar + 4 HomePage MSW: render posts / empty / mood filter refetch / 500 error). Total **68 FE tests pass**.
- **T-064** FileAttachments + FILE_CFG (M8): port từ design-file/myblog-components.jsx:339-371. Header `// attachments [N]` + rows: type badge (color theo FILE_CFG: PDF red, DOC/DOCX blue, XLS/XLSX green, TXT muted, CSV yellow) + filename ellipsis + size formatted + download link (`<a download href={file.url} target="_blank" rel="noopener">`). `lib/file-config.ts`: `FILE_CFG` 7 types match BE Prisma enum + `getFileConfig(type)` case-insensitive fallback + `formatBytes(n)` helper (B/KB/MB với 1 decimal khi MB < 10). 9 unit tests (4 formatBytes + 4 getFileConfig + 2 component empty/render).
- **T-063** ImageGrid + ImgSlot fallback (M8): responsive grid layout 1/2/3+ images match design-file/myblog-components.jsx:161-188. 1 → single 200px; 2 → 2-col 160px; 3+ → 2-col left-big + right stacked; `+N` overlay trên cell cuối nếu count > 4. `<ImageCell>` wraps `<img>` với `onError` swap sang `<ImgSlot>` fallback. `ImgSlot` cyberpunk placeholder (striped diagonal bg + ⬡ glyph + `photo.NN` label, 4 color variants cycle theo idx). `loading="lazy"`. 7 unit tests (empty/1/2/3/5 layouts + image url thiếu + onError fallback).
- **T-062** PostContent markdown renderer (M8): port custom parser từ design-file/MyBlog Feed.html:161-184 — chỉ split `fenced code blocks` + paragraph (`\n\n` boundary). Pure `parsePostContent(content)` trong `lib/markdown.ts` (separation for fast-refresh compat); strip optional language hint `^[a-zA-Z]+\n`. `<PostContent>` component: code blocks render `<pre>` với cyberpunk theme (bg #070A14 + border-l green + green text monospace); paragraphs `<p>` mb-2. Props `variant: 'card' | 'detail'` adjust font-size 14px/15px + line-height. Zero deps. 7 unit tests (4 parser + 3 component variants).
- **T-065** Shared primitives MoodBadge + TagPill + Avatar (M8 start): port từ design-file/myblog-components.jsx. MoodBadge inline-flex pill emoji+label với background/border/glow theo MOOD_CFG (7 moods). TagPill chip với hover shift bg + glow shadow; auto `#` prefix nếu thiếu; optional onClick → role=button. Avatar circle 3 sizes (sm 28px / md 36px / lg 52px), gradient cyan→purple bg + cyan border + initial uppercase fallback hoặc `<img src={avatarUrl}>`, optional online dot bottom-right. `lib/mood-config.ts` MOOD_CFG full 7 moods + MOOD_KEYS readonly array. 11 unit tests pass.
- **T-052** CommandPalette ⌘K (close M7): createPortal overlay match design-file/MyBlog Feed.html:302-379. Backdrop `bg-black/70 backdrop-blur-md`, surface 560px max-w (90vw) top 100px, cyan border + glow shadow. Input `~$ type a command...` JetBrains Mono + Esc button right. 8 commands registry trong `commands.ts` (3 groups recent/navigate/actions): Go to feed / Search posts / Create new post / ~/feed / ~/saved / ~/admin / Toggle theme / Logout. Realtime filter substring match trên `label` + `desc`. Empty state `// no results for "<q>"`. Keyboard nav: ↑↓ select (clamp), ↵ navigate (useNavigate) + onClose, Esc close, click backdrop close. Mouse enter row → select (sync với keyboard). Items render `role="option" aria-selected`. Zustand store `use-command-palette.ts` `{ open, setOpen, toggle }`. Global ⌘K/Ctrl+K listener installed trong AppLayout (toggle). FR-08 implemented. 9 unit tests (null khi closed + render groups + filter + empty + Esc key + Esc button + Enter navigate + ArrowDown selection + click navigate). M7 đóng — **26 FE tests pass** (5 routes + 5 TopBar + 5 StatusBar + 9 CP).
- **T-051** StatusBar (M7): fixed-bottom 28px terminal-style match design-file/MyBlog Feed.html:461-480. `bg-[#070A14]` + `border-t border-b1`, JetBrains Mono 11px. Props `path` (default `~/feed`, cyan highlight bg), `info?` conditional, `online?` (default 3, green pulse dot). Sections cách `border-r border-b1`: path | info | `──────` (td color) | `build: {sha}` | spacer | `● N online` | `[ v0.1.0 ]`. Build hash đọc từ `VITE_BUILD_SHA.slice(0,6)` fallback `'a1b2c3'`. Wire vào AppLayout với `pathLabel(pathname)` helper map URL → terminal path (e.g. `/admin/create` → `~/admin/create-post`, `/post/abc123` → `~/post/abc123`). 5 unit tests (default render + custom path + conditional info + custom online + build hash).
- **T-050** TopBar (M7): fixed-top 52px shell match design-file/MyBlog Feed.html:382-458. Logo SVG `< >` brackets cyan+purple polylines (strokeWidth 2.2) + text "kha.blog" Space Grotesk 700 với `.cyan` separator + `.blog` muted 500, glitch animation 9s (text-shadow chroma split). Centered search input 440px max-w (hidden < md breakpoint), JetBrains Mono 13px, focus → cyan border + glow shadow, `⌕` left icon + `⌘K` right hint button. Right cluster: `[ v0.1.0 ]` badge + `● 3` online (green pulse) + Avatar 32px circle (gradient cyan→purple bg, online dot bottom-right). Avatar dropdown 5 menu items (Create Post cyan ⌘N / Admin Dashboard purple ⌘3 / System Settings yellow / Profile separator / Logout red ⌘Q), fade-up animation + click-outside close. `Logo.tsx` extracted reusable cho Login. AppLayout wire TopBar slot + stub `onOpenCommandPalette` (TODO T-052). useAuth stub providing admin role để dropdown hiển thị `[ ADMIN ]` badge. 5 unit tests pass (render + ⌘K callback + dropdown open + click-outside close + username display).
- **T-055** App router + layout shells (M7): rewrite `routes.tsx` với 6 routes (/, /post/:id, /admin, /admin/create, /auth/login, \*) + lazy load + 2 layouts (AppLayout shell với TopBar/StatusBar slot placeholders cho T-050/T-051 fill, AuthLayout minimal cho Login). ProtectedRoute component check role + Navigate redirect (`requireRole` prop, query `?next=` preserve). `useAuth` hook stub hardcode admin (wire thật M10 với Zustand + cookie session). 5 placeholder pages (PostDetail/Admin/CreatePost/Login + HomePage refactor). 7 integration tests pass (routes.test.tsx — memory router navigate qua mọi path + verify AppLayout vs AuthLayout shell). Xóa `App.tsx` (replaced by AppLayout).
- **T-056** Design tokens alignment với design-file specs (M7 start): fix `glitch` keyframe → text-shadow chroma split (cyan/mag/purple/orange) + skewX transform match Feed.html:55-60 (thay vì translate đơn giản); thêm `scan-line` keyframe 6s linear infinite cho CRT vertical scan effect; bổ sung `::-webkit-scrollbar` rules (5px width, b2 thumb, bg track); thêm utility class `.sb-lbl` (section label `// label` + flex-1 hr line) trong `@layer components` cho aside/panel sections.
- **T-002** Monorepo skeleton: `package.json` + `pnpm-workspace.yaml` + `turbo.json` + `.npmrc` + `.nvmrc` + `packages/`. Turborepo 2.9.14, pnpm 9.15.0, Node 24 LTS. Pipeline tasks build/dev/lint/test/typecheck.
- **T-003** `docker-compose.yml`: 2x Postgres 16-alpine (`postgres-main` :5432 persistent volume, `postgres-test` :5433 tmpfs in-memory) + healthcheck.
- **T-004** Scaffold `apps/api` NestJS skeleton: bootstrap (helmet/compression/cookie-parser/CORS/ValidationPipe/Swagger dev), `common/` (HttpExceptionFilter + Transform/Logging interceptors), `config/env.schema.ts` (Zod fail-fast validate), `prisma/` (nestjs-prisma + Placeholder model). NestJS 10.4, Prisma 5.22, @nestjs/swagger 7.4, helmet 8.
- **T-005** Scaffold `apps/web` Vite + React 19 skeleton: RouterProvider (RR v7 + lazy + Suspense) + QueryClient (TanStack Query 5 + DevTools dev-only) + Tailwind 3.4 (cyberpunk tokens) + shadcn/ui init + Zod env validation + fetch client wrapper + Vitest smoke test.
- **T-006** ESLint 9 flat config + Prettier 3 + Husky 9 + lint-staged + commitlint. Root `eslint.config.mjs` shared base (no-console, no-explicit-any, typescript-eslint) + per-app extends (NestJS, React hooks+refresh). Pre-commit: lint-staged auto-fix + format. Commit-msg: commitlint enforce Conventional Commits. Format baseline áp dụng 28 files.
- `.vscode/` workspace config: `extensions.json` recommend 14 extensions (ESLint, Prettier, Tailwind CSS, Prisma, Vitest, pretty-ts-errors, dotenv, cSpell EN+VI, Docker, YAML, TOML, GitHub PR); `settings.json` (format-on-save + ESLint flat config + Tailwind cva/cn regex + Prisma formatter + file/search excludes + cSpell custom words). `.editorconfig` cross-editor (LF, 2 spaces, max 100). Update `.gitignore` allow track `.vscode/{extensions,settings,launch}.json`.
- **T-007** đóng task: env validation đã có qua Zod (BE `config/env.schema.ts` T-004 + FE `lib/env.ts` T-005). `dotenv-safe` defer permanent — Zod cover cả 2 use case (existence + type/format).
- **M2 milestone hoàn tất** (T-002 → T-007): monorepo scaffold + Docker compose + NestJS skeleton + Vite skeleton + ESLint/Prettier/husky + env Zod validation.
- **T-010** Prisma schema initial migration `20260517165932_init`: 14 entities (User/Post/Image/File/Comment/Like/CommentLike/Tag/PostTag/SavedPost/PostView/AnonymousSession/RefreshToken) + 4 enums (Role/Mood/FileType/CommentStatus). Composite unique constraints (Like/CommentLike per user+post), composite PK (PostTag/SavedPost), indexes feed sort + post view dedup. Add `dotenv-cli` cho `pnpm --filter api prisma:migrate` auto-load `.env.local`. Docker postgres-main port `:5432` → `:5434` (tránh conflict local postgres). M3 started.
- **T-011** Seed scripts: `prisma/seed.ts` (admin upsert + 2 tags + 3 sample posts khác mood + 1 anon comment, idempotent) + `prisma/seed-test.ts` (admin only). Scripts `db:seed` + `db:seed:test` qua dotenv-cli. Deps: `bcrypt 5.1`, `tsx 4.19`. ESLint allow `no-console` cho `prisma/seed*.ts` + `scripts/**` (CLI output).
- **T-012 + T-013** AuthModule full feature (gộp): JwtModule + PassportModule + AuthService (bcrypt + JWT issue/verify + refresh rotation với reuse detection family revoke) + 2 strategies (Jwt + JwtRefresh từ cookie) + 2 guards + DTOs (Register/Login/AuthUser) + Controller 5 endpoints (POST /auth/register|login|refresh|logout, GET /auth/me) + cookie httpOnly setup (sameSite lax dev / none+secure prod). RefreshToken DB hash SHA-256 + tid trong JWT payload. Deps: @nestjs/jwt 10.2, @nestjs/passport 10.0, passport 0.7, passport-jwt 4.0. Smoke test 10 cases pass.
- **T-014 + T-015** UsersModule + common infrastructure (gộp): 4 decorators (@Public/@Roles/@CurrentUser/@AnonymousId) + RolesGuard + AnonymousIdMiddleware (`anon_id` cookie hex `0x7F4A2C` 1yr TTL) + JwtAuthGuard refactor (Reflector-aware @Public skip). UsersService + Controller 5 endpoints: GET /users admin pagination, GET /users/:id (email ẩn cho non-admin/non-self), PATCH /users/:id (admin or self), POST /users/:id/ban (admin, revoke refresh tokens, không ban self/admin), POST /users/:id/unban. Smoke test 10 cases pass (ban kick session ✓).
- **Test infra + retroactive M3 tests** (T-012/13/14/15): BE test pyramid Jest unit (mocked Prisma) + Supertest integration (real postgres-test :5433). Helpers: `tests/_helpers/{test-app,db-reset,factory,auth}.ts` + `jest-global-{setup,teardown}.ts` (migrate + seed test-admin). 47 tests pass (27 unit AuthService/UsersService + 20 integration auth.e2e/users.e2e). Env simplify: rename `apps/api/.env.local` → `apps/api/.env` + new `apps/api/.env.test`. Scripts: `test:e2e` + `test:e2e:migrate` (dotenv-cli wrap .env.test).
- **T-043** Rate limiting (@nestjs/throttler): Global 100/60s/IP + per-endpoint @Throttle 10/min cho POST /auth/register, /auth/login, /posts/:id/comments, /posts/:id/like, /comments/:id/like (NFR-04). skipIf flag `THROTTLE_DISABLED=1` (set trong .env.test) để existing e2e không bị burst-fail. Map ThrottlerException → `RATE_LIMITED` 429 trong HttpExceptionFilter. 2 integration tests (burst verify 429 + global limit không throttle GET browse).
- **T-040** AdminModule (M6 start): 3 endpoints admin-only — GET /admin/stats (4 metrics + 12-day sparkline + deltaToday), GET /admin/moods (zero-filled 7 moods), GET /admin/heatmap (28-day post creation). Helper `bucketByDay` UTC-based zero-fill. Drop overlap với T-014 Users (skip /admin/users + /admin/users/:id/ban), defer /admin/comments/pending + /admin/visitors. 6 unit + 9 integration tests.
- **T-032** SavedModule bookmark (close M5): 2 endpoints auth-only — POST /posts/:id/save toggle, GET /me/saved paginated (sort savedAt DESC). Composite key (userId, postId). Reuse `toPostView` + `POST_INCLUDE` từ PostsService (export thêm 2 symbols). 6 unit + 9 integration tests.
- **T-031** CommentsModule + admin moderation: 4 endpoints (GET role-aware public APPROVED/admin all status, POST optional auth với anonymousName, DELETE admin cascade CommentLike, PATCH /:id/status admin APPROVED|REJECTED). Status default APPROVED. Single controller `@Controller()` no base + per-method guards mix optional/admin (pattern reuse từ LikesController). 15 unit + 19 integration tests.
- **T-030** LikesModule (M5 start): 2 endpoints POST /posts/:id/like + POST /comments/:id/like với JwtOptionalAuthGuard (reuse từ T-021). Identity prefer userId (auth) hoặc anonymousId (anon cookie). Toggle idempotent qua unique constraint Like (postId, userId|anonymousId) + CommentLike. Comment likes chỉ APPROVED, PENDING/REJECTED → 404. Response `{ liked, count }`. 11 unit + 10 integration tests.
- **T-023** TagsModule CRUD + color rotation (close M4): 4 endpoints (GET public top N + POST/PATCH/DELETE admin). `TAG_COLORS` palette 7 cyberpunk colors (DESIGN_SYSTEM.md), cycle theo `tag.count() % 7` khi auto-create. Refactor PostsService inline `tag.upsert` → delegate sang `TagsService.upsertMany(names, tx?)` áp dụng color nhất quán + transaction-aware. 16 unit + 18 integration tests (CRUD + color cycle wrap-around + cascade PostTag + auto-create via Posts).
- **T-022** FilesModule — Cloudinary signed upload + delete: 2 endpoints (POST /files/sign + DELETE /files/:id, admin-only). Dep `cloudinary ^2.10` SDK. `CloudinaryService` wrapper (sign + `destroyMany` best-effort). **Cascade Cloudinary cleanup** hook vào `PostsService.remove()` + `update()` (replace images/files) — fix image/file leak khi T-020 hard delete cascade DB rows. `createTestApp()` override CloudinaryService provider với mock cho integration tests. 3 unit + 9 integration tests.
- **T-021** View tracking POST /posts/:id/view: optional auth via new `JwtOptionalAuthGuard` (reusable cho likes/comments sau). Dedup 30 phút theo userId (auth) hoặc anonymousId (anon, cookie từ AnonymousIdMiddleware). PostView record + Post.viewCount increment trong `$transaction`. Response `{ viewCount, counted }`. 5 unit + 5 integration tests.
- **T-020** PostsModule CRUD (M4 start): 5 endpoints (GET /posts public paginated + mood/tag filter, GET /posts/:id public, POST /posts admin, PATCH /posts/:id admin, DELETE /posts/:id admin hard-cascade). DTOs CreatePostDto/UpdatePostDto (PartialType từ @nestjs/swagger)/ListPostsDto/PostResponseDto + nested ImageInputDto/FileInputDto. Service auto-upsert Tag (lowercase + strip `#` prefix); `$transaction` cho create/update (atomic tag/image/file replace). 14 unit (mock Prisma) + 20 integration (Supertest auth 401/403, validation 400, 404, cascade verify). View tracking defer T-021; Cloudinary signing defer T-022; Tag color rotation defer T-023.
- **CLAUDE.md enforcement**: F1 step 5 strengthened (BẮT BUỘC trước commit, smoke KHÔNG đủ) + Pre-flight Checklist 2 items mới (test paths + test DB infra) + Do NOT bullet cấm commit F1/F2 thiếu test.
- Init git repository (default branch `main`) + `.gitignore` (Node + Turborepo + Vite + NestJS + env secret + IDE + OS). Trunk-based workflow chính thức bắt đầu.

### Changed

- **UI design refactor 2026-05-18** — Global Sidebar (admin nav 220px) và RightPanel (mood.distribution + activity.heatmap + live.visitors) removed khỏi shared layout shell. Feed page giờ centered max-width 820px, không còn 3-col. Aside content di chuyển sang per-page: Admin Dashboard nhận mood.distribution + activity.log, PostDetail nhận post.meta/tags/share/related, CreatePost nhận live.preview. M7 scope co từ 7 → 5 tasks: T-053 Sidebar và T-054 RightPanel DROPPED. Updated `docs/UI_DESIGN.md` (Feed layout + Components + Responsive + Real-time sections), `docs/DESIGN_SYSTEM.md` (Sidebar + RightPanel sections collapsed to DROPPED note; primitives MoodBar / Activity Heatmap / Anonymous Visitor Card vẫn giữ cho consumer pages), `docs/ARCHITECTURE.md` (FE folder structure), `docs/TASKS.md` (T-053/T-054 status + M7 scope note).
- **M6 milestone closed partial (2026-05-18)** — T-040 AdminModule + T-043 Rate limiting hoàn tất; T-041 RealtimeGateway Socket.io + T-042 Activity log persist deferred vào backlog (realtime feature có thể làm hoặc skip tuỳ scope sau). Tỉ lệ tổng: 5/14 → 6/14 milestones (43%).
- Add 2 convention rules vào CODING_CONVENTION.md: (1) cấm string literal union làm enum ảo — define Prisma `enum` (BE) hoặc `as const` + `z.nativeEnum` (FE); (2) cấm `console.*` — dùng NestJS `Logger` (BE) hoặc `loglevel` qua `@/lib/logger` (FE). Refactor `apps/api/src/main.ts` + `apps/web/src/lib/env.ts` xóa console. Cross-ref CLAUDE.md Do NOT + Pre-flight Checklist.
- **Convention change**: test files PHẢI tách khỏi src — chuyển sang `apps/<app>/tests/` (mirror src structure). Move `apps/web/src/App.test.tsx` → `apps/web/tests/App.test.tsx`, `apps/web/src/test/setup.ts` → `apps/web/tests/setup.ts`, `apps/api/test/` → `apps/api/tests/`. Update vitest + jest configs + tsconfig include + e2e config rootDir. Helpers/factories/setup vào `tests/_helpers/`. Imports test → source dùng alias `@/*` (KHÔNG relative). Update CODING_CONVENTION.md + TESTING_STRATEGY.md.
- Split root `.env.example` thành `apps/api/.env.example` (BE vars) + `apps/web/.env.example` (FE `VITE_*` vars). Root file đã xóa — per-app convention chuẩn Turborepo, dễ maintain khi scaffold M2. Update `README.md` + `docs/DEPLOYMENT.md` Quick Start bỏ dòng `cp .env.example .env.local` root.

---

## [0.2.0-alpha] - 2026-05-17

### BREAKING — Migration v1 → v2 (docs + stack)

Major restructure: tech stack chuyển từ Next.js full-stack → monorepo Turborepo (Vite React FE + NestJS BE) + design overhaul cyberpunk theme + SDD docs theo new framework. **Codebase v1 chưa init**, nên không có breaking deps update — chỉ docs refactor + design retarget.

### Changed

- **Tech stack:** Next.js full-stack → Monorepo Turborepo
  - FE: `Vite + React 19 + React Router v7 + TanStack Query + Zustand` (thay Next.js App Router)
  - BE: `NestJS + Passport JWT + bcrypt + class-validator` (thay Next.js API routes + NextAuth)
  - DB: PostgreSQL via `Prisma` qua `nestjs-prisma` (giữ ORM)
  - Storage: Cloudinary signed upload (giữ)
  - Real-time: `WebSocket via @nestjs/websockets + Socket.io` (thay SSE)
  - API contract: OpenAPI 3.0 auto-gen từ `@nestjs/swagger` (thay narrative markdown)
  - Testing: `Vitest (FE) + Jest (BE) + Supertest + Playwright` (thay Vitest + Playwright)
  - Deploy: Vercel (FE static SPA) + Fly.io free tier (BE) + Neon free tier (DB)
  - Local dev: Docker Compose 2 Postgres (main + test)
- **Docs structure** restructure theo SDD principles:
  - REQUIREMENTS.md: thêm Vision + Personas (P1/P2/P3) + Glossary + Use Cases (UC-01→UC-12) + Acceptance Criteria (Given/When/Then) + Traceability mini-matrix
  - ARCHITECTURE.md: C4 diagrams (Context/Container/Component) + 8 ADRs + Security policy + Operations runbook
  - DATA_MODEL.md (rename từ DATABASE_SCHEMA.md): Prisma snippet đầy đủ + migration log summary strategy (chi tiết → `apps/api/docs/MIGRATIONS.md`)
  - API_CONTRACT.md (rename từ API.md): narrative + error catalog + WebSocket events catalog + link tới `contracts/openapi.yaml`
  - UI_DESIGN.md: 5 screens chi tiết theo design source cyberpunk (Feed/Post Detail/Create Post/Admin/Login) + Shared Layout
  - DESIGN_SYSTEM.md: dark-only cyberpunk theme rewrite hoàn toàn — tokens (color/typo/spacing/radius/shadow/motion) + ~25 component primitives + Mood/File color maps + Token versioning
  - CODING_CONVENTION.md: split Universal + Frontend + Backend sections + Security & Performance checklists
  - DEPLOYMENT.md: Vercel + Fly.io + Neon + Docker local + env matrix
  - INDEX.md: navigation updated cho 14 file mới + Doc Update Trigger table
  - PROGRESS.md, TASKS.md, BUGS.md: update template với `Affected layer` field + flow mapping
- **Design system overhaul:** Pastel notebook → Cyberpunk / terminal dark theme (per `design-file/`)
  - Color: deep navy `#0A0E1A` background + cyan `#00FFE5` accent + 8 accent colors
  - Typography: Space Grotesk (brand/heading) + Inter (body) + JetBrains Mono (terminal/code) — thay Plus Jakarta Sans
  - Motion: glitch (9s logo), pulse (online dot), scan line (Login card), CRT scanline overlay
  - Mood emoji EXCITED: 🎉 → ⚡; mood colors update theo design source
  - Light mode removed (TBD)

### Added

- **FR-06: File Attachments** — Admin upload PDF/DOC/DOCX/XLS/XLSX/TXT/CSV (max 20/post, ≤ 20MB) qua Cloudinary signed URL. Hiển thị + download trong PostCard/Post Detail.
- **FR-07: Admin Dashboard** — 4 stat cards với sparkline + mood distribution + users table (ban/view) + comments moderation (approve/delete) + activity log real-time.
- **FR-08: Command Palette** — ⌘K overlay global navigation/actions với filter + keyboard nav.
- **FR-09: Real-time** — WebSocket activity log feed + live visitors panel + online count + comment hot-reload + 28-day activity heatmap.
- Extended: FR-02.5 (file upload), FR-03.5 (comment likes), FR-04.5 (view tracking), FR-04.6 (post views), FR-04.7 (anonymous naming format).
- **8 ADRs** (ARCHITECTURE.md):
  - ADR-001: Monorepo Turborepo
  - ADR-002: NestJS cho backend
  - ADR-003: React Router v7
  - ADR-004: WebSocket via Socket.io
  - ADR-005: Prisma ORM (giữ)
  - ADR-006: JWT trong httpOnly cookie
  - ADR-007: Fly.io BE deploy
  - ADR-008: OpenAPI auto-gen từ NestJS
- **New entities (DATA_MODEL.md):** `File`, `CommentLike`, `PostView`, `AnonymousSession`, `RefreshToken`
- **New enums:** `FileType` (7 values), `CommentStatus` (3 values); extended `Role` với `BANNED`
- **`docs/TESTING_STRATEGY.md`** — extract từ CLAUDE.md + expand đầy đủ test pyramid + tools + data strategy + E2E catalog (E2E-01→E2E-13) + CI plan
- **`docs/contracts/openapi.yaml`** — placeholder (sẽ auto-gen sau khi BE scaffold)
- **Root `README.md`** — quick start + stack overview + link docs/INDEX.md
- **Root `.env.example`** — env matrix theo DEPLOYMENT.md

### Removed

- **`docs/PROMPT.md`** — spec gốc obsolete sau khi đổi stack + theme + features. `REQUIREMENTS.md` mới là single source of truth (Vision + Personas + Use Cases + FR + NFR + Traceability)
- **`docs/DATABASE_SCHEMA.md`** (renamed → `docs/DATA_MODEL.md`)
- **`docs/API.md`** (renamed → `docs/API_CONTRACT.md`)
- **Light mode spec** trong DESIGN_SYSTEM.md (TBD — sẽ document khi có yêu cầu)

### Migration notes

- v1 codebase chưa init → no breaking deps update
- v2 docs là source of truth cho mọi implementation tasks sau này
- Recovery: nếu cần spec gốc cũ, recover từ git history `git show <commit>:docs/PROMPT.md`
- Light mode: recover từ git history khi cần

---

## Template thêm release mới

```markdown
## [X.Y.Z] - YYYY-MM-DD

### Added

- ...

### Changed

- ...

### Deprecated

- ...

### Removed

- ...

### Fixed

- ...

### Security

- ...
```
